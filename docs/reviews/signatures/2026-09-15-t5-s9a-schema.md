# Reviewer signature — T5 §S9a schema additions (prabhasa-nyaya)

Signed conditionally 2026-09-15 10:04 BST at `trackB/t5-grammar-additions` a06d876 (verified: suite 460 passed /
18 skipped in a throwaway worktree; diffs of `src/nyaya_ir/{models,validators,codec}.py` and goldens 21/22 read).

Approved: `UNSUPPORTED` rule-free abstention sentinel with the spike's precondition; `holding_selection` and
`statute_identification` kinds with `Target.dataset_label`, structurally forced to abstain with `dataset_label_only`.

Conditions before any SFT data is generated from the schema:
1. candidate ids in `SELECT`/`IDENTIFY` must resolve to passages/candidates in the immutable prompt; goldens 21/22 regenerated;
2. envelope-less parse path infers `holding_selection`/`statute_identification` from the wire line (negative test added).

Binding scorer rule: dataset-label kinds are scored by exact match / set-F1 and excluded from §4.4 abstention and
citation statistics; an `UNSUPPORTED` sentinel on a gold-supported record scores as a recall miss.
T5b (rule registry) may proceed in parallel. Condition lifted when the two fixes are re-checked.
