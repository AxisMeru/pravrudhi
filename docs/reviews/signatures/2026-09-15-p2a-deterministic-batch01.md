# P2a deterministic full-IR batch-01 — Track B content read, 2026-09-15 15:16 BST

`prabhasa-nyaya` `trackB/t5b-deterministic-records` @ `3adf566`: `scripts/p2a_deterministic_records.py`, `research/gates/P2a/deterministic_records/batch-01/{records,provenance}.jsonl` (31 records: 14 control, 14 omission, 3 denial).

## Passes
- **Scenario distinctness (amendment c): PASS.** The 14 control scenarios are materially distinct fact patterns (different parties, conduct, object, setting), each authored to its own limb: e.g. warehouse rice (405 misappropriation), cold-storage contract breach (405 use/disposal), locker keys (405 wilfully suffers), replica watch (415 property), co-signed loan (415 damaging act), exam-hall personation (416), false permit report / false smuggling report (182 a/b), false job-offer inducement (69), Mumbai→Dubai instigation (47), dowry harassment (85), forged magistrate letter (46 instigation), fence-cutting burglary plan (46 conspiracy), getaway car (46 aid). None is a rephrasing of another.
- **Schema conformance:** all 31 records pass `validate_record` and `codec.parse` under `src/nyaya_ir` (signed S9a) with the generator's rule registry; my older spike prototype rejects them only because it predates S9a.
- **REG discipline:** element strings from `--describe-contract`; 31/31 slices pass the live wire; provenance rows carry scenario_id, contract_id, slice, claim wires, expected/actual verdict, raw REG line and binary sha256, as ruled. Control ratio 45 %.

## HELD on one defect, two smaller items
1. **Omission slices contradict their prompts (all 14).** Each `__omit_0` record keeps the full fact list, including `F_el0` (the dropped element's own fact) and a narrative that itself asserts the element ("was entrusted with…"), while the target marks `el0` `unknown`, abstains with `missing_fact`, and answers "the account does not establish entrusted…". A model trained on this learns to abstain on facts that plainly establish the element. Fix: the omission slice's prompt must genuinely lack the element: drop `F_el{i}` and use a narrative variant in which that element is absent or neutral (same scenario lineage, `scenario_id` unchanged, variant recorded), with spans recomputed. The REG claim set already omits the element; the prompt must match it.
2. **`audit.checker_report` says `certified: false`** on every record although the REG run passed. The field is the structural validator's output and never updated. Fix: after the REG pass, set `checker_report` to `{structurally_valid, certified: true, checker: "reg", contract_id, binary_sha256, verdict}`; at replay the checker recomputes and must agree. A record whose own audit says uncertified cannot be counted as accepted.
3. At scale, vary the dropped element (`omit_0 … omit_{n-1}`) per scenario, as the cap in amendment (c) allows; batch-01 only ever drops the first.

Registry follow-up, not a hold: in the three denial slices the element "dishonestly misappropriates/converts" is `established` alongside the good-faith defeater. That is how the signed IPC 405 Contract encodes it (the mental word sits inside the element string), so the record follows the registry; when Phase 2b's typed verdict lands, the element wording and the denial should be reconciled so a record never asserts "dishonestly" and "not dishonestly" together. Minor: double full stop in the 405 denial answer text.

Status: **HELD** pending items 1–2; item 3 applies from batch-02. Scale-up (≈10 scenarios/id) may proceed in parallel with the fix as long as the generator change lands before batch-02 is emitted; I read batch-02 for distinctness and the fixed omission prompts.
