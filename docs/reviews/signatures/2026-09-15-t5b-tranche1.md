# Reviewer review — T5b rule registry tranche 1 (IPC 405, IPC 416, BNS 85)

Branch `trackB/t5b-rule-registry` 8e64f7a. HELD, not signed (2026-09-15 10:16 BST). `lake build` verified clean by
the reviewer in a throwaway worktree (the #guards compile, so the Contracts are internally consistent — that is not
the question; the content is).

Required fixes:
1. **IPC 405 is disjunctive at the offence level.** "in violation of any direction of law … or of any legal contract"
   qualifies only the use/dispose limb; misappropriate/convert carries no such condition. Split into Contracts A
   (entrustment ∧ dishonest misappropriation/conversion), B (entrustment ∧ dishonest use/disposal ∧ violation of
   direction/contract), C (entrustment ∧ wilfully suffers another to do A or B); #guard a misappropriation-only reading
   passes A. (The data-feasibility spike had flagged exactly this.)
2. **BNS 85's cruelty element has a quotable source**: `research/nyaya/corpus/bns.json` document "Bharatiya Nyaya
   Sanhita/Section 86 — Cruelty defined" ((a) wilful conduct likely to drive to suicide / grave injury; (b) harassment to
   coerce an unlawful demand). Quote it with provenance; shrink notFormalisable to the real residue.

Accepted: IPC 416 with `cheats` as an unformalised outer predicate; IPC 415 (present in ipc.json) goes first in tranche 2.
Binding registry rule: quotations come from the corpus JSON documents (id/act/section/provenance), never from the
eval JSONL; a definition in a neighbouring section is quoted, not declared unquotable.

## Re-check and signature — 2026-09-15 10:21 BST, `trackB/t5b-rule-registry` @ `3bd1106`

Range re-checked: `8e64f7a..3bd1106` (one commit, 4 files, +360/−194).

Verified by the reviewer, not taken from the report:
- `lake build` from `lean/` in a throwaway detached worktree at `3bd1106`: 125 jobs, exit 0 (worktree removed after).
- IPC 405 now three Contracts, one per limb the text names: `contractMisappropriation` (entrustment ∧ misappropriates/converts, no violation condition), `contractUseOrDisposal` (entrustment ∧ uses/disposes ∧ violates direction/contract), `contractWilfullySuffers` (entrustment ∧ wilfully suffers another). `#guard`s per limb: control, each limb's own omission, and the good-faith defeater refuting limb A only. Matches the fix I required.
- Good-faith denial quotes illustration (d) of s.405 verbatim from `research/nyaya/corpus/ipc.json` (checked, including the source's own "thought" for "though"; left as quoted, correct).
- BNS 85: `cruelty` sourced from s.86(a)/(b), both quoted verbatim from `bns.json` Section 86 (checked); residual gradation questions moved to `notFormalisable`. BNS 85 stays conjunctive (relationship ∧ cruelty). Correct.
- Provenance: both cited hashes (`fa8878e0…977f85` for IPC, `d4449e99…167f5` for BNS) are the file-level `source.pages[0].sha256` present in the corpus JSON. Accepted.
- IPC 416: re-sourced to `ipc.json` (text unchanged); the unsourced true-identity denial removed per Track A's shape fix; IPC 415-first ordering for tranche 2 recorded in the module doc.
- Design doc records both reviewers' fixes and the registry-wide quotation rule (corpus JSON only; neighbouring-section definitions quoted).

Open follow-up, accepted as flagged-not-fixed: `prabhasa-samskrutam/scripts/law_apply/items.py` IPC 405 v1 is ambiguous between limbs A and B. Decision: HOLD — do not edit the samskrutam item file in this tranche. It is re-authored when the `law_apply` set migrates into prabhasa-nyaya (directive T4 inventory item), where v1 becomes two items, one per limb.

**Content signature: SIGNED at `3bd1106`.** Merge range `8e64f7a^..3bd1106` (i.e. the whole branch), subject to Track A's shape sign-off (dual gate). Tranche 2 (IPC 415 first, then IPC 182, BNS 69, BNS 47) may start on a fresh branch from the merge.
