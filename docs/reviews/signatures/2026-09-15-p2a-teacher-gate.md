# P2a teacher gate — Track B reads and rulings, 2026-09-15 16:42 BST

## 1. Inventory D-count @ `assistant/trackA/p2a-inventory-d-count` `07606c0` (code `0e548f8`, an ancestor): CLEARED
D = 480 with records/provenance hashes; cross-check records == non-skipped provenance rows; 140 control / 30 deny / 310 omit; 40 skips excluded; 10 control scenarios per id; every verdict matched. **Ruling on the floor: 25 % is authoritative** (amendment (d), 2026-09-15 15:38 BST, superseded amendment (c)'s 40 %, which was structurally unreachable with one omission slice per element). 29.2 % passes. The inventory note may cite amendment (d) and drop the discrepancy text at the next regeneration; no other change.

## 2. P2a-manifest.json DRAFT @ `assistant/trackA/p2a-manifest-draft` `6e7533b`: NOT FROZEN; three rulings to fold in, then route back for the freeze signature
(a) **Per-id allocation**: the flat 143/142 split stays as the cap per id, but sampling runs in **rounds of 20 proposals per id (280 per round)**, stopping when T ≥ 200 and every id has A_i ≥ 5, or at the cap, whichever first. Same allocation, less spend; the round index goes into each attempt's provenance.
(b) **Teacher** (methodology mine; spend the lead's): the box has one configured API provider, DashScope international (chat.env, model qwen-plus), and the routing configs name `qwen3.8-max`. Proposal: **primary teacher `qwen3.8-max` via DashScope international**, snapshot id taken from the endpoint's model listing at freeze time and recorded; temperature 0.7, top_p 0.9, max_tokens 2048; seed per attempt = first 8 hex of sha256(attempt_id); system prompt: "emit only the nyaya-law-v1 wire form for the given prompt object; no prose"; user prompt = the record's `prompt` object rendered exactly as the D-tier records render it plus the grammar section of `docs/typed-ir/nyaya-ir-schema.md`; few-shot = 3 D-tier batch-03 records (one control, one omission, one denial) drawn from contract_ids other than the target id, fixed per id and listed in the manifest. Fallback teacher, only if the primary's parse rate < 0.5 after round 1: `gpt-6-astra` through the Codex CLI with the same prompt, recorded as a manifest amendment. Lead confirms DashScope credit availability and cost before freeze. Nothing samples until I sign the complete manifest.
(c) **`checker_adapter.verdict_field_mapping` (ruled here, same rule the D-tier generator uses):** a proposal asserts element *k* of `contract_id` iff `target.elements[k].status == "established"` AND it carries ≥ 1 evidence span into the prompt's facts; each such element becomes one `G_SATISFIES(E(the conduct in the facts,AC),E(<element string from --describe-contract>,EL))` claim; an element entry whose id is the Contract's denial entity with status established becomes the denial claim; anything else (unknown, missing, no evidence) is not sent and surfaces as `omitted`. Graph nodes and the teacher's AUDIT/answer text are never a source of claims. The expected verdict for acceptance is `grounded` for a control-shaped proposal, `flagged` naming exactly the elements the proposal itself marks not-established for an omission-shaped one, and denied for a denial-shaped one; a proposal whose self-declared shape and the checker's verdict disagree is rejected (`checker_disagrees_with_self_report`).

## 3. replay/decide DESIGN @ `assistant/trackA/p2a-replay-decide-design` `ab2c066`: CLEARED for implementation
Five-stage replay (round-trip, structural validation, independent span/hash/event/rule checks, REG certification, fail-closed), exact + narrative-only Jaccard dedup, lineage/template-family exclusion closing exclude's deferred item, decide as literal conjunction with invalidation before arithmetic: all as the prereg requires. **Ruling on the ambiguity: fresh checker call.** `replay` re-runs the compiled binary for every record; a teacher-time REG report is kept in `proposals.jsonl` only as a cross-check, and any disagreement between the stored report and the fresh call rejects the record (`replay_disagrees_with_sampling_time`). The binary sha256 in the manifest must equal the sha256 replay records for every call.

Addendum (via lead): DashScope international serves `qwen3.8-max` on the configured key (HTTP 200 ping by the lead); API spend signed off by the lead, bounded by rounds-of-20 and the T ≥ 200 / A_i ≥ 5 stop. Manifest additions: parse the `content` field only; `reasoning_content` stored verbatim, never parsed; `max_tokens` applies to content; reasoning budget/disable recorded if the API exposes it, else "not controllable"; truncated or empty content = malformed attempt in the denominator, retries are new attempts with their own `attempt_id`.

## Manifest @ `a3e7e26`: NOT FROZEN, one structural fix; replay/decide @ `2f2d719`: two rulings — 2026-09-15 16:59 BST

**Manifest defect:** `teacher.user_prompt_template` binds "the target record's own prompt object" from batch-03. The teacher would then emit IR for the D-tier's own 140 scenarios; by amendment (c) lineage (scenario_id) those are the same records, T would be a re-labelling of D, and the yield gate's T ≥ 200 would measure nothing about the teacher's ability to produce new supervision. Ruling: **the teacher batch samples new scenarios, two stages per attempt, both recorded under one `attempt_id`:**
- Stage 1 (scenario): given `contract_id`, its element list from `--describe-contract`, the limb read from the Contract's module doc, the chained-element and dependency rules, and a target shape (`control` | `omit:<k>` | `deny`) drawn round-robin, the teacher authors a prompt object (neutral narrative + one self-contained fact per element it intends to establish). Guards before stage 2: narrative-only Jaccard ≥ 0.40 against every D-tier scenario of the same id and every earlier accepted proposal of the same id → rejected as rephrasing (logged, counts in the denominator); the omission/dependency rules apply (an `omit:<k>` shape may not target a declared dependency; the dropped element's fact must be absent).
- Stage 2 (IR): the stage-1 prompt object rendered as the D-tier renders prompts, plus the grammar section, plus the fixed cross-id few-shot; output parsed from `content` only.
- Acceptance = stage-2 record passes replay (structure, spans into the stage-1 facts, fresh REG verdict agreeing with the self-declared shape). Provenance per attempt: `attempt_id`, `contract_id`, `shape`, `round`, `seed`, `stage1_prompt_sha256`, `jaccard_max_overlap` (+ against), stage-2 wire, REG raw, binary sha256.
Few-shot policy (fixed cross-id batch-03 examples) stands. The two deferred live values (`model_snapshot_id`, `reasoning_budget_control`) are fetched by the lead now and inserted; I sign the freeze when the two-stage prompt source and the live values are in.

**replay/decide rulings:** (1) canonical `target.elements` shape is the positional list in `--describe-contract` order, as the real records have it; `_normalize_elements` accepting a dict-by-name too is acceptable provided a dict with a name not in the element list is rejected, not dropped. (2) Near-duplicate dedup is required, not optional: reuse the D-tier script's constant (narrative-only Jaccard ≥ 0.40 reject, 0.30–0.39 flagged in `checks.jsonl`), computed on the stage-1 narrative against all D-tier narratives of the same id and all earlier accepted proposals of the same id; exact-hash alone does not meet the prereg's "rephrasings do not multiply" rule. Otherwise the implementation follows the cleared design.

Rulings (via lead, 17:02 BST): live values accepted (`qwen3.8-max-0902`; `enable_thinking:false` recorded as `reasoning_budget_control`; enabling thinking later is a manifest amendment with a new sha). Elements shape: one canonical positional form, dict-by-name rejected as `non_canonical_elements` (reconciled with Track A). P/R scope: `P_min`/`R_min` remain conjuncts; with no P/R data, `decide` emits **PARTIAL**, never PASS, citing the prereg lines; D and T reported in full; the teacher batch runs on the frozen manifest; P and R inventories are the next work item; P2b opens only on a six-conjunct PASS.

## Two-stage branch @ `ce9968c` — manifest content READ OK; replay HELD; freeze deferred — 2026-09-15 17:13 BST

Verified by me from a throwaway worktree at `ce9968c`: build 137/0; binary sha256 `9bff8f30…` equals the manifest's pin; `--describe-source bns69` returns the s.69 text; `decide` fails closed when `checks.jsonl`/`accepted.jsonl` are missing (correct). Manifest content: the two-stage teacher (stage-1 authoring with the ≥ 0.40 Jaccard guard before stage 2; stage-2 rendering with cross-id few-shot; provenance incl. `stage1_prompt_sha256`), live values (`qwen3.8-max-0902`, `enable_thinking:false` with the amendment-path fallback), rounds of 20, verdict mapping and self-report agreement, fallback teacher not pre-authorised: all as ruled. Nothing in the manifest content changes; the binary pin re-points to the post-`#guard` build per the lead, and the freeze names that SHA.

**replay defect (HELD):** stage 1 `_round_trip_ok` accepts a `wire` only if it starts with a REG/COMP/A3 tag. The records' `wire` field (D-tier batch-03 and, by the manifest, every teacher proposal) is the nyaya-law-v1 line grammar (`RECORD … NODE … CLAIM …`), which is what the teacher emits and what the schema round-trips. Reproduced: an untouched batch-03 control record submitted as a proposal is rejected at `round_trip`; so are all three test proposals; `contract_id` never gets derived. As written, replay would reject 100 % of valid proposals, and the "literal D-tier copy caught at Jaccard 1.0" test cannot have been run against the real `wire`. Required fix: stage 1 = a real round-trip, `nyaya_ir.codec.parse(wire, record.prompt, registry=<contract's rule registry>)` succeeding and re-encoding to a target equal to `record.target` (or, if the codec has no encoder, structural equality on elements/claims/trace); the REG line is built at stage 4 from the canonical elements, as the design already says. Re-test with the three cases (D-tier copy → `near_duplicate_rephrasing` at 1.0; dict elements → `non_canonical_elements`; new narrative → accepted) and commit the test proposals and `checks.jsonl` as evidence.

Run order accepted: reachability regenerated against the pinned binary before replay; decide's "binary changed since reachability" check enforces the pin.

## MANIFEST FREEZE SIGNED — 2026-09-15 17:33 BST, `assistant/trackA/p2a-two-stage-teacher` @ `79dc50b`

Verified by me from a throwaway worktree at `79dc50b`: `lake build` 137/0; binary sha256 `9bff8f30…` equals the manifest pin and `reachability.json`'s recorded binary (reachability PASS, regenerated against the pinned binary; run order honoured). Manifest content unchanged from the `ce9968c` read (two-stage teacher, stage-1 Jaccard guard, `qwen3.8-max-0902`, `enable_thinking:false`, rounds of 20, verdict mapping, self-report agreement, fallback not pre-authorised). replay stage 1 is now a real `nyaya_ir.codec` round-trip with structural equality against `record.target`; REG built at stage 4 from canonical elements.

Replay re-tested by me on my own three proposals: D-tier copy → `near_duplicate_rephrasing`, Jaccard 1.0, `contract_id` derived; dict elements → `non_canonical_elements`; new narrative with a stale wire → `round_trip` rejection (correct: the wire's embedded prompt hash no longer matched the edited prompt, i.e. tamper protection), and the same record with its wire re-serialised by the generator → accepted, Jaccard 0.03, self-report agrees. The committed evidence artifact (`research/gates/P2a/evidence/track-b-round-trip-retest/`) shows the same three outcomes from replay's own output.

**Freeze: SIGNED at `79dc50b`.** The frozen manifest is `research/prereg/P2a-manifest.json` at that SHA (status text to be flipped to FROZEN with this signature cited in the merge commit, no other edit). Lead merges the range onto main on Track A's confirmation at the same SHA; the teacher batch then samples by API in rounds of 20 per id under the signed spend, writing `proposals.jsonl` immutably; `replay` then `decide` (expected PARTIAL until P/R exist). Any change to the manifest after this point is an amendment with a new sha and a new signature.

## Round-1 T read (partial, batch paused at the DashScope quota wall) — 2026-09-15 18:27 BST, `assistant/trackA/p2a-teacher-sampling` @ `19da65e`

Read against the pushed artifacts (sha256 `proposals.jsonl` `cdec04a0…`, `sampling_log.jsonl` `70745c61…`, `checks.jsonl` `bfe30772…`, `accepted.jsonl` `5004c246…`, `yield.json` `53d22d11…`, `gate.json` `85075958…`); byte-identical to the working-tree snapshot I took at 18:21. Every `checks.jsonl` row carries the pinned binary `9bff8f30`. Recount by `jq` on the raw files, not by the gate script.

**Counts (corrected).** 239 attempts, 12 of 14 ids reached (bns46_conspiracy, bns46_intentional_aid never sampled; bns85 and bns46_instigation 0 accepted, every attempt an API error). 118 sampler-accepted, 66 replay-accepted, decide FAIL (T 66 < 200; A_i floor unmet). API errors: 41 stage-1 + 1 stage-2 quota 403s, plus 3 content-moderation 400s (all bns69 omit:0, a recurring denominator cost for that id). ipc405_misappropriation is 10/20 sampler-accepted (8 after replay), ipc405_use_or_disposal 9/20 (5); the "20/20" figures were attempts, not acceptances. Token use so far 928k prompt / 80k completion.

**The 66/118 gap is systematic, not teacher noise.** The 66 replay-accepted records are all `control`. Every one of the 48 omission-shaped proposals was rejected `not_grounded`; the other 4 rejections are controls where stage 1 authored 1–2 facts for a 3-element contract and stage 2 still asserted `_POS` — correct refutations, 4/70 = 6 % teacher noise. The omission slice yields 0 by construction, on three defects that stack:

1. **replay contradicts the frozen manifest for non-control shapes.** Manifest `stage_2_ir_rendering.acceptance`: "a fresh REG verdict that agrees with the proposal's own self-declared shape". replay computes `self_report_agrees` (true on all 52) and then unconditionally requires `actual_verdict == "grounded"` (`p2a_yield_gate.py` ~L1100), so an omission whose checker verdict is the correctly diagnosed `flagged` can never be accepted; the same holds for denial shapes. Conformance fix, no amendment: verdict must be shape-conditional (control → grounded; omit:k → flagged with exactly element k in the omitted bucket; deny → denied).
2. **harness envelope hard-codes `kind="law_apply"`** (`p2a_sample_teacher_batch.py` ~L343) while `validators.py:143` requires kind ↔ `target.abstain`. The 16 `stage2_envelope_serialize_failed: kind/abstain mismatch` attempts (all omission shapes: bns69 7, ipc416 4, ipc405m 3, ipc405u 2) are the cases where the teacher DID abstain, i.e. imitated the D-tier omission exemplar (`law_abstain`, `ABSTAIN true missing_fact`, `NIGAMANA not_proved`) and was killed by the harness. Fix: kind derived from `target.abstain`. Raw stage-2 text is not logged on this failure path (only on parse failures), so their content cannot be audited; log it.
3. **the 48 that passed the envelope are wrong records**: element k absent from the prompt AND from the record (no `ELEMENT elk e unknown`), yet `CLAIM c_applies … BY <ID>_POS`, `NIGAMANA proved`, `ABSTAIN false`. That is the teacher concluding without an element. Nothing in the manifest's acceptance rule checks target/verdict consistency, so under fix (1) alone these would be accepted as T supervision. **Ruling (amendment, my signature): T acceptance adds target–verdict consistency** — grounded ↔ `proved`/`ABSTAIN false`; flagged (omission) ↔ `not_proved`/`ABSTAIN true missing_fact`/omitted element `unknown`/`kind law_abstain`; denied ↔ the D-tier deny convention. With that rule the 48 stay rejected, and correctly so. The stage-2 prompt invites the error: it names only the `_POS` rule and gives no omission instruction. **Amendment (pre-approved wording):** append to the stage-2 prompt: "If the prompt has no fact for an element, mark that element `unknown`, cite no claim for it, emit `NIGAMANA not_proved` and `ABSTAIN true missing_fact`, as in the omission example." New manifest sha + Track A shape sign + my signature; then the batch resumes.

**Sampler shape bug (harness, no amendment).** `shape = shape_cycle[(attempts_by_id[cid] + i) % 4]` while `attempts_by_id[cid]` is also incremented per attempt, so the index advances by 2: shapes alternate control/omit, `deny` is never reached (0 deny attempts in 239), and `k = index % n_required` collapses for even element counts — 2-element ids only ever get `omit:0`, 4-element ids only `omit:2` (observed: omit:0 74, omit:1 15, omit:2 30). For ipc405_wilfully_suffers, whose only omit index is its declared dependency, 10 of 20 attempts were rejected pre-API (`omit_targets_declared_dependency`, 19 across four ids). The manifest (guards_before_stage_2) says such attempts are "rejected at stage 1", so they legitimately sit in N_i for round 1; going forward the sampler must draw only from allowed shapes (index from `attempts_by_id` alone; k over non-dependency indices; deny for denial-bearing ids).

**decide denominator does not match the frozen `denominator_policy`.** `yield.json` N_i sums to 118 (proposals only); the manifest counts every attempt (malformed, refusal, checker error) — that is the 239 rows of `sampling_log.jsonl`. A_i/N_i is therefore inflated (ipc405_wilfully_suffers reads 1.0; it is 6/20). Conformance fix: N_i from `sampling_log.jsonl`, A_i unchanged. 26 `stage1_json_parse_error` rows (all `finish_reason: stop`, 78–196 completion tokens, "Expecting ':' delimiter" at ~600–900 chars) and 14 `KarakaRole` parse failures (`Agent`, `KARTA`, `Kartr`…) are unrecoverable without the raw text; log stage-1 raw content on failure, and the grammar block should enumerate the six role tokens (the schema's `ROLE` line does not). A `response_format: json_object` change would be a sampling-parameter amendment; not requested.

**Standing.** The 66 replay-accepted controls stand as T records (binary pinned, recount agrees, Jaccard max 0.39, 12 flagged 0.30–0.39). FAIL is the honest decide state; nothing signed, the freeze stands. Before any further spend: fixes (1), (2), sampler, denominator, logging; then the amendment (stage-2 omission instruction + target–verdict consistency rule) with a new sha routed to me; replay re-run on the existing 118 (no spend) to show the 48 still reject under the new rule and the 66 still pass; then resume to T ≥ 200 with A_i ≥ 5 on all 14 ids. Denial and omission slices must be present in T (they are 0 today) — I will not read a T tier that is 100 % controls as meeting the manifest's shape cycle.

## Teacher swap notice (operator-directed, via lead) — 2026-09-15 18:33 BST

Lead reports the operator has directed the teacher to change from `qwen3.8-max-0902` (DashScope quota exhausted) to `claude-haiku-4-5-20251001` run headless on seat 2, riding in the same amendment sha as fix 3. No objection in principle. Signature conditions sent to the lead: (1) sandbox as a binding manifest field — all tools disabled, empty cwd outside any checkout, no `--add-dir`, `--output-format json`, argv logged per row (the checker does not catch a teacher that reads the held-out sets); (2) sampling fields stated honestly — no seed/temperature/top_p claims, non-seeded, attempt_id the identity, thinking off, malformed output stays in the denominator; (3) per-record `teacher_model`, the 66 qwen controls stay as qwen records, yield split by teacher; (4) a content read of accepted records for fact–element fit is a non-optional part of the T read, since the checker validates the IR layer only. Flagged, not ruled: seat-2 load versus its pod stand-by role.

## Harness fixes 1/2/4/5 @ `efc65d9` — pre-read, one gap — 2026-09-15 18:38 BST

Scope change noted from the lead: the amendment sha will be fix 3 only; the teacher stays `qwen3.8-max-0902`; the Haiku swap and my four conditions are deferred to a possible follow-on amendment. Fine — my read said qwen was not what broke.

Read of `efc65d9` (code-only, on `assistant/trackA/p2a-teacher-sampling`): fix 2 kind derived from `target.abstain` (L350) — OK. Fix 1 replay verdict is shape-conditional (control → grounded and no denial; omit:k → flagged with the dropped element in the omitted bucket; deny → flagged with a denied bucket; any unmapped claim rejects under every shape) — OK, and the lead reports fix 1 alone passes 90/118, confirming the 24 proved-with-missing-element records need fix 3. Fix 5 N_i from `sampling_log.jsonl` with a fallback when absent — OK. Raw stage-1 content and stage-2 target logged on every failure — OK. **Gap in fix 4:** `allowed_shapes` (L506) lists `omit:k` for every k, so dependency-target omissions (ipc405_wilfully_suffers omit:0, ipc405_use_or_disposal omit:0, ipc182_misdirected_act omit:0, ipc415_damaging_act omit:1) are still drawn and still rejected pre-API; with the shorter cycle they are now 1 in 5 attempts for wilfully_suffers rather than 1 in 2. The list must exclude the dropped index in `OMISSION_DEPENDENCIES[cid]`. The shape index now advances by 1 and `deny` is reachable — OK. Control share per cycle is 2/(n+2) or 2/(n+3): 25 % for bns47 with denial absent, i.e. at the amendment (d) floor; acceptable, to be reported in yield.

## AMENDMENT fix-3-target-verdict-consistency @ `7d7f6cb` — content SIGNED at the git sha; two conditions — 2026-09-15 18:44 BST

Verified from origin (`assistant/trackA/p2a-teacher-sampling` @ `7d7f6cb`): teacher unchanged (`qwen3.8-max-0902`); my omission instruction line verbatim in the stage-2 template; `.acceptance` carries the target–verdict consistency rule (grounded ↔ proved/abstain false; flagged ↔ not_proved/abstain true missing_fact/kind law_abstain; denied ↔ D-tier deny convention); replay enforces `target_verdict_inconsistent` after the fix-1 shape check; fix-4 gap closed (`allowed_shapes` drops the `OMISSION_DEPENDENCIES` index); yield reports control share. No-spend re-run on the same 118, recounted by me: 66 accepted (all controls), 28 `not_grounded`, 24 `target_verdict_inconsistent` (all `omit:2`, the proved-with-missing-element records), binary `9bff8f30` on every row, N_i sums to 239 (fix 5 in effect), decide FAIL. This is the evidence I asked for.

**Signature: the amendment content is SIGNED at git `7d7f6cb`.** Two conditions, neither a content change:

1. **The content sha is not reproducible.** `status` cites `d9e3da0d…` and `amendments[0].prior_sha256` cites `96b3b61c…`; `amendments[0].new_sha256` is null. Neither value reproduces from the committed file by any obvious rule (raw bytes; `jq -S` with/without `status`, `signatures`, `amendments`; `json.dumps(sort_keys)`), and the raw sha of the manifest at `79dc50b`/`3c43fd5`/`efc65d9` is not `96b3b61c` either. The manifest states no rule. A freeze sha nobody can recompute is not a pin. Required: the manifest states the rule (recommended: sha256 of `json.dumps(manifest_without_status_and_signatures, sort_keys=True, ensure_ascii=False)`, computed by `p2a_yield_gate.py` and printed by a `--manifest-sha` flag), `new_sha256` populated, and `prior_sha256` recomputed by the same rule from the manifest at `3c43fd5`. Until then the binding identity of this amendment is the git sha `7d7f6cb` and this signature attaches to that.
2. **Stage-1 fact labelling bug (harness, fix before resume, no manifest change).** `stage1_prompt_obj.facts` labels the teacher's fact list by position (`enumerate(facts)` → `F_el{i}`), and the stage-1 prompt asks for a plain list "or omitted per the shape". For any `omit:k` with k < n−1 the list is one short and every later fact is mislabelled: the 12 `omit:0` records carry `F_el0`/`el0` for what is really element 1 (the D-tier exemplar keeps the true index, `F_el1`). Such records are rejected by the fix-1 shape check (21 of the 28 `not_grounded` are these), so no validity leak, but omission yield for k < n−1 is 0 by construction. Fix: stage 1 returns facts keyed by element index (`{"0": …, "2": …}` or a null at the omitted slot) and the harness labels `F_el<index>`; the stage-2 prompt gains one line: "element ids are positional: el<k> is the k-th required element; a fact `F_el<k>` supports el<k> only". If the lead treats the stage-2 line as manifest content, fold it into the next sha with the teacher decision; I pre-approve that wording.

On (1) resolved and Track A's shape-sign, merge. Resume still waits on the operator's teacher decision and on (2).

## Sha rule reconciled; T coverage shape given to d0 — 2026-09-15 18:48 BST

Track A's shape-sign is in; it independently found condition 1 (status embeds its own hash). Lead reconciled the content-sha rule to mine (exclude `status`, `signatures`, amendment sha fields; sort_keys; ensure_ascii=False; `--manifest-sha` flag); both reviewers recompute the pin on d0's next tip before merge. Resume bucket additions: Track A's deny comparison against `DENIAL_ELEMENT_BY_ID`; my condition 2. Correction sent: the four never-omitted elements are the dependency-dropped side of the frozen definitional dependencies and must stay unsampled by rule; the labelling bug is what zeroes the other indices. Coverage shape given: omission directions = every index except the dependency-dropped one; deny for 405m/405u/bns69 only; control ≥ 25 % per id; yield reports accepted per direction and per deny. Advisory (not a frozen conjunct): ≥ 1 accepted per allowed direction and ≥ 1 deny per denial-bearing id on top of A_i ≥ 5, applied as a signing check on the T read.

## Condition 1 met @ `20c6aa4` — manifest sha reproduced; amendment SIGNED at content sha `391535fa…` — 2026-09-15 18:50 BST

Rule now stated in the manifest (`manifest_sha_rule`: strip top-level `status` and `signatures` and each amendment's two sha fields, then sha256 of `json.dumps(sort_keys=True, ensure_ascii=False)`). Recomputed by me two ways: my own six-line script on the raw file from origin, and `p2a_yield_gate.py manifest-sha` from a throwaway checkout at `20c6aa4`. Both give `391535faee670db6cc074223d33aff9056a8cccd1da44f4f8075f20c52c102a4`; the prior over the `3c43fd5` base gives `7bb639390211f775bcd613b699eca0f5a4fa6662fc0ad418ae32305b6df1debf`; both equal the manifest's fields. Content diff `7d7f6cb`→`20c6aa4` touches only the sha fields, `status`, the rule text and the script. **The fix-3 amendment signature now binds to content sha `391535fa…` (git `20c6aa4`).** Lead merges on Track A's confirm. Teacher decision: operator stays on qwen3.8-max (paid credits); no Haiku amendment; the next sha carries condition 2, Track A's deny comparison and per-direction reporting only.

## Resume-bucket amendment condition-2-positional-fact-labelling @ `32aa071` — content SIGNED at `fd5ec12e…`; merge HELD on evidence — 2026-09-15 18:58 BST

Pin: my script and `manifest-sha` from a throwaway checkout both give `fd5ec12eba7040c8d404d0beff6aabebd23e4480e87391a9b792d70931874dd6`; prior `391535fa…` = merged fix-3 tip. Content diff vs main: the amendment entry plus my positional-element-id line verbatim in the stage-2 template; nothing else. Harness: stage 1 must return exactly n facts (n+1 for deny) with `""` at the omitted slot, enforced before indexing (`stage1_fact_count_mismatch`, raw logged), so `F_el<i>` is now the true index. Replay: deny branches compare the checker's denied bucket to `DENIAL_ELEMENT_BY_ID[cid]`; accepted rows gain `shape` and `teacher_model`; decide gains `accepted_by_direction_i`. Coverage shape is the one I specified. **Content: SIGNED at `fd5ec12e…`.**

**Merge HELD on one thing:** the evidence at `32aa071` was not regenerated — `research/gates/P2a/` is byte-identical to main, `accepted.jsonl` rows carry no `shape`/`teacher_model`, and `yield.json` has no `accepted_by_direction_i`. The reported "still 66/118, all-control per direction" was not produced by this code as committed, so the new reporting path is untested. Required before merge: replay + decide re-run at the tip, committed, with the fields present and 66/28/24 unchanged. Not a content objection.

## Evidence tip `a68aa45` recounted — hold released; merge and resume GO fine from Track B — 2026-09-15 19:01 BST

Manifest byte-identical to the signed `32aa071` (content sha `fd5ec12e…` carries). Evidence regenerated by this code: `accepted.jsonl` 66 rows, all `shape=control`, `teacher_model=qwen3.8-max-0902`, binary `9bff8f30`, the same 66 attempt_ids as main; `checks.jsonl` 66 / 28 `not_grounded` / 24 `target_verdict_inconsistent`, binary pinned on every row; `yield.json` T 66, N 239, `accepted_by_direction_i` present (all-control for round 1, as expected before the cycle fix), `control_share_i` reported; decide FAIL. One harness change after my sign, read and accepted: `stage1_fact_count_mismatch` becomes a fixed reason string with got/expected as their own log fields (Track A's note), no behaviour change.

Resume expectations restated for the T read: rounds of 20 per id across all 14 ids; omission directions per the coverage shape and deny for 405m/405u/bns69 actually drawn; per-direction acceptance reported; advisory ≥ 1 accepted per allowed direction and ≥ 1 deny per denial-bearing id checked at signing; a Sonnet seat-2 content read of accepted records for fact–element fit before I sign T.

## Condition-2 amendment MERGED (`c65f132`); resume snag on the teacher pin — 2026-09-15 19:07 BST

Main `c65f132` carries both amendments; content sha `fd5ec12e…` reproduces on main. Resume GO released, then stopped cleanly by d0: the paid credits do not serve the exact pinned snapshot `qwen3.8-max-0902` (135 attempts, all 403 FreeTierOnly, 0 spend); the alias `qwen3.8-max` and other qwen models succeed on paid. The lead's earlier live check had tested the alias, not the snapshot. Operator being asked: (a) enable paid access for -0902 (preferred, no amendment), or (b) amend the request string to the alias. My conditions for (b), sent to the lead: alias only if every response's returned `model` field is recorded per attempt and equals `qwen3.8-max-0902`, with any other value rejected as `teacher_model_mismatch` in the denominator; if the response reports only the alias, the resolution is unproven and a dated paid snapshot must be pinned instead and treated as a teacher change with the existing per-teacher split reporting; the amendment states request string, required resolved string and the date of the models-list check; the 135 zero-output 403s stay in N_i per the frozen policy but yield reports `infra_error_count_i` separately.

## Teacher-change amendment teacher-model-change-qwen3.7-max @ `642b856` — content SIGNED at `c87bb4d1…` — 2026-09-15 19:17 BST

Operator approved the teacher change and delegated the pick: `qwen3.7-max-2026-06-08`, a paid fixed dated snapshot whose response.model echoes the dated id (the alias failed that test and is out). Pin: my script and `manifest-sha` from a throwaway checkout both give `c87bb4d149827b9ed0ae300492857d1cbeac87b078fcaaf8d7540007d8231b08`; prior `fd5ec12e…` = merged condition-2 tip. Manifest diff vs main: model/model_snapshot_id, a snapshot note, `response_model_guard`, `sandbox` (N/A, API is the only channel), `sampling_params_note` (0.7/0.9/seed kept), the amendment entry, and the note that my content read stays mandatory. Nothing else. Code: harness records `stage1_resolved_model`/`stage2_resolved_model` from response.model and rejects `teacher_model_mismatch` into the denominator; replay reads `teacher_model` from each proposal (fallback label `qwen3.8-max-0902` for round-1 rows); decide adds `T_by_teacher`, `A_i_by_teacher`, `infra_error_count_i`. Evidence regenerated by this code: 66/28/24 unchanged, all 66 labelled `qwen3.8-max-0902`, `T_by_teacher` = {qwen3.8-max-0902: 66}, infra errors bns85 20, bns46_instigation 19, bns69 3, bns47 2. **Content: SIGNED at `c87bb4d1…` (git `642b856`).** Merge on Track A's sign; resume GO on the new snapshot.

Advisory (not blocking): the 3 bns69 rows counted as infra are DashScope content-moderation 400s (`data_inspection_failed`), a teacher-side refusal on the content, not infrastructure; report them under a `content_moderation_count_i` when the next sha happens. The hard-coded fallback teacher label should apply only to rows that predate the field; the harness now always writes it, so fine as long as that stays true.

## Round-3 read @ `da2d129` — content signal POSITIVE, but a structural HOLD (attempt_id collision on resume) — 2026-09-15 19:35 BST

New-teacher (qwen3.7-max-2026-06-08) first output: 26 replay-certified acceptances, 13 control / 4 omit:0 / 9 omit:1 — omission acceptances for the first time, so condition-2's positional fix works; Jaccard max 0.28 across the 26; binary `9bff8f30` on every row; `resolved_model` on every WRITTEN row equals the pin (api-error rows carry none, correct); 3 deny attempts drawn and correctly rejected (teacher authored no denial fact); infra vs moderation split present. That is the positive signal the lead flagged.

**HOLD — attempt_id collides across the resume boundary.** The resume restarted the sampler's counters (round_idx→1, attempts_by_id→0) instead of continuing them, so every new attempt is `t_<cid>_r1_<nnnn>`, colliding with round-1's ids. Confirmed: `sampling_log.jsonl` 283 rows / 239 unique; `proposals.jsonl` 150 / 134 (16 dupes); `accepted.jsonl` 92 / 84 (8 accepted ids each name two different records, different teacher, different content — e.g. `t_ipc405_misappropriation_r1_0000` is Kavita/sealed-parcel under qwen3.8 AND "sold the collection at auction" under qwen3.7). Consequences: (1) `proposals.jsonl` is supposed to be append-only immutable keyed by attempt_id — the key is now ambiguous; (2) `seed = sha256(attempt_id)` reuses the SAME seed (2650936628) for two different prompts, breaking the per-attempt determinism claim; (3) the `round` field is null on all 26 new records, so the manifest's promise ("every attempt carries its own round_index; a round-3 attempt is distinguishable from a round-1 attempt") fails; (4) any downstream consumer keying by attempt_id silently drops the 8 collided records. T=92 counts correctly TODAY only because decide iterates rows rather than keying by id — at 200+ the collisions multiply.

**Required before scaling (harness, no manifest change):** attempt_id must be globally unique across resume boundaries — resume continues the counter, or prefixes a run-id/teacher tag, or a persisted monotonic counter; the `round` field is populated on every record; `proposals.jsonl` append refuses an id that already exists with different content. Re-emit round 3 under unique ids before resuming to 200 (no re-spend needed if the existing 26 are re-keyed deterministically from their own content/teacher, but the seed-collision means a clean re-sample is cleaner). Content read (Sonnet seat-2, spikeF) is running in parallel; verdict to follow.

## Round-3 content read (Sonnet seat-2, spikeF) — NOT clean; narrative non-neutrality + template repeats — 2026-09-15 19:39 BST

Reader output committed at `docs/reviews/spikes/trackb-p2a-tread/content-read-r3.json`. Per-question FAILs over the 26: fact_fit 2, narrative_neutral 22, omission_real 0, denial_real 0 (n/a), coherent 0, english_register 0. The teacher itself authors well — coherent 26/26, plain-English register 26/26, omissions genuine 26/26, fact_fit 24/26 — so this is NOT a qwen3.7-max quality rejection. Two systematic supervision-quality defects, both stage-1-prompt, fold into the re-sample:

1. **F_narrative is not neutral (22/26).** The stage-1 template already asks for "neutral scene-setting, no element content", but the teacher ignores it: the narrative states el0 (the entrustment/dominion relationship) and often el2 (the governing direction) in full, before the element facts restate them. Verified directly: use_or_disposal 0003's narrative names Priya Sharma "appointed as the estate executor… the will explicitly mandated…" — that is el0 and el2 established in the narrative. The checker cannot see this (it validates only that element k's evidence span sits in F_el<k>), so a record whose narrative pre-establishes an element is certified anyway. This round no omit record leaked its OWN omitted element (the 4 omit:0 kept the narrative anonymous — 0000: "a dispute arose… legal counsel reviewed"), so no false omission slipped THIS time; at scale an omit whose narrative leaks element k would be a false omission the checker passes. This is exactly the licensed-but-wrong supervision the content read exists to catch. **Fix: a stage-1 narrative-neutrality guard (reject before stage 2 when F_narrative's content overlaps any F_el<k> beyond a threshold, or independently establishes an element), plus stronger prompt wording. A guard is more reliable than wording for a weaker model.** The 2 fact_fit FAILs are a downstream symptom (element facts redundant because the narrative already stated them).

2. **Template repeats (3 pairs in 26).** Priya-Sharma-estate-executor (0003/0007), informal-village-entrustment (0002/0012), and Elias-Thorne-archival-custodian which repeats the SAME character across two different contracts (misappropriation 0013 / wilfully_suffers 0002). The narrative-only Jaccard missed all three because wording varies while character/template repeats. **Fix: cap identical scenario frames per id (my earlier advisory ≤ 4) and add a character-name/template diversity check across ids, not just narrative-token Jaccard.**

**Verdict: HOLD the round on content too.** Both fixes fold into the re-sample d0 is already doing for the attempt_id collision. Neither changes the teacher pin. Whether the narrative-neutrality guard needs a manifest amendment (the template wording is manifest content) or rides as harness code is the lead's call; I sign either, but no round scales to T≥200 until narrative neutrality is enforced and the template-repeat rate is controlled. The 66 round-1 records stand as the T baseline unchanged.

## Next sha spec: neutrality guard + wording + diversity — pre-approved wording and guard calibration — 2026-09-15 19:42 BST

Lead takes both (guard = hard enforcement of the validity hole; stronger stage-1 wording as a manifest amendment to cut the leak rate so the guard only mops up the residual, prior c87bb4d1). My inputs so d0 builds to a signable target:

**Guard calibration (my sign checks this).** A token-overlap threshold guessed in the abstract will mis-fire. The guard must be calibrated and its calibration committed as evidence: run it over the existing round-3 26 and show it REJECTS the 22 narrative_neutral FAILs and PASSES the 4 clean omit:0 records (0000/0005/0010/0015). Gold-standard definition of a leak, to calibrate against: strip all F_el facts, leave only F_narrative, and if the checker would establish any element from narrative spans alone, the narrative leaks — the guard is a cheap proxy for that and must agree with it on these 26. Commit the guard's per-record output alongside the re-sample.

**Pre-approved stronger stage-1 neutrality wording** (drop-in for the narrative field spec, replacing "neutral scene-setting, no element content"): "A 2-3 sentence factual backdrop naming only the parties and the setting. It MUST NOT state or imply any required element: no entrustment or custodial relationship, no governing direction/contract/duty, no dishonest act, use, disposal, deception, or mental state. Test: a reader seeing ONLY this narrative must not be able to tell whether any element is satisfied. Each element's content appears ONLY in its own F_el<k> fact."

**Diversity check definition.** Cheap hard check: the primary party name in F_narrative must not repeat a prior accepted record's, across ALL ids (this alone catches Elias-Thorne-across-two-contracts and the Priya-Sharma pair). Softer: a scenario-frame signature (relationship type + object class) capped at ≤ 4/id. Narrative-token Jaccard stays but is not the diversity gate — it missed all three repeats.

I sign the sha on: pin reproduces; the wording is verbatim the above (or a variant Track A and I agree); the guard-calibration evidence shows 22 reject / 4 pass on the round-3 26. Then re-sample under guard+reworded prompt, fresh batch to me for the content read.

## Amendment narrative-neutrality-and-diversity-guards @ `42b8a90` — content SIGNED at `bba608d2…`; two architecture rulings — 2026-09-15 19:53 BST

Pin reproduces (my script = `bba608d2821659c61478232dc0eaf56b823b49637b44a27da6ff53942387ea12`; prior `c87bb4d1`). Wording verbatim mine. Diversity guard = primary-party-name hard gate across all ids + frame-signature ≤4/id soft cap, as specified. Calibration committed (`narrative_leak_guard_calibration.json`): 0 false positives on the 4 gold-clean (0000/0005/0010/0015), 9/22 recall on the gold-leaky, gold standard = my content read. Crucially the manifest describes the guard HONESTLY — "a cheap regex proxy, not a full semantic replacement for Track B's content read… reported as measured, not claimed" — and keeps the content read MANDATORY before I sign T. No overstatement, so **content SIGNED at `bba608d2…`.**

**The lead's architecture point is correct and I rule on it.** A bag-of-words guard cannot reach 22/22 because the leaks are semantic paraphrases (use_or_disposal 0003 scored zero overlap); my own gold standard ("strip F_el, does the checker establish an element from the narrative alone") is itself a semantic judgment needing an LLM. A 22/22 regex is an impossible bar; holding the sign on it would block on something that cannot exist.

**Ruling (a) — sign now, recall deferred to a measured re-sample: YES.** The signature covers the verbatim wording and the diversity guard; the 41%-recall/0-FP regex is a cheap first-line pre-filter, not the neutrality gate, and the manifest says exactly that.

**Ruling (b) — the T-validity gate at scale is a FULL content read of every accepted record, not a slice.** With a sub-100%-recall guard, weak-guard + slice-read is the one unsound combination (a leak that passes the guard and misses the slice enters T). The content read is cheap relative to teacher sampling — one Sonnet seat-2 agent read all 26 this round in a single call; a few hundred records is a handful of calls — so a full read is affordable and is the authoritative semantic check. I decline the Haiku-guard-so-the-read-can-stay-a-slice option: it puts a second, weaker model with its OWN uncalibrated recall and its own provenance into the sampling loop, to save a read that is already cheap. Operationally the full read is INCREMENTAL: each round's newly-accepted records get a full content read before they count toward T, and I sign T only when every constituent record has passed. The reworded prompt + regex pre-filter are efficiency (fewer wasted stage-2 spends, fewer leaks reaching the read), not the gate. Escalate to an LLM guard only if a measured round shows the full read is impractically large — and even then it would supplement a read, after its own recall calibration, never replace it.

Next: re-sample a small batch (~20-40) under the reworded prompt + guards + the attempt_id-collision fix; I full-read it and we measure the NEW leak rate (informs efficiency, not whether the read is needed). Merge on Track A's shape-sign.

## Diversity hard-gate flaw (my spec's fault) — reframe ruling + pre-approved wording — 2026-09-15 20:01 BST

d0's measured re-sample (round4-measured branch, 12 attempts, 0 accepted, 0 spend) rejected 9/12 as `primary_party_name_reused` — d0 flagged rather than loosening a signed guard, correct. The flaw is in MY spec, now in the manifest text I signed (line 91: "primary party name (first proper noun in the narrative)"): keying the FIRST capitalized token against ALL-TIME accepted history is a lifetime ban on common names. Worse, the extractor `_primary_party_name` returns the first Title-case token, which is often not a person — round-1's top "names" are Meera 26, Pune 23 (a city), Ravi 17, Tuesday 10, Their 10. So the gate bans cities and days too. My finding was never "no two records share a first name"; it was the SAME DISTINCTIVE CHARACTER in the SAME FRAME (Elias Thorne across two contracts; the Priya Sharma executor pair).

**Consequence to note: neutrality is STILL UNMEASURED.** 0 accepted this round is the diversity gate over-rejecting, not the reworded prompt passing — the whole point of the round (the new leak rate) is not yet known.

**Ruling (1) — scope: the hard cross-id gate fires only when BOTH the primary party's full name AND the frame signature match a prior accepted record.** The frame conjunction is the load-bearing fix: "Ravi in a warehouse entrustment" and "Ravi in a bank fraud" are different frames and must not collide; only "the same named character in the same scenario frame" is the repeat I flagged. Also fix the extractor to target a person name (the first run of one-or-more consecutive capitalized name tokens, excluding place/day/sentence-initial words), not the first capitalized token. Keep the frame-signature ≤4/id soft cap (it does the per-id anti-padding) and narrative-Jaccard (near-dup). I decline the recency-window and cap-K alternatives — both still key on the wrong thing (name alone) and either ban common names within the window or admit exact-frame repeats above K.

**Ruling (2) — manifest amendment, both sign.** The flawed rule is manifest text I signed, so correcting it is a new sha, not a harness-only fix. Pre-approved wording (drop-in for the diversity clause): "the HARD cross-id gate rejects an attempt (rejection_reason character_frame_reused) only when BOTH the primary party's full name — the first run of one or more consecutive capitalized name tokens, excluding place/day/sentence-initial words — matches a prior ACCEPTED record's AND that record's frame signature also matches: the same named character in the same scenario frame (the round-3 repeats: Elias Thorne across two contracts, the Priya Sharma executor pair). A shared common first name alone, or a shared city/day, does NOT trigger it. The frame signature (every capitalized name token in the narrative) is capped at ≤4 identical repeats per id (soft cap, scenario_frame_repeated_too_often). Narrative-token Jaccard stays as the near-duplicate guard, not the diversity gate."

Sign the next sha on: pin reproduces; wording the above or an agreed variant; a re-run of the round4 12 showing the 9 now pass the diversity gate (they are distinct scenarios, only common names collided) — that is the diversity analogue of the 22-reject/4-pass calibration. Then the measured re-sample actually produces acceptances and I do the full content read for the neutrality number.

## Amendment diversity-guard-scope-fix @ `d113977` — content SIGNED at `eaf5c49c…` — 2026-09-15 20:12 BST

Pin reproduces (my script = `eaf5c49c7292d69f5609300f0ad73ea1fa5b95aed770824b9071c79833dbb999`; prior `bba608d2`). Wording verbatim my ruling (hard gate = full-name AND frame-match, `character_frame_reused`; frame ≤4/id soft cap; Jaccard not the gate). Extractor fixed: `_primary_party_name` now skips days/months and place-preceding prepositions and greedily joins a consecutive capitalized run into a multi-word name ("Elias Thorne", "Priya Sharma"), not the bare first token; `_PROPER_NOUN_STOPWORDS` now also excludes he/she/it/they/we/i/you so pronouns no longer leak. The attempt_id-collision fix rides in this branch (ids continue as `t_<cid>_0020`, `round` field on every row, append-refuse check seeded from proposals.jsonl history).

**Equivalent-shape calibration ACCEPTED over spending 9 paid calls** (the round-4 9 rejected narratives were never stored — rejected pre-stage-2 before this commit added unconditional logging — and reproducing them costs ~9 stage-1 calls; d0 correctly did not spend). `diversity_guard_calibration.json` replays both extractors over round-1's real 118 narratives: old (first-token, name-alone-vs-all-history) → 79/118 triggers, reproducing the ~67 % over-rejection failure mode round-4 hit; new (person-name run, name AND exact-frame conjunction) → 3/118. Stronger than the literal 9: 118 real narratives, same failure mode at the same rate, fix reduces to near-zero.

**I independently verified all 3 residual new-gate triggers are genuine identical-cast repeats** (reproduced the extractor+signature against the 118 proposals): (1) ipc415_property_r1_0012 (Ravi/Meera/Jaipur, antique workshop) vs r1_0003 (Ravi/Meera/Jaipur, textile shop); (2) ipc415_damaging_act_r1_0007 (Ravi/Meera/Coimbatore, logistics) vs ipc415_property_r1_0009 (Ravi/Meera/Coimbatore, logistics depot); (3) bns47_r1_0011 vs r1_0001 — Rohan Mehta / Pune / India logistics consultancy, near-VERBATIM opening. All three are the same named characters in the same setting, cosmetically varied — exactly the low-diversity padding the gate should catch and that narrative-Jaccard missed. The hard gate keys on the exact conjunction (full_name, full capitalized-token frozenset), so only an identical cast+setting can trigger it; a shared common first name alone cannot. **Content: SIGNED at `eaf5c49c…`.** Merge on Track A's shape-sign; then the measured re-sample finally accepts records and I do the full content read for the neutrality number.

## Round-5 measured read @ `86f9883` — NEUTRALITY FIXED (0/15), but NO-GO: sibling-fact omission leak (2/7) — 2026-09-15 20:29 BST

The measurement this detour existed for. Provenance verified by me: 81/81 unique ids (collision fix holds), 15 new records all `qwen3.7-max-2026-06-08` with resolved_model proven on every written row, binary `9bff8f30`. Reader output committed at `docs/reviews/spikes/trackb-p2a-tread/content-read-r5.json`.

**Neutrality: FIXED.** narrative_neutral 0/15 leaks, down from round-3's 22/26 (85%). The reworded prompt works — the narrative no longer pre-states elements. That question is answered.

**NO-GO on merge / no scaling — a DIFFERENT validity hole the full read exposed.** narrative_neutral passing did not make the records valid:
- **omission_real 2/7 FAIL — false omissions where the omitted element leaks into a SIBLING F_el fact** (not the narrative, so the narrative guard is blind to it, and the checker certifies them because it only checks the omitted element's own evidence slot is empty): (1) ipc415_damaging_act_0020 omit:0 — el0 (deception) "absent", but F_el1 states the act was one "she would not have taken had she known the true status of the hull inspection", i.e. the concealment/deception is established via F_el1; (2) ipc182_abuse_of_power_0021 omit:1 — el1 (intent that the false info cause the consequence) "absent", but F_el2 states Mehta "provided the false tip-off with the specific purpose of triggering a police raid… knowing such official action would cause distress", i.e. el1 is fully stated in F_el2. Both accepted (verdict flagged, IR-correct) but the supervision is WRONG: the student learns to abstain on a scenario where the element is actually present. Same licensed-but-wrong class as the narrative leak, relocated to sibling facts.
- **fact_fit 5/15 FAIL** — chained-element violations (a fact entails another element) and one self-contradiction (use_or_disposal_0023 F_el0: "transfer ownership… under a bailment" negates the entrustment premise). This is my D-tier chained-element rule, unenforced on the teacher.
- **coherent 5/15 FAIL**; **1 duplicate pair** (bns69_0021/0022); **1 phantom-element record** (use_or_disposal_0025 emits el3 on a 3-element contract — accepted because el3 is unknown, so replay's canonical check validated id-format but not count-vs-contract); **name recurrence** Rohan Mehta 5/15, Mehta surname 8/15 (the exact-frame gate misses same-name-different-frame).

**This VINDICATES the full-read-is-the-gate ruling.** The 2 false omissions are invisible to both the checker and the narrative regex; only the full content read caught them. A slice read would have passed them into T. Do not weaken the gate to a slice.

**Fixes for the next re-sample (NO-GO until they land):** (1) single-element-per-fact rule in the stage-1 prompt — "each F_el<k> must establish ONLY element k and must NOT state, imply, or entail any other element, INCLUDING the omitted one"; the full content read stays the omission-completeness gate (checker/regex cannot cover sibling-fact leaks). (2) replay rejects a target whose element ids are not exactly el0..el_{n-1} for the contract (phantom-element hole). (3) advisory: a soft cap on full-name reuse across the batch (cosmetic diversity; not a hard gate, to avoid the common-name over-rejection just fixed). The 66 round-1 records stand; these 15 do NOT enter T.

## Amendment single-element-per-fact @ `2e015a5` — content SIGNED at `8efd6a06…` — 2026-09-15 20:36 BST

Pin reproduces (my script + manifest-sha = `8efd6a06574c52178d688b0be6bdc9dd8d59f36b6319f4563225c06c93f8dae7`; prior `eaf5c49c`). My clause verbatim in the manifest field `single_element_per_fact_spec` AND in the harness stage-1 prompt (script L361). Manifest diff vs main = only that field + the amendment entry. Fix #2 (canonical-count, Track A shape-sign): replay now requires target positional ids == exactly {el0..el_{n-1}} for the contract, +1 non-positional (denial entity) only for deny shape, else `phantom_or_missing_element` — this rejects the round-5 use_or_disposal_0025 el3 phantom. Fix #3 (full_name_reuse ≥3×) code-only advisory in yield, per my soft-cap ruling. Lands the RULE only; admits no records — the 15 stay out, 66 stand. **Content: SIGNED at `8efd6a06…`.** Merge on Track A's shape-sign; then re-sample and I full-read again (the read stays the omission-completeness gate).

## Round-6 measured read @ `88ebf27` — OMISSION GATE PASSES; NO-GO: diversity fails (29% duplicate-clustered) — 2026-09-15 21:02 BST

Provenance verified: 90/90 unique ids, 24 new all qwen3.7-max-2026-06-08 resolved-model-proven, binary 9bff8f30. Structural pre-check: all 8 omission records keep el_k unknown with correct element count (retention clarification + fix #2 hold; 0 phantom). Reader output committed.

**Omission-completeness gate: PASS.** omission_real 0/8, fact_fit 0/24, narrative_neutral 0/24, coherent 0/24, english_register 0/24. The single-element-per-fact rule fixed the round-5 sibling-fact leak class — no omitted element leaks into a sibling F_el fact. The fix this round tested is validated.

**NO-GO on merging the 24 — diversity fails, demonstrably.** 7/24 (29%) are in duplicate clusters the exact-frame gate and narrative-Jaccard missed (only 1 caught as near_duplicate_rephrasing at sampling): (1) ipc405_wilfully_suffers 0025 vs 0026 — near-VERBATIM ("Rohan Mehta managed inventory records for Zenith Logistics at their Pune distribution center. During a routine audit, discrepancies…" vs "Vikram Mehta managed inventory records for Apex Logistics at their Pune distribution center. During a routine external audit, discrepancies…"), only name+company swapped; (2) ipc182_misdirected_act 0020/0023/0024 — same two parties Elias Thorne / Mara Kovic, municipal/water settings, 3 records; (3) ipc405_misappropriation 0028 shares Rohan Mehta / Zenith / Pune / custody phrasing. The (full-name AND exact-frame) hard gate misses these because the token-sets differ by the cosmetic swap; narrative-Jaccard misses them because wording varies. The advisory name-cap is insufficient — this is the round-3 diversity concern now demonstrated at 29%, and at T=200 it would pad T with 2–3 templates.

**Verdict.** The fix works; the batch is not mergeable as-is. Options: (a) merge only a de-duplicated subset (one representative per cluster — I can specify), or (b) NO-GO on the batch and fix diversity before scaling. Either way, before scaling the full content read's duplicate detection becomes the diversity GATE (dedup accepted records to one per template cluster), AND d0 should tighten sampling-time near-dup detection (template-level frame signature or lower Jaccard) so the teacher doesn't burn spend on duplicates that get dropped. The 24 do NOT enter T as-is; 66 stand. Deny still 0 (the 'negates' ClaimOp wire-grammar gap, routed to Track A).

## Round-6 dedup keep-set + diversity-guard design ruling — 2026-09-15 21:06 BST

**Ask 1 — clusters + keep-set (full-read diversity gate applied to the 24):**
- Cluster A (structural template, entity-masked shingle-Jaccard 0.33): {ipc405_wilfully_suffers_0025, _0026} — KEEP 0025, DROP 0026 (near-verbatim, only name+company swapped).
- Cluster B (named-entity reuse, Elias Thorne/Mara Kovic + water/municipal setting): {ipc182_misdirected_act_0020, _0023, _0024} — KEEP 0020, DROP 0023, 0024.
- KEEP-SET = 24 − {0026, 0023, 0024} = 21. ipc405_misappropriation_0028 and ipc415_damaging_act_0024 are name-reuse-only (distinct offence/conduct), kept. The residual name-clustering in the 21 (Rohan Mehta ×7, Vikram Singh ×4, Elias Thorne ×4) is a distributional concern the forward name-cap addresses, not grounds to delete otherwise-distinct supervision.

**Ask 2 — entity-masked-Jaccard alone does NOT capture my intent; the guard needs TWO orthogonal detectors.** Proven on the data: masking helps one duplicate class and erases the other.
- Class A — structural template reuse (same skeleton, swapped entities): entity-masked shingle-Jaccard catches it (405w_0025/0026 = 0.33 masked). The lead's proposal is correct FOR THIS CLASS.
- Class B — named-entity reuse (same distinctive parties recur, varied structure): entity-masking DESTROYS the signal — 182m_0020/0023/0024 score maskedJ 0.00 / 0.13 / 0.02, but share the full names Elias Thorne / Mara Kovic. Masking makes true duplicates look distinct. This class needs an UNMASKED distinctive-full-name / name-pair reuse cap (a full name recurring ≥ N across the batch, or a full-name pair recurring ≥ 2, flags a cluster). This is the round-3 concern, graduated from advisory to gate.
- **Ruling: the durable guard runs BOTH — entity-masked structural Jaccard (class A) AND unmasked full-name/name-pair reuse cap (class B) — and rejects if either fires; sampling-time reject + accept-time dedup, full read as backstop.** Calibration target (ground truth): must flag 0026 via A and 0023/0024 via B, without collapsing the 21 keep-set. New guard → manifest amendment → my content-sign (what counts as a template duplicate: either structural-masked-overlap ≥ threshold OR shared distinctive full-name/name-pair) + Track A shape-sign (the masking + signature computation). Entity-masked-Jaccard as the WHOLE guard would silently pass the class-B duplicates — do not ship it alone.

## Diversity guard — entity-set scope ruling (content definition to sign) — 2026-09-15 21:09 BST

Confirmed keep-set: DROP {ipc405_wilfully_suffers_0026 (via check a), ipc182_misdirected_act_0023, _0024 (via check b, share the Thorne+Kovic person-pair)}, KEEP 21 including ipc405_misappropriation_0028 (single-person reuse, distinct offence — NOT a duplicate).

**Check (b) entity-set scope = PERSON full-names ONLY, trigger on a shared PAIR (≥2 common person names), org/place/title tokens EXCLUDED.** Concrete justification from round-6: a naive capitalized-bigram set counts "Zenith Logistics" as an entity, so 0028∩0025 = {Rohan Mehta, Zenith Logistics} = 2 → would FALSELY flag two different-offence records as duplicates, re-triggering the exact common-name/city over-rejection fixed in the diversity-guard-scope amendment. Person-only gives 0028∩0025 = {Rohan Mehta} = 1 → correctly keeps both; 0023∩0024 = {Elias Thorne, Mara Kovic} = 2 → correctly flagged. Single shared person, shared org, and shared place do NOT trigger check (b).

**Design principle I'm signing:** the two sampling-time checks are HIGH-PRECISION spend-savers — they must never falsely reject a distinct scenario (a false reject wastes the teacher's valid output and re-introduces over-rejection). The full content read is the RECALL backstop for the residue (e.g. 0020, which shares only one person + a similar setting with the trio and is not safely machine-detectable). Content definition of a template duplicate: EITHER masked-structural overlap ≥ threshold (check a) OR a shared person-name PAIR (check b, org/place excluded). Calibration ground-truth: (a) flags 0026, (b) flags 0023/0024; NEITHER fires on 0028/0025 or on common city/org sharing. New guard → amendment → my content-sign + Track A shape-sign.

## Deduped-21 CONFIRMED for merge @ `7a153b2`; T 66→87; deny-label ruling — 2026-09-15 21:12 BST

Verified from origin: accepted 87/87 unique; drops {405w_0026, 182m_0023, 182m_0024} ABSENT; keeps {405m_0028, 415d_0024} PRESENT; provenance {66 qwen3.8-max-0902, 21 qwen3.7-max-2026-06-08}, binary 9bff8f30; content-sha unchanged 8efd6a06 (accepted.jsonl not in pin); dropped rows flipped to rejection_reason=template_duplicate_track_b_ruling with citation; decide FAIL correct (T=87<200, P/R uncomputed). CONFIRMED — the 21 enter T under the full-read content gate (omission PASS + diversity keep-set), same basis as round-1's 66.

Deny eldeny convention: **instruct-in-prompt-only; checker stays LABEL-AGNOSTIC.** Correctness does not need the literal string — the denial binds via <CID>_NEG + condition-B denied_field NAME-match, not the wire element-id. Fix #2's canonical-count check already enforces the STRUCTURE (exactly 1 non-positional element id for deny shape, 0 otherwise) regardless of its spelling — that is the right level. Hard-standardizing "eldeny" would false-reject a correct record labelled el_deny/denial (the over-rejection pattern). Prompt-instructing eldeny is good (concrete id → stops phantom el2/el3 invention); the checker enforces the count/structure, not the label.

## Template-dup guard @ `93f5be4` — definition APPROVED; rule AMENDMENT + reviewed_drops — 2026-09-15 21:22 BST

Guard verified: detector A = masked-shingle-Jaccard ≥ threshold (near-verbatim swap); detector B = shared person-entity-set ≥ 2 (name-pair), org/place excluded via preposition + org-indicator filter (fixes the 9 false person-matches, e.g. "Zenith Logistics"); EITHER fires → reject (my ruling). Calibration on round-6's clean 24: false_positives_among_keepers 0, recall 100% (A→405w_0025/0026; B→the 182m pairs). Definition APPROVED — it matches my check-(a)/check-(b) ruling exactly.

**Amendment question: PIN IT (amendment), not code-only.** The guard determines what enters T; the content-sha must pin T's acceptance semantics, consistent with the neutrality and diversity guards being amendments. d0 adds the reject-rule clause (detector A OR detector B, person-pair org/place-excluded) + the accept-path below; I content-sign that new sha. Do not ship code-only.

**Hazard ruling: reviewed_drops.jsonl, re-applied by cmd_replay — YES, and it is NECESSARY, not optional.** cmd_replay regenerates accepted.jsonl from proposals, so a manual 3-drop edit is silently reverted (d0 caught it; T=87 intact). Wiring the guard into replay would only reproduce MACHINE-detectable dups — it cannot reproduce my full-read backstop drops (e.g. 182m_0020, which shares one person + a similar setting and is deliberately NOT machine-flagged). So the full-read drops can ONLY be persisted as an explicit record. Rule: persist `reviewed_drops.jsonl` = {attempt_id, cluster_id, kept_id, reviewer_record_ref}; cmd_replay re-applies it after regenerating accepted.jsonl; the accept-path is documented in the manifest (it determines T). This keeps MY full-read ruling authoritative rather than code re-deriving it. **Seed it NOW with the round-6 3-drop {405w_0026, 182m_0023, 182m_0024}** so the current T=87 is replay-durable, not a vulnerable manual edit. The sampling-time guard (future spend-saver) and reviewed_drops (accept-time full-read authority) are complementary; both belong in the pinned amendment.

## Amendment template-duplicate-guard-and-reviewed-drops @ `cba75a4` — content SIGNED at `69549003…` — 2026-09-15 21:31 BST

Pin reproduces (`695490039aa64b3bc6573c77e8cdf8c9cf2b37d455fad34c735d342165c5aaf1`; prior `8efd6a06`). Reject-rule clause matches my ruling: (A) masked-shingle-Jaccard ≥ 0.30 OR (B) PERSON-only entity set (org/place excluded) shares ≥ 2 names, setting explicitly NOT part of B; code `_TEMPLATE_DUP_MASKED_JACCARD_REJECT=0.30`, `_TEMPLATE_DUP_NAME_PAIR_MIN=2`; calibration 0 FP / 100% recall on round-6's 24, cited; clause honestly states the guard is NOT a substitute for the full read. reviewed_drops.jsonl carries my exact 3 drops with correct keeps — {405w_0026→0025, 182m_0023→0020, 182m_0024→0020}, each ref'd to my record 8fdcbf5 and carrying the reviewed wire_sha256; cmd_replay re-applies after regenerating so T=87 is replay-durable. Verified: accepted 87, the 3 drops absent, provenance {66 qwen3.8-max-0902, 21 qwen3.7-max-2026-06-08}. **Content: SIGNED at `69549003…`.** Merge on Track A's shape-sign → T=87 replay-durable + guard pinned. Then round-7's 7 (re-keyed) for the omission read.

## Round-7 read @ `34b21be` — GO on 3 clean (T→90), NO-GO on 3; detector-B false-positive finding — 2026-09-15 21:46 BST

Provenance: 93/93 unique, 6 new all qwen3.7-max-2026-06-08 resolved-model-proven, binary 9bff8f30; reviewed_drops held through this fresh replay (durability confirmed). omission-completeness: **PASS 0/2** — the single-element fix holds on this batch. narrative_neutral 0/6.

**Content defects — GO on 3, NO-GO on 3.** fact_fit 2/6 FAIL (chained-element: 405m_0023_r7 F_el1 folds the instruction into the act; 405m_0027_r7 F_el1 "dishonestly converted the ENTRUSTED ₹50,000" entails el0 entrustment); coherent 1/6 FAIL (405m_0025_r7 narrative). Reader also flags a within-6 template cluster {0023_r7, 0027_r7, 0028_r7} (misappropriation-of-entrusted-cash). Net: DROP {405m_0023_r7 (fact_fit), 405m_0025_r7 (coherent), 405m_0027_r7 (fact_fit)}; KEEP {405m_0028_r7 (clean; sole survivor of the template cluster after the other two drop), 405w_0023_r7, 405w_0024_r7 (clean)}. **GO on 3 → T 87→90; the 3 defective do NOT enter T.**

**Finding on my own signed detector B (important, pre-scaling).** My cross-batch check found 405m_0028_r7 shares the person-PAIR {Rohan Mehta, Vikram Sethi} with merged-21's 405m_0025 — but they are DISTINCT scenarios (0028_r7 = cash entrustment→misappropriation; 0025 = an inventory-audit scene; masked-Jaccard 0.00). So detector B (≥2 shared persons, pair-alone → hard reject), as I ruled and signed, is a FALSE POSITIVE here: it flags distinct scenarios that merely reuse the teacher's narrow name pool. The round-6-only calibration passed 0-FP because that fixture contained no distinct-scenario-shared-pair case. Two consequences: (1) the sampling guard's dedup history is round-LOCAL (it did not compare 0028_r7 against the merged 21 — which here accidentally SAVED a valid record from a false reject); (2) detector B must NOT be naively extended to the full-T corpus, or it will false-reject distinct scenarios sharing a recurring name pair. **Refine before scaling:** detector B should hard-reject on a shared person-pair ONLY together with structural similarity (a secondary lower masked-Jaccard) or same offence+setting; a pair-with-distinct-structure is a soft name-diversity flag, not a drop. 0028_r7 stays (distinct scenario).

**Scaling blockers (not blocking the 3-record merge):** (a) fact_fit chained-element ~33% (2/6) — the single-element PROMPT rule is guidance the teacher still violates; the full read catches it but it wastes ~a third of spend. (b) narrow name pool (Rohan Mehta / Vikram Sethi / Vikram Mehta recurring) is the ROOT of both the detector-B false positive and the template repetition — needs a name-diversity instruction (fresh parties per scenario) + a soft distributional cap. Address (a) and (b) before a scaling run, or each round loses ~half to the full read. Deny still 0 (predates eldeny fix; next round).

## Round-7 follow-ups: reviewed_drops for content-defect drops (I was wrong) + detector-B refinement — 2026-09-15 21:50 BST

**1. The 3 content-defect drops MUST go in reviewed_drops — the lead is right, I was wrong.** {0023_r7, 0027_r7 fact_fit; 0025_r7 coherent} are accepted_by_replay=TRUE and checks.jsonl has no fact_fit/coherent field (my full-read-only judgments the checker doesn't compute), so plain removal → next replay regenerates them → T pollution, the exact hazard reviewed_drops closed. This is my own earlier rationale (full-read backstop drops persist ONLY as an explicit record) — it applies to CONTENT-defect drops too, not just template duplicates. The mechanism is reason-agnostic; record them reason=fact_fit / coherent. CONFIRMED → T 87→90 durable.

**2. Detector-B refinement: DOWNGRADE person-pair to a soft FLAG, not a hard reject — there is no reliable cheap secondary condition.** I checked: the 182m REAL dups score masked-Jaccard 0.00 / 0.13 / 0.02, and the 0028_r7/0025 FALSE POSITIVE scores 0.00 — both real-dup and name-pool-reuse are ~0 structurally (that is the defining feature of the name-pair class: cosmetically varied). So a lower masked-Jaccard secondary threshold CANNOT separate them, and same-offence doesn't (both same offence). The distinguisher is semantic scenario/conduct identity, which only the full read computes. Ruling: detector A (masked-Jaccard ≥0.30, near-verbatim) stays a HARD reject; detector B (person-pair ≥2) becomes a SOFT flag (rejection_reason→template_duplicate_name_pair_CANDIDATE, logged not rejected); the full read arbitrates flagged candidates → confirmed dups to reviewed_drops. I decline a setting-based secondary condition (place extraction is fragile — the exact trap the earlier gate hit). Re-pins detector B → new amendment, dual-sign.

**Cross-corpus scope: YES.** Both detectors must dedup against the full merged T, not round-local (the round-local gap is why 0028_r7 wasn't compared to merged-21). Detector A cross-corpus hard-reject catches cross-round near-verbatim dups (real gap). Detector B cross-corpus SOFT flag has no false-reject risk. Confirm in the same amendment.

**3. Scaling blockers — agree, land before the scaling run; the 3-merge doesn't wait.** (a) fact_fit ~33%: strengthen the stage-1 fact prompt; a sampling-time fact_fit guard is LOW-VALUE (fact_fit is semantic like the omission leak — a regex catches little; the full read stays the gate). (b) Name pool is the ROOT: a name-diversity instruction (fresh parties per scenario, no reuse across the corpus) + a soft distributional cap eliminates the name-pair collisions at source, which makes detector-B flags rare — fixing (b) upstream cuts the guard's load, exactly as the lead notes.

## Amendment #8 reason-field-fix-detector-b-downgrade-round7-merge @ `5e41e34` — content SIGNED at `204fbd2c…` — 2026-09-15 21:58 BST

Pin reproduces (`204fbd2c2cb5fbe93e89298701664b66e6bb53e3db6187e17a13d119a2204d08`; prior `695490`). Detector B correctly downgraded: code path — detector A masked-Jaccard ≥0.30 → `return log` (hard reject); detector B person-pair ≥2 → accumulates `template_duplicate_name_pair_CANDIDATE`, never returns (soft flag, full-read arbitrated). Stale "A OR B → reject" wording gone (0 occurrences). Both detectors cross-corpus (full on-disk proposals history). Clause records my no-reliable-secondary rationale accurately (real 182m dups masked-J 0.00/0.13/0.02 indistinguishable from the 0.00 FP; place-secondary declined). reviewed_drops = 6 with correct reasons {template_duplicate ×3, fact_fit ×2 (0023_r7/0027_r7), coherent ×1 (0025_r7)} — no content drop mislabeled as duplicate; drop schema cluster_id/kept_id optional, reason required. 3 GO records {405m_0028_r7, 405w_0023_r7, 405w_0024_r7} present in accepted; T=90, provenance {66 qwen3.8-max-0902, 24 qwen3.7-max-2026-06-08}. **Content: SIGNED at `204fbd2c…`.** Merge on Track A's shape-sign → T=90. Then the fact_fit + name-diversity prompt fixes, the deny-inclusive clean round, then scaling.

## Amendment #9 fact-fit-strengthening-and-name-diversity @ `da8d216` — content SIGNED at `2c69d15e…` — 2026-09-16 02:34 BST

(#8 merged: main 138e1e2, T=90, my content-sign d398efe + Track A 84082f4c.) Pin reproduces (`2c69d15ebc44c7f9ad739c72cce206922cef123b74401757a4bed2623c169b44`; prior `204fbd2c`). Prompt-only (2 manifest clause fields + harness prompt injection; no checker/guard change); T=90 unchanged. (a) fact_fit_strengthening_spec targets exactly my round-7 chained-element finding (bee7b57) — its worked example ("because the property had already been entrusted, X then misappropriated it") is 0027_r7's defect; adds a per-fact isolation self-check ("read only that one sentence — does it alone establish exactly one element"); NO regex guard, full read stays the gate (my semantic-gate stance honored, stated in the clause). (b) name_diversity_spec injects a cross-corpus avoid-list of every person name used ≥ 2× in the full proposals history (capped 40), SOFT/advisory prompt guidance only, no hard-reject — the root fix I identified for the narrow name pool that drove both detector-B candidates and template repetition; uses the same `_person_entity_set` extractor. Both clauses' wording appears verbatim in the stage-1 prompt. **Content: SIGNED at `2c69d15e…`.** Merge on Track A's shape-sign → then the deny-inclusive clean measured round (omission + control + deny, eldeny end-to-end) for my read, then scaling.

## Round-8 @ `3595fcd` — prep PASS; content read HELD for Track A's deny-fix ruling; surname finding — 2026-09-16 02:56 BST

Code-independent prep done; per the lead's instruction the CONTENT READ (esp. the 2 first-ever deny records) is HELD until Track A rules on the deny-fix replay code, since that code determines the deny records' certification basis. Prep: 107/107 unique; provenance {66 qwen3.8-max-0902, 41 qwen3.7-max-2026-06-08}, binary 9bff8f30; all 6 reviewed_drops still ABSENT (durability held through the deny-fix replay); omit records keep el_k unknown (correct); both deny records (405m_6daee122_0039, 405u_..._0039) carry eldeny:established alongside established required elements — the expected deny structure, matching fix-#2's one-non-positional-id allowance. 17 new: control + omit:0/1/2 + 2 deny.

**Name-diversity (lead's surname question): the full-name avoid-list WORKS, but a SURNAME-register residual is real.** Full-name reuse within round-8 is only "Liora Vance" ×2, and 0 round-8 full names appear in the merged-90 (my initial cross-corpus overlap was a false artifact — verified "Caspian Vane"/"Liora Vance"/"Elara Thorne" all appear 0× pre-round-8; the avoid-list holds cross-corpus). BUT the teacher shifted to a narrow SURNAME register: within 17 records, ~12 distinct characters share Vane/Vance/Thorne (Caspian Vane, Cedric Vance, Darius Vane, Jorah Vane, Jorin Vane, Lorcan Vane, Lorna Vane, Caleb Vance, Liora Vance ×2, Elara Thorne, Kaelen Thorne). Milder than full-name reuse (these are DISTINCT characters, not the same one recurring — not duplicates, does NOT block merge), but at scale a corpus where a third of characters are "-Vane/-Vance" is a distributional oddity. **Recommendation: a surname-level extension to the name_diversity_spec avoid-list (surnames used ≥ N× added to the advisory "avoid" list, same soft mechanism), before scaling — not blocking this round.** Full content read (omission-completeness + fact_fit-under-#9 + deny substantive correctness) launches the moment Track A's deny-fix ruling lands.

## Round-8 full read @ `3595fcd` — GO on 14 (T→104), NO-GO on 3; DENY validated (1/2 correct); NOT yet scaling-ready — 2026-09-16 03:03 BST

Track A cleared the deny-fix as a bug fix (certifications legitimate). My content read of the 17, incl. the first-ever deny records:

**DENY shape — validated as workable, but 1 of 2 is substantively wrong.** 405u_0039 (use_or_disposal deny): denial_real PASS, coherent PASS — el0/el1/el2 established, eldeny (good faith) grounded, verdict not_proved/abstain false, answer "does not apply because [good faith]." A CORRECT deny — first legitimate deny supervision, admit. 405m_0039 (misappropriation deny): **denial_real FAIL + coherent FAIL** — el1 asserts "transferred the funds INTENDING TO KEEP the money for her personal benefit" (dishonest mens rea) while eldeny asserts "good faith, not dishonestly" on the same act. A good-faith defence NEGATES dishonesty; it cannot coexist with an el1 that explicitly asserts it. Internally contradictory → bad supervision (teaches "dishonestly kept it for herself, but good faith, so no offence"). DROP.

**Go/no-go: GO on 14 clean → T 90→104; NO-GO on 3 → reviewed_drops.** Drops: {405m_0039 (deny, denial_real/coherent), 405m_0035 (omit:0, omission_real+narrative_neutral — F_narrative "managed by event coordinator Nikhil Rao" leaks the omitted custody element el0), 415property_0030 (omit:0, omission_real+fact_fit — F_el1 "fraudulently induced" leaks the omitted deception element el0, sibling-fact class)}.

**Scaling readiness: NOT YET — three residuals.** (1) fact_fit strengthening (#9) WORKED: 1/17 = 6%, down from round-7's 33% — the chained-element defect is largely fixed. (2) BUT omission still leaks 2/6 (one narrative leak, one sibling-fact leak) — the neutrality + single-element rules reduced but did not eliminate. (3) NEW deny-specific defect: for a deny with a mens-rea-negating defence (good faith), el1 (the dishonesty element) must be established via CONDUCT/actus-reus WITHOUT asserting the specific mental state the denial negates — 405u_0039 does this right ("disposing contrary to her mandate", no personal-benefit motive), 405m_0039 does it wrong. **Recommend before scaling:** a deny-authoring prompt rule (el1 establishes conduct only; the exculpatory mental state lives ONLY in the denial fact), plus continued attention to the omission leaks, plus the surname avoid-list extension already routed. Diversity: full-name avoid-list works (0 cross-corpus reuse); surname-register residual noted. Provenance {66 qwen3.8, 41 qwen3.7}, 107/107 unique, reviewed_drops durable.

## Endpoint/model provenance fork — recommendation A(goal)+C(interim), B last-resort — 2026-09-16 03:11 BST (T=104 merged @63ceed1)

Operator's joint call; I own the response-model anti-spoof requirement. **Recommendation: pursue A, hold at C now, avoid B.**

**Framing — per-response snapshot provability is HIGHLY DESIRABLE but not strictly ESSENTIAL to T's VALIDITY.** Every T record is gated by the Lean checker certification AND my full content read (omission/fact_fit/denial/diversity); a record that passes both is valid supervision regardless of which model authored it (the same point I made against the Haiku swap: teacher identity shifts acceptance RATE, not the legitimacy of an accepted record). Per-response proof protects PROVENANCE integrity — reproducibility, detection of silent mid-batch model drift, and a meaningful T_by_teacher split — not validity. So the anti-spoof requirement is about audit trail, not whether T is good.

**A (PAYG on the general endpoint) is clearly best** — keeps the provable pinned qwen3.7-max-2026-06-08, no model change, no amendment, no provenance loss; costs only an operator billing action. Primary recommendation.

**C (hold at T=104, land the prompt fixes, defer the endpoint) is the correct interim — and it is FORCED regardless.** I ruled round-8 NOT scaling-ready: the deny-authoring rule, omission tightening, and surname extension must land AND a clean measured round must read clean before any scaling. So sampling is NOT the current bottleneck — the prompt fixes are. There is no urgency to accept a provenance downgrade under time pressure; do the prompt work now, pursue A in parallel, and by the time the fixes + a clean round are done the operator will have had time to enable A.

**B (plan alias) — last resort, acceptable ONLY under mitigation, and unnecessary given C.** The alias echoes only the alias string, so per-response snapshot proof is impossible. If B were ever forced, the minimum mitigation: (1) a cross-endpoint fingerprint probe at each batch start — a fixed deterministic prompt set sent to BOTH the alias and the pinned -2026-06-08 (one general-endpoint call), outputs compared, re-probed every N attempts, halt on divergence; (2) the alias tranche recorded as a DISTINCT provenance class (teacher_model="qwen3.7-max@plan-alias", probe evidence committed), never merged with the snapshot-proven tranche in T_by_teacher; (3) a manifest provenance_caveat + teacher-model amendment, dual-sign. Caveat: the mitigation is IMPERFECT — the fingerprint probe needs temp 0 (≠ the temp-0.7 sampling config), qwen determinism at temp 0 is unverified, and the reference call may itself be free-tier-walled. Because C removes all urgency, I recommend against B.

**Joint recommendation to the operator: enable A (PAYG on the general endpoint) to keep the provable snapshot; meanwhile hold at C and land the pre-scaling prompt fixes, which gate scaling regardless of the endpoint. Do not adopt B — sampling is not the bottleneck, and B trades away provenance for speed we don't need.**

## Amendment #11 deny-authoring-rule-and-surname-extension @ `1bd6f23` — content SIGNED at `8b26ec7f…` — 2026-09-16 03:19 BST

Pin reproduces (`8b26ec7f621c902cdd7a175aa7bc2bb2fa169e519c37b69e7fc656360403deff`; prior `2c69d15e`). Prompt-only (manifest clause + sampler prompt; no checker/guard). All four verified: (a) deny_authoring_spec = my rule verbatim (conduct facts state the outward act only, never the mental state the denial negates — the 405m_0039 fix; 405u_0039 did it right); (b) surname extension "vary the surname too… MUST NOT be reused even with a different first name" (cap 20, soft) — the Vane/Vance/Thorne register; (c) neutrality role/title categories ban "coordinator/custodian/in charge of/responsible for/entrusted with" + synonyms — exactly the 405m_0035 "managed by event coordinator" custody-implication leak; (d) sibling-fact worked example anonymized to "Party B" (no corpus names), BAD "fraudulently induced" correctly identified as leaking the deception element, GOOD "agreed to the transfer and arranged for delivery" states only the inducement without asserting deception — the correct BAD/GOOD contrast from 415property_0030's dropped leak. **Content: SIGNED at `8b26ec7f…`.** Merge on Track A's shape-sign (surname mechanic). Then, once the endpoint resolves (recommend A), a clean measured round under all fixes → my read = the scaling gate.

## B (plan alias) provenance contract — 4 rulings — 2026-09-16 03:44 BST

Operator chose B (my A/C recommendation was reaffirmed against; I proceed). Material change: my B-mitigation required a CROSS-endpoint fingerprint probe, which is impossible now (general endpoint still 403 FreeTierOnly). So B proceeds WITHOUT provenance verification — the alias echoes literally "qwen3.7-max", proving only "whatever qwen3.7-max resolved to at call time," and a provider-side backing-model swap is UNDETECTABLE per-response. The only protection is honest labeling. This must be stated plainly in the manifest, not buried.

**1. teacher_model string (alias tranche):** `qwen3.7-max@plan-alias` (matches the echoed response.model + endpoint). Do NOT overwrite `model_snapshot_id` (stays the pinned provable `qwen3.7-max-2026-06-08` for the snapshot tranche); the alias string goes in a distinct field, per d0's draft.

**2. FLOOR:** alias records COUNT toward T_min (≥200) and F_per_id — consistent with my earlier ruling that provability is provenance, not validity: every alias record still passes the Lean checker + my full content read, so it is valid supervision. T≥200 MAY be mixed-provenance. REQUIRED: `T_by_provenance_class` split reported in yield.json (snapshot_proven N vs alias_unpinned N) so anyone can see how much of T is reproducible. I do NOT require the snapshot-proven tranche alone to meet the floor — that would freeze the gate on the 403. When A lands, future growth reverts to the provable snapshot; the mix stays visible.

**3. provenance_caveat location:** `teacher.provenance_caveat` (top of the teacher block, prominent), referenced from the manifest `status`. It states: the alias proves only the resolved-at-call-time model; a provider-side backing-model change is undetectable during B; the cross-endpoint fingerprint mitigation is unavailable (general endpoint 403); reproducibility of the alias tranche is limited to "qwen3.7-max as served on <date range>."

**4. explicit teacher_provenance_class:** YES — every record carries `teacher_provenance_class` ∈ {`snapshot_proven`, `alias_unpinned`} in addition to the model string (the 66 qwen3.8-max-0902 + the 41 qwen3.7-max-2026-06-08 = snapshot_proven; new alias records = alias_unpinned). Machine-clean split for T_by_provenance_class and downstream filtering; a bare model string is too easy to misparse as pinned.

**Recommended (not a hard block):** a WITHIN-endpoint fingerprint probe — a fixed prompt set to the alias at batch start and every N attempts, outputs hashed; a change flags a mid-batch backing-model swap (it can't say to WHAT, but it catches drift the per-response echo can't). Imperfect (temp-0.7 non-determinism; needs temp-0 or a stable signal), so best-effort, documented as such. Better than zero drift detection during B.

Scaling unchanged: the clean measured round under amendment #11 must still read clean before scaling; B doesn't relax that. This provenance amendment is dual-signed (my content + Track A shape) before any B sampling.

## DEFINITIVE teacher-provenance contract — alias is permanent, T≥200 majority-alias — 2026-09-16 03:45 BST

Operator committed to B PERMANENTLY (no PAYG on the general endpoint; the pinned provable qwen3.7-max-2026-06-08 is done as a live teacher). All "revert if A lands" framing dropped. T≥200 (~96 more records, essentially all alias) will be MAJORITY alias-provenance. Definitive rulings:

**FLOOR — YES, T≥200 may be met by a majority-alias T; this is the standing permanent position, not a one-batch exception.** Every record — alias or snapshot — is gated by the Lean checker certification AND my full content read; a record passing both is valid supervision regardless of author. Provability is provenance, not validity (my consistent position). A weaker resolved model would only raise the rejection rate, not degrade an admitted record. So alias records COUNT toward T_min(≥200) and F_per_id, sharing the floor in AGGREGATE with the snapshot tranches. REQUIRED: `T_by_provenance_class` reported in yield.json (snapshot_proven vs alias_unpinned counts) so the composition is permanently auditable even though the floor is met in aggregate. No separate snapshot-only floor (impossible under the permanent 403).

**Rulings 1/3/4 stand, with 3 updated for permanence:** (1) alias teacher_model = `qwen3.7-max@plan-alias`; `model_snapshot_id` keeps its historical pinned value for the snapshot tranche, alias string in a distinct field. (3) `teacher.provenance_caveat` (prominent, referenced from status), UPDATED: the alias is the PERMANENT committed teacher; T is permanently mixed and majority alias-provenance; the alias proves only the resolved-at-call-time model; a provider-side backing-model change is undetectable per-response; alias-tranche reproducibility is limited to "qwen3.7-max as served on <dates>." (4) explicit `teacher_provenance_class` ∈ {snapshot_proven, alias_unpinned} per record.

**UPGRADE (recommended → REQUIRED): a within-endpoint fingerprint probe.** For a PERMANENT, unverifiable, majority teacher, this is the only drift detection available and the accumulating risk of an undetected provider-side backing-model swap over a long run makes it non-optional: a fixed prompt set to the alias at batch start and every N attempts, outputs hashed; on a fingerprint change, segment the tranche at that point (dated marker / provenance sub-class) and alert me. Best-effort caveat documented (temp-0.7 non-determinism; needs a temp-0 or otherwise stable signal). Without it, a permanent B run cannot even tell whether T was authored by one model or several.

**Honest documentation (not relitigating — the operator decided and reaffirmed):** T is now a permanently, largely UNREPRODUCIBLE dataset. That is an accepted, documented property. Downstream SFT/product claims must not overstate T's provenance beyond "majority authored by qwen3.7-max (plan alias, unpinned) as served during <dates>, minority by pinned snapshots." This contract is dual-signed (my content + Track A shape) before any B sampling; scaling still requires the clean measured round under amendment #11 reading clean first.

## Amendment alias-teacher-provenance @ `d184234` — content SIGNED at `f120c60d…` — 2026-09-16 03:59 BST

Pin reproduces (`f120c60db42139c9bf0480f0ee02dafb98b1981fad56a4542bf220afe45fbba8`; prior `8b26ec7f`). The standing provenance contract for the remainder of the project — verified it says exactly what T's provenance IS. provenance_caveat is complete + honest: all four weakenings (alias echoes only the string; backing-swap undetectable by the response-model guard; cross-endpoint fingerprint unavailable, 403 + no PAYG; reproducibility limited to "as served during <dates>"), my downstream-claims constraint VERBATIM ("permanently, largely unreproducible… must not overstate provenance beyond 'majority qwen3.7-max plan-alias (unpinned)… minority pinned snapshots'"), and the validity-not-provenance clarification. Labels distinct: alias_teacher_model_label `qwen3.7-max@plan-alias`, alias_model_id `qwen3.7-max`, model_snapshot_id untouched. Floor: alias counts in AGGREGATE toward T_min/F_per_id, T_by_provenance_class reported, no snapshot-only floor. All 104 backfilled snapshot_proven (66+38). Fingerprint probe REQUIRED, temp-0.7 (same as sampling), every 20 + baseline, hash→alias_fingerprint_log.jsonl, HEURISTIC FLAG only (never reject), segments tranche with a dated marker on change, and honestly documents its imperfection (temp-0.7 noise can raise suspicion never confirm; stable hash never confirms absence). **Content: SIGNED at `f120c60d…`.**

**Non-blocking improvement (does not gate the sign — the contract is met and honestly labeled):** the fingerprint prompt is a single trivial "reply FINGERPRINT" — LOW SENSITIVITY, because two similar backing models both return "FINGERPRINT", so a swap between them is missed. A stronger probe uses a few prompts with model-idiosyncratic outputs (open-ended continuations whose exact tokens differ by model) so a swap is more likely to change the hash. Recommend d0 strengthen the probe prompt set when convenient; the current one satisfies the REQUIRED best-effort bar and the manifest overstates nothing. Merge on Track A's shape-sign → the clean #11 measured round on the alias → my read = the scaling gate.

## Amendment fingerprint-probe-set-strengthening @ `fa3a828` — content SIGNED at `b3d78c8b…` — 2026-09-16 04:19 BST

Pin reproduces (`b3d78c8bd137c6dde960ffd9a979134d59dbd1cecc373322f207da94201fe5b1`; prior `f120c60d`, base B-provenance merged at T=104). The 4-prompt set genuinely captures model-idiosyncratic outputs, addressing my low-sensitivity flag: #2 (open-ended sentence continuation, "The old lighthouse keeper…") and #4 ("explain why the sky is blue") are open-ended and would diverge across two similar backing models; #3 (three colors) is medium; #1 (FINGERPRINT) is the liveness/format check. Drift flags only when a MAJORITY (>half, i.e. ≥3/4) of hashes change at once — tolerates ordinary temp-0.7 noise on any one prompt. Heuristic-flag-only, segments on change, never rejects (unchanged from the signed contract). checks.jsonl regenerated to 184 (stray test row removed; checks==proposals==184, accepted=104 — Track A verifying that gate-evidence side). **Content: SIGNED at `b3d78c8b…`.**

**Non-blocking refinement (deeper version of my sensitivity flag; does not gate the sign):** running the probe at temp 0.7 UNDERCUTS the idiosyncratic prompts. At 0.7 the open-ended prompts (#2, #4) — the ones that actually distinguish models — change hash from noise nearly every probe, so the ≥3/4 majority rule effectively leans on the LOW-idiosyncrasy prompts (#1, #3, which rarely change but also barely differ between similar models). Better: run the fingerprint probe at temp 0 / greedy (detection, not sampling-condition-matching, is the probe's job) — then #2 and #4 become deterministic AND model-characteristic, a hash change is a real swap signal, and the noise-tolerance majority rule is no longer needed to paper over creative-output variance. Recommend d0 switch the probe to greedy decoding if the alias endpoint honors it; the current temp-0.7 set still satisfies the REQUIRED best-effort bar and the manifest overstates nothing. Merge on Track A's shape-sign → FIRST alias round with this probe from batch 1 → my full read of that round = the scaling gate.

## Endpoint non-determinism — add explicitly to provenance_caveat (tiny follow-on) — 2026-09-16 04:23 BST

Greedy defeated: d0 verified the plan endpoint is NON-DETERMINISTIC even at temp 0 + fixed seed (same prompt+seed twice → "he lit the beacon." vs "he closed his eyes."), so my greedy-probe refinement can't apply; the temp-0.7 + majority set stays as signed (b3d78c8b). That's fine.

**Ruling: YES, add the endpoint-non-determinism fact explicitly to teacher.provenance_caveat.** My current "reproducibility limited to 'qwen3.7-max as served during <dates>'" covers it only implicitly; this is a DISTINCT and STRONGER weakness that must not be left to inference. Two points to add: (a) the plan endpoint is non-deterministic even at temp 0 with a fixed seed (evidence: d0's two-run test) — so the alias tranche is unreproducible at the ENDPOINT / generation level, not only the model-identity level: no re-run reproduces a given record even with the same model string, seed, and temperature; (b) consequently the manifest's `seed_derivation` (seed = sha256(attempt_id)) is NON-FUNCTIONAL for reproducibility on this endpoint for the alias tranche — the seed is still recorded for audit but does NOT pin the generated output. Pre-approved wording: "The plan endpoint is non-deterministic even at temperature 0 with a fixed seed (verified: identical prompt+seed produced different completions across runs); the alias_unpinned tranche is therefore unreproducible at the ENDPOINT/generation level, not merely the model-identity level — no re-run reproduces a given record even with the same model string, seed, and temperature. The recorded per-attempt seed is retained for audit but does NOT pin output on this endpoint." Tiny follow-on manifest amendment (new sha, dual-sign); does NOT block the probe merge or the first alias round, but must land before the alias tranche is cited in any downstream provenance/reproducibility claim.

## Round-9 (FIRST ALIAS round) full read @ `6a2ea82` — SCALING GATE: GO on 31 (T→135), NOT scale-ready — 2026-09-16 05:10 BST

Provenance verified: 149/149 unique, {snapshot_proven:104, alias_unpinned:45}, all 45 new labelled qwen3.7-max@plan-alias (fail-closed derivation works), binary 9bff8f30, drift probe n_changed=1/4 no-drift. First broad coverage (bns46/47/85 sampled for the first time). Endpoint worked from attempt 1.

**Verdict: NOT scale-ready. GO on 31 clean-unique → T 104→135; NO-GO on unattended scaling to 200.** 14 drops: 11 content-FAIL + 3 dedup (2 bns85 {0028,0030 → keep 0026}, 1 bns46_intentional_aid {0010 → keep 0006}). Defect rate 14/45 = 31%.

**DENY 0/2 — the deny-authoring rule half-worked.** It DID fix the round-8 mens-rea contradiction (el1 is conduct-only in both: 405m_0044 "withdrew… bought groceries", 405u_0044 "sold… retained proceeds" — neither asserts dishonest intent). But a NEXT-LEVEL defect: the good-faith denial is CONTRADICTED by self-serving details in the conduct facts — 405m_0044 el1 "PERSONAL groceries" vs eldeny "served Caleb's advantage / supplies he requested"; 405u_0044 el1 "retained the sale proceeds in his firm's operating account" vs eldeny "good faith to protect Tamsin." A person genuinely acting in good faith for the principal would not buy personal goods / pocket the proceeds; the conduct fact defeats the defence. **Fix: a further deny-authoring rule — the conduct facts must be genuinely EXPLICABLE by the good-faith claim; no self-serving markers (personal use, retaining proceeds for oneself) that contradict the defence.** Deny shape still not producing correct records; 0 legitimate deny this round.

**Other residuals (elevated, at the teacher's quality ceiling):** fact_fit 6/45 = 13% (up from round-8's 6%; 3 bns47 + 405m/405u/415prop) — the #9 strengthening isn't fully holding; omission 3/15 = 20% leak (405m_0045 fact proves+leaks, 405u_0040 generic-boilerplate abstention, bns47_0026 narrative+fact leak); narrative_neutral 3/45 (416_0034/0035, bns47_0026 — role/title bans reduced but didn't eliminate); diversity — surname avoid-list WORKED (0 Vane/Vance flags) but 2 template-dup clusters slipped the guard in the new bns contracts (full read caught them).

**Strategic note for the push to T≥200:** these residuals (fact_fit/omission/deny coherence) are semantic over-assertion at qwen3.7-max's precision ceiling — the full read catches them but they won't drop to zero by prompt tuning alone. Plan for ~70% full-read yield: reaching T≥200 from 135 needs ~90+ more sampled attempts (not 65), each gated by my full read. Path: land the deny self-serving-conduct fix (the one clear prompt lever, deny is 0/2) + continue rounds; accept that omission/fact_fit sit at ~10-20% and the read gates them. Merge the 31 → T=135; 14 drops to reviewed_drops (reasons: denial_real/fact_fit/omission_leak/narrative_leak/coherent for the 11, template_duplicate for the 3).

## Two content-signs (T=135 merged) — 2026-09-16 05:17 BST

Deduped-31 merged → T=135 {snapshot_proven:104, alias_unpinned:31} — first alias supervision in T.

**Deny self-serving-conduct fix @ `3a67b84` — content SIGNED at `e1a65a6c…`.** Manifest content-sha reproduces `e1a65a6c3cab50b597a2aac61e2589fec52062f8f5cbe68d135e1a8475a594ee`. The extended `deny_authoring_spec` targets my exact round-9 finding: bans self-serving markers (personal benefit, diverting funds/property to the actor's own use or account, pocketing proceeds) that make the good-faith claim implausible, with positive guidance (write the conduct as directed toward the entrusting party's OWN stated purpose, never the actor's personal use / a separate account under the actor's control) + a concrete worked example. Directly fixes 405m_0044 "personal groceries" and 405u_0044 "retained proceeds in his firm's operating account." Prompt-only (the large file diff is the not-yet-rebased T=135 records, not amendment content — the content-sha I bind to is invariant to that).

**Endpoint-non-determinism caveat @ `bf1a61a` — content SIGNED at `bbd430f4…`.** Content-sha reproduces `bbd430f49a75d8e9f01b160b116669869573f88a35cb7bcba0fe74e7b0625a35`. My pre-approved wording VERBATIM (endpoint non-deterministic even at temp 0 + fixed seed; alias tranche unreproducible at the ENDPOINT/generation level; per-attempt seed retained for audit but does NOT pin output), and it correctly VOIDS the seed_derivation reproducibility claim for the alias_unpinned tranche with a cross-reference. **Both SIGNED.** On Track A's deny-fix shape-sign the lead merges both; then the next alias round runs under the deny fix.

## Deny-fix re-sign after parallel-merge rehash @ `8441df4` — content SIGNED at `4e847974…` — 2026-09-16 05:23 BST

Lead self-corrected the "content-sha invariant to prior" claim (d0 caught it): the manifest hash excludes each entry's own sha fields but includes OTHER entries' text, so the caveat entry joining the chain before the deny-fix changed the composite hash. Re-verified myself: new sha reproduces `4e84797466b660b2c4b733d1b19c8811fa2a2c2267e5d46aa27fca2c02c6af21`; `deny_authoring_spec` is BYTE-IDENTICAL to what I signed at 3a67b84 (diff empty); the ONLY manifest delta vs 3a67b84 is the added `endpoint-non-determinism-caveat` amendment entry (its own date/changes/signatures_required fields — all additions, nothing I signed deleted or altered), and that caveat's content I already content-signed at bbd430f4. **Re-confirmed: content SIGNED at `4e847974…`** — the deny rule reviewed is unchanged, only the composite hash shifted from parallel-merge ordering.

## Alias round 2 full read @ `c008c07` — GO on 27 (T→162); DENY FIX VALIDATED; fact_fit/omission regressed — 2026-09-16 06:13 BST

Provenance clean (72 alias_unpinned / 104 snapshot_proven, no mislabels, drift stable), every_A_i_meets_floor already MET at T=135 → only aggregate T≥200 remains. GO on 27 clean-unique → T 135→162; NO-GO on 14 (13 content + 1 dedup); yield 27/41 = 66% (matches the ~70% projection).

**DENY self-serving fix VALIDATED.** The one deny (405u_0049) PASSES denial_real AND coherent — conduct redirects entrusted funds to a third-party emergency-repair CONTRACTOR (not the actor's own account), denial explains preventing the vessel's structural failure = serves the entrusting party; genuinely explicable, no self-serving markers. Clear contrast to round-9's "personal groceries"/"own account" failures. The deny-authoring rule now produces a substantively correct denial. (Caveat: that same record still DROPS on an orthogonal fact_fit flaw in a conduct fact, so 0 deny ENTERS T this round — but the SHAPE is confirmed working.)

**Regression — fact_fit and omission climbing, not falling.** fact_fit 10/41 = 24% (up from round-9's 13%), spread across contracts, heavy on ipc182 (182m_0036/0038, 182a_0031/0033 = 4/10). omission 4/13 = 31% (up from 20%). narrative_neutral 0/41 (role/title bans holding). The chained-element and omission-leak residuals are NOT improving with prompt tuning — they WORSEN as coverage broadens to the harder, mens-rea-heavy contracts (ipc182, bns46/47/85). This is qwen3.7-max's precision ceiling on complex-element limbs, exactly the ~ceiling I flagged round-9; the full read gates them (27 clean enter T) but yield on hard-contract-heavy rounds will sit at ~66% or lower, not climb. Per-id floors already met, so this is a yield/effort cost, not a coverage blocker.

**14-drop list → reviewed_drops:** fact_fit: 405u_0048, 405u_0049, 415d_0039, 416_0039, 182m_0038, 182a_0033, bns47_0031, bns46_intentional_aid_0013; omission_leak: 415prop_0041, 415prop_0042, 182m_0036, 182a_0031; coherent: bns85_0032; template_duplicate: bns46_intentional_aid_0015 (keep …_0012). Merge 27 → T=162; ~1-2 more rounds at ~66% reach T≥200.

## Alias round 3 full read @ `cd7c1ab` — GO on 24 (T→186); deny denial validated 3rd time but recurring el1/el2 entanglement — 2026-09-16 06:57 BST

Provenance clean (95 alias_unpinned / 104 snapshot_proven, no mislabels, drift stable). GO on 24 clean-unique → T 162→186; NO-GO on 13 (12 content + 1 dedup); yield 24/37 = 65%. One more round clears T≥200.

**DENY denial validated a 3rd consecutive time (denial_real PASS, coherent PASS)** — 405u_0054 diverts relief tarpaulins to a worse-flooded ward (Ward 9), not personal use; good-faith belief genuinely negates dishonesty. The self-serving fix is solid. **BUT the deny DROPS on fact_fit AGAIN** (2nd round running): el1 "distributed to Ward 9 instead of Ward 12, contrary to the explicit allocation clause in the written agreement" also asserts el2's contract-violation content. This is a RECURRING, SPECIFIC entanglement in the use_or_disposal deny — el1 (the disposal act) and el2 (violation of the governing agreement) are naturally coupled, so the teacher folds el2 into el1. Distinct from the general fact_fit ceiling: it's a targeted, fixable structural issue. **Optional targeted fix (thickens deny coverage — T currently holds only ONE deny record, round-8's 405u_0039):** a use_or_disposal-deny prompt clarification — el1 states ONLY the disposal act (what was done with the property), NOT that it was "contrary to the agreement" (el2's job). Flagging because deny is validated as a shape yet keeps failing to ENTER T on this one entanglement; if the operator wants deny supervision thicker than 1 record, this is the lever. Not a ceiling issue.

**Residuals at ceiling (unchanged trend):** fact_fit 9/37 = 24% (stable), omission 5/14 = 36% (climbing on hard contracts — 182a, bns46/47), narrative_neutral 2/37, coherent 4/37, english_law_register 1/37 (first occurrence). Per-id floors already met; full read gates all; yield ~65%.

**13-drop list → reviewed_drops:** fact_fit: 405m_0055, 405u_0054, 415prop_0045, 416_0043, 182a_0038, bns47_0040, bns46_intentional_aid_0020; omission_leak: 415prop_0046, 182a_0036, bns47_0038, bns46_instigation_0037, bns46_intentional_aid_0017; template_duplicate: bns85_0040 (keep …_0036). Merge 24 → T=186.

## Amendment #15 deny-el1-el2-disentanglement @ `3c071bd` — content SIGNED at `4fb94e42…` — 2026-09-16 07:07 BST

(deduped-24 merged, main 4539673, T=186 {snapshot_proven:104, alias_unpinned:82}.) Pin reproduces `4fb94e429d117fc258d9dc9047a3eed68fd6297b7a718fee9508021b6a0f351a` (prior `4e847974`). Manifest diff confined to deny_authoring_spec + the amendment entry. Injected wording is my round-3 spec VERBATIM: when a deny-shape's conduct element naturally couples with a separate violation/breach element (use_or_disposal: disposal act vs breach of the written term), el1 states ONLY the disposal/conduct act (what/to-whom/where) and must NOT assert it was "contrary to / in violation of / breaching" the agreement — that belongs EXCLUSIVELY to el2's fact; framed as the single-element-per-fact rule applied to the deny conduct fact, co-injected with the mental-state + self-serving clauses when shape==deny. Present in the harness prompt (L666+). Fixes the recurring fact_fit drop that kept validated denials out of T (deny currently only 1 record in T). **Content: SIGNED at `4fb94e42…`.** Merge on Track A's shape-sign → final alias round under the fix → my read → the round that should carry T past 200 WITH deny records entering.

## Alias round 4 (FINAL) full read @ `030d3ed` — T CLEARS 200: GO on 26 → T=212 — 2026-09-16 08:08 BST

Provenance clean (125 alias_unpinned / 104 snapshot_proven, no mislabels, drift 16 rows/0 flagged). Snapshot deny 6daee122_0039 confirmed = my round-8 validated deny already in T=186 (not an unread deny). **GO on 26 clean-unique → T 186→212 — the T≥200 count gate is CLEARED on durable, full-read-gated records (not the raw 229).** NO-GO on 17 (16 content + 1 dedup); yield 26/43 = 60% (ipc182-heavy round, so at the low end of the ceiling band).

**DENY 0059 — disentanglement fix worked on el1, but the leak relocated to el0.** denial_real PASS + coherent PASS (4th consecutive correct denial — the self-serving + mental-state + el1/el2 rules all hold: el1 "transferred to Apex for reconditioning" is disposal-only, no "contrary to"). BUT fact_fit FAIL for a NEW reason: el0 "received… under a written agreement specifying he would hold the machinery SOLELY for inspection" pre-establishes el2's restriction content. The restriction leak is whack-a-mole — cleared from el1, reappears in el0. So the new deny still drops; deny stays at ONE record in T (round-8's). If deny thickness matters beyond 1, the single-element rule needs extending to el0 (entrustment states possession/custody only, never the restriction — that's el2). Optional; T=212 is met regardless.

**Residuals (ceiling, ipc182-heavy):** fact_fit 13/43 = 30% (6 of them ipc182 — the hardest, mens-rea-heavy limb), omission 2/? low, narrative_neutral 3, coherent 3, register 0. Consistent with the qwen3.7-max ceiling flagged since round-9; full read gated them.

**17-drop list → reviewed_drops:** fact_fit: 405u_0057, 405u_0058, 405u_0059, 415prop_0054, 416_0047, 416_0050, 182m_0046, 182m_0047, 182a_0043, 182a_0044, 182a_0045, bns47_0041; omission_leak: 182m_0048, bns46_intentional_aid_0021; narrative_leak: 415prop_0053; coherent: 405w_0058; template_duplicate: bns46_intentional_aid_0022 (keep …_0023). Merge 26 → **T=212**.

**Status of the P2a T-tier: COMPLETE at T=212.** The T-related decide conjuncts are met (T≥200 now, every_A_i already met, reachability/D/leakage/no-lineage). Remaining for a full P2a PASS (P2b/SFT gate): the P (partial-IR) and R (retrieval-only) inventories — separate work, not teacher sampling.

## Two frozen-prereg interpretation rulings (P/R inventory scoping) — 2026-09-16 08:19 BST

**RULING 1 — R multiplicity: 3 DISTINCT task-framings/provision ARE permitted as distinct R instances, IN PRINCIPLE, no amendment — conditioned on my concrete distinctness check of studio's designs.** The frozen prereg sources R_min=2,000 (line 145) as "≈ 2 per provision across IPC + BNS PLUS held-out-disjoint templates" — the prereg ITSELF contemplates additional distinct templates/framings per provision beyond the base ~2, so this is interpretation within the freeze, not an amendment. The bar is the prereg's own rule (lines 27-28): "rephrasings, option permutations and repeated teacher attempts do not multiply source-row/instance count." So each framing must be a genuinely DISTINCT retrieval TASK testing a different competency, NOT a reworded query. d0's three — (a) identify-provision-from-case-facts, (b) cite-from-section-text, (c) discriminate-within-a-candidate-set — are structurally different task types in principle (a model can pass one and fail another), so they qualify. Conditions before R is declared cleared: (i) I judge studio's three concrete designs for genuine task-distinctness (approve only if not rephrasings); (ii) "held-out-disjoint" — each framing excluded against the held-out eval sets (leakage); (iii) d0 reports the NET distinct-instance count after leakage exclusion AND after dropping any framing a given provision cannot support (e.g. (c) needs confusable neighbours) — that net must clear 2,000, not the gross 915×3=2,745. If the net clears 2,000 with genuinely-distinct framings, R is built to floor with no amendment. If it falls short, THEN amend R_min down or approve a 2nd corpus source (IL-TUR stays excluded per leakage). Forward studio's three framings for my distinctness judgment.

**RULING 2 — control-ratio floor: the binding figure is 25%, measured 29.2% PASSES; both the 40% and the README's 28.6% are stale.** This is already settled by my signed amendment (d) (inventory-exclude signature, 2026-09-15 15:38 BST): "Amendment (c)'s 'control ≥ 40% of D' conflicts with its own instruction… The floor is replaced: **control ≥ 25% of D, ≥ 10 distinct control scenarios per id**…" I documented there that 40% is structurally unreachable (1 control per n+2 records ≈ 25-30%), so the 40% (lead-relayed) is the SUPERSEDED figure, not binding. The README's 28.6% (= 2/7, a per-scenario structural example for a 5-element contract) is a mislabelled illustration, not the floor. **Binding: control ≥ 25% of D. Measured 29.2% ≥ 25% → PASSES.** Also verify the real coverage guarantee — ≥ 10 distinct control scenarios per id — which amendment (d) makes the actual bound, not the ratio. No prereg amendment needed; the binding figure already exists via amendment (d). Fix required: correct the README (28.6%) and any lead-relayed doc (40%) to cite the amendment-(d) figures (25% ratio + ≥10-scenarios/id) — a documentation correction, not a dual-sign amendment.

## Amendment #16 deny-el0-el2-disentanglement @ `a556cf3` — content SIGNED at `4b5f3f17…` — 2026-09-16 08:20 BST

Pin reproduces `4b5f3f176f11e63a5a6af59cc8a51f6a454722aaafc28ae8afa150a30e3ffb6d` (prior `4fb94e42`). Diff confined to deny_authoring_spec + amendment entry (2 files: manifest + sampler). Injected wording is my round-4 el0 spec VERBATIM: the entrustment/possession element (el0) states ONLY the fact of possession/custody (who held what, from whom, in what capacity) and must NOT state the restriction/condition/purpose ("solely for", "only to", "on condition that") — that belongs EXCLUSIVELY to el2 (violation/breach); same single-element-per-fact rule applied to the deny entrustment fact. Present in the manifest field and spliced deny-only in the harness (L680, inside _deny_mental_state_rule right after the el1↔el2 clause, co-injected when shape==deny). Fixes the round-4 whack-a-mole (el1 fix cleared el1, restriction leak relocated to el0's "held solely for inspection"). **Content: SIGNED at `4b5f3f17…`.** Merge on Track A's shape-sign → deny-thickness alias round under it → my read. T=212 is already met; this thickens deny coverage (currently 1 record) before SFT.

## P (partial-IR) inventory @ `1656340` — mapping content SIGNED — 2026-09-16 08:31 BST

(el0 amendment #16 merged, main 29ab159, pin 4b5f3f17.) P build is a yield_gate.py code change, no pin. **P mapping content SIGNED** — faithful to the prereg's partial-IR definition (line 22: "grounded spans/roles/labels or typed abstentions, NO certified application claim"; line 71: "unproved application claims must be partial-IR"; feasibility §3 reserves the 5,314 CaseHOLD-val rows, the source I kept for P in amendment (d)). Verified all 5255 records: kind=holding_selection (5255/5255), target.dataset_label = candidate-set + selected_id (a dataset-LABEL selection, not a certified inference), abstain=true (5255/5255), NIGAMANA not_proved (5255/5255), and ZERO records carry an "applies"/proved application claim (jq scan empty). The answer frames itself "Dataset selection: candidate N… the supplied dataset label" — supervises the selection/label + typed abstention without certifying the legal holding. That is exactly partial-IR. Count: 5,314 CaseHOLD-val rows → 5,255 valid + 59 skipped (structurally_valid null; malformed source rows missing `<HOLDING>`, clean); 5,255 ≥ P_min 1,000; decide P_meets_floor MET. Leakage disjointness holds by construction (P ⊆ CaseHOLD-val, which the prereg excludes from D/T/R).

**Non-blocking provenance-hygiene flag (does not gate the sign):** the provenance.jsonl rows carry only {record_id, row_id, structurally_valid, tier, wire_sha256} — no explicit `source_dataset` label and an opaque integer `row_id` (42508+). The CaseHOLD-val sourcing is confirmed only via the builder script's doc, not self-documented in the evidence. For leakage-auditability, add `source_dataset:"casehold_val"` + the CaseHOLD example id to each provenance row so P's reserved-source sourcing is verifiable without reading the builder. Recommend before SFT; not a mapping-validity issue. **Content: SIGNED** (P mapping) at `1656340`; Track A shape in parallel.

## R inventory — distinctness ruling + feasibility decision-tree — 2026-09-16 08:42 BST

**RULING 1 (distinctness): all three framings ARE genuinely distinct retrieval instances — they multiply legitimately, no rephrasing/padding.** (a) fact-snippet identification (input: Illustration segment; candidates: same-chapter neighbors; competency: fact→law), (b) doctrinal citation-from-paraphrase (input: authored title paraphrase, never quotes text; candidates: cross-act pool; competency: doctrine→citation), (c) verbatim-text discrimination (input: operative segment; candidates: lexically-confusable neighbors; competency: fine text-discrimination). They differ on BOTH the input-source axis AND the candidate-construction axis, and test competencies a model can pass/fail independently. None is a rephrasing, option-permutation, or repeat-attempt (the prereg's three no-multiply cases, lines 27-28). The segment-disjoint leakage design (a=Illustration only, c=operative only, no shared substring; b never quotes text) means (a) and (c) from the same provision are not "the same text counted twice." This IS the "≈2/provision PLUS held-out-disjoint templates" the frozen prereg's R_min basis (line 145) already contemplates — interpretation within the freeze, not an amendment. **All three approved as distinct.** Per-instance rule: exactly ONE (c) per eligible provision — a provision re-used with a DIFFERENT distractor set is an option-permutation and does NOT multiply.

**RULING 2 (feasibility): sequencing (i) — compute the real net FIRST; pre-stated decision tree so d0 does not round-trip.** Honest net so far: (b) ~914 + (a) ~153 = ~1,067; (c) is the swing, unsized until d0 computes the corpus similarity graph (≥1 confusable neighbor per provision). d0 computes the net = |distinct (a)+(b)+(c) instances| after leakage exclusion AND per-provision eligibility (drop a framing a provision can't support). Then:
- **Net ≥ 2,000:** R clears, NO amendment. Done.
- **Net < 2,000:** amend R_min DOWN to the honest achieved distinct-instance count (dual-sign). Rationale: the prereg's own note calls these "deliberately modest floors, not targets" (line 137); if the corpus + genuinely-distinct-framing reality yields < 2,000, the floor moves to reality. **I do NOT approve two tempting alternatives:** (1) stretching the prereg's undefined "held-out-disjoint templates" phrase into a license to keep adding framings until 2,000 is hit — that manufactures instances to pass a floor, exactly the padding the no-multiply rule bars; the phrase already describes what (a)(b)(c) ARE, it does not unlock more. (2) a 2nd corpus source without separate leakage vetting (IL-TUR stays excluded). The three genuinely-distinct framings are the legitimate R; if they honestly yield < 2,000, the floor amends to the real number, not the number stretched to the floor.

Honest projection: even generous (c) eligibility (~60-90% of 915) lands (a)+(b)+(c) at ~1,600-1,900 — PROBABLY short of 2,000 — so expect the amend-R_min-down path. That is the faithful outcome, not a failure. Forward d0's computed net and I rule the exact amended R_min (or confirm the clear).

## P provenance-hygiene re-confirm @ `trackB/p-provenance-hygiene` — content OK — 2026-09-16 08:45 BST

My non-blocking flag (7cd1c5c) is built and confirmed. records.jsonl BYTE-IDENTICAL (sha `42460f41…`) so the mapping sign still binds; only provenance.jsonl changed. Each of the 5,314 provenance rows now carries `source_dataset:"casehold_val"` + `casehold_example_id`, all populated; `casehold_example_id` equals `row_id` = the real CaseHOLD dataframe id (42508+, not an opaque 0-counter — trackB's correction, source was more traceable than assumed). Field naming `casehold_example_id` avoids colliding with the schema's own `Udaharana.example_id`. P's reserved-source (amendment (d) CaseHOLD-val) sourcing is now self-documenting and leakage-auditable from the evidence alone. **Content OK** — the two fields are exactly what I wanted; closes the pre-SFT P item. P=5255/pin/decide unchanged.

## R feasibility — amend-down authorized: tau=0.5, R_min→real built count (17th amendment) — 2026-09-16 08:46 BST

studio's TF-IDF cosine (IPC+BNS jointly, deterministic, no ML lib) fixes the R ceiling: (a)153 + (b)914 + (c)808@tau=0.5 = **1,875**; even loosest tau=0.3 = 1,939. R CANNOT reach 2,000 on the 3-framing design at any threshold — a hard corpus ceiling, not a tuning gap. My pre-stated tree triggers: amend R_min DOWN to the honest achieved count.

**Ruling 1 — tau=0.5 APPROVED.** Since R_min amends down regardless, tau optimizes (c) QUALITY, and quality here IS the distinctness bar: (c) is a genuine discrimination task ONLY if its distractors are genuinely confusable. 0.5 captures real confusable pairs (IPC 25↔26 fraudulently/dishonestly, 34↔35 common-intention, IPC↔BNS parallels) per the spot-checks; loosening to 0.3 admits loose-metric artifacts that make some (c) instances trivial, for ~131 more instances toward a floor we're not reaching anyway. Quality over coverage. (If any 0.5-admitted (c) pair proves not genuinely confusable on my build spot-read, tighten toward 0.6.)

**Ruling 2 — sequencing (i): amend to the REAL built count.** Pre-authorized now: the amend-down mechanism + tau=0.5 + the 3-framing design, no padding, no 4th framing (studio declined one — matches my rule). d0 builds all three to the validated count (real ≤ 1,875, since some eligible provisions may fail validate_record). Then the 17th amendment (dual-sign) sets **R_min to a clean stated figure at or just below the validated built count** — I set the exact number once d0 reports it (a stated floor with a hair of honest margin, not literally floor==inventory). Rationale for the record: the prereg calls these "modest floors, not targets" (line 137) existing to prove the pipeline YIELDS real retrieval supervision; 915 provisions × up to 3 distinct competencies = ~1,875 genuine distinct instances is ample retrieval supervision — the floor moves to the honest corpus reality, never reality stretched to the floor.

**Two conditions on the R build before I sign the inventory (same content-gate standard as P/T):** (i) R provenance self-documents per instance: framing (a/b/c), source provision id, source SEGMENT used (Illustration / operative / title-paraphrase), and for (c) the distractor set + tau — so distinctness AND leakage-disjointness are auditable from the evidence alone; (ii) I do a DISTINCTNESS SPOT-READ of the built (a)/(b)/(c) records (a sample per framing) to confirm they are genuinely distinct retrieval tasks as designed, before signing the R_min amendment + inventory. Forward the built count + a sample and I rule the exact R_min and sign.

## R inventory distinctness spot-read + R_min ruling @ `p2a-retrieval-scaffolding` — 2026-09-16 08:59 BST

Built net = 1,909 = (a)153 + (b)914 + (c)842@tau=0.5, all through validate_record (0 skipped). Spot-read from the branch (rebase is a stale-base fix, R content unaffected; I sign on the lead's clean-branch confirm).

**Distinctness spot-read PASSES.** (1) Structure faithful: all 1,909 kind=statute_identification, target.abstain=true, answer "Dataset identification: <provision>", and ZERO records carry an applies/proved application claim — retrieval-only, no certified inference. (2) The three are genuinely distinct competencies on the built records: (a) Illustration fact → provision (fact→law), (b) title-paraphrase → provision, cross-act distractors (doctrine→citation), (c) operative text → provision among confusable neighbours (fine text-discrimination); segment-disjoint confirmed (a=Illustration, c=operative, b=paraphrase, no shared substring). (3) **(c) distractors are genuinely confusable at tau=0.5 — I checked the actual cosine scores, not just the design:** the Section 4 set that looked loose (→ 79/14/55/125) scores 0.618/0.596/0.589/0.573 — solidly similar, not artifacts; IPC 25↔26 at 0.619; and tau=0.5 correctly EXCLUDES sub-0.5 (IPC 25's next neighbours 0.449/0.444/0.388 dropped). A 16% marginal tail (0.50-0.55 edges) remains more-similar-than-random and keeps (c) non-trivial. **tau=0.5 STANDS — no tighten to 0.6** (tightening would thin good distractor sets, e.g. drop 3 of Section 4's 4, for marginal precision on an already-genuine task). (4) (a) same-act ±2 document-order proxy for "same-chapter neighbour" (corpus has no chapter metadata, `part` empty) is ACCEPTABLE — adjacent statute sections are topically related, so ±2 are plausible distractors; documented in provenance. (5) ids slugified per nyaya-law-v1 (no spaces/slashes), real id preserved in Passage.source_uri + provenance.source_provision_id — cosmetic, fine. (6) The (c) 842-vs-808 gap is TF-IDF implementation variance at the 0.5 boundary; the committed artifact is d0's 1,909 build, so it's settled.

**R_min RULING: amend R_min from 2,000 → 1,900** (17th amendment). 1,900 is a clean stated floor just below the honest validated built count (1,909), a 9-record margin — a floor with a hair of margin, not floor==inventory. Rationale for the record: the frozen prereg calls these "deliberately modest floors, not targets" (line 137) existing to prove the pipeline YIELDS real retrieval supervision; 915 provisions × up to 3 genuinely-distinct competencies = 1,909 distinct instances is ample; the 3-framing design hard-ceilings below 2,000 at any tau (max 1,939), so the floor moves to the honest corpus reality with no padding and no 4th framing. **I authorize content-signing the 17th amendment (R_min 2000→1900) + the R inventory** on the lead's confirmation the rebased branch is clean (R content byte-identical). On that + Track A shape-sign + merge, R_meets_floor flips MET → FULL P2a PASS (T=212, P=5255, R=1909 all ≥ their floors).

## Amendment #17 retrieval-only-rmin-to-corpus-reality @ `8c2cc76` — content SIGNED at `baffcb21…` — FULL P2a PASS — 2026-09-16 09:04 BST

Pin reproduces `baffcb2194b08bc457af5f5587db8843f03371541e120b192f1bc91eef685b5a` (prior `4b5f3f17`). thresholds.R_min = 1,900 (my ruling, 8deefb2). R inventory = 1,909 records, the artifact my distinctness spot-read validated (framings distinct, tau=0.5 (c) distractors genuinely confusable at 0.57-0.62, no application claim, provenance self-documenting). **decide → PASS; all 9 conjuncts MET:** reachability_all_ids, D_meets_floor, T_meets_floor (212), every_A_i_meets_floor, leakage_index_built, no_accepted_lineage_matches_excluded_group, reviewed_drops_resolved, P_meets_floor (5,255), R_meets_floor (1,909 ≥ 1,900). **Content: SIGNED at `baffcb21…`** — the R_min=1,900 figure (my pre-authorized ruling) + the R inventory. On this + Track A's shape-sign the lead merges → **FULL P2a PASS**. This is the last signature on the gate.

**P2a gate CLOSED.** Teacher-tier T=212 (104 snapshot_proven + 108 alias_unpinned, majority-alias provenance documented + auditable), P=5,255 partial-IR (CaseHOLD-val, self-documenting), R=1,909 retrieval-only (915-provision corpus × 3 distinct framings, honest floor). Every record checker-certified AND full-read-gated; every drop durable in reviewed_drops; provenance honest to the endpoint-non-determinism/alias caveats. Remaining beyond P2a: P2b/SFT under its own prereg (GPU, RunPod house rules) — separate gate, not opened by this signature.

## Deny augmentation read — alias 0069 NO-GO; deny thread CLOSED at 1 — 2026-09-16 09:25 BST

One-shot read of t_ipc405_use_or_disposal_670350a9_0069 (alias_unpinned, binary 9bff8f30). **NO-GO — fact_fit FAIL. Deny stays at 1 (round-8's 405u_0039); deny sampling ends per agreement.**

**The el0/el1 disentanglement fixes WORKED** — the reader confirms el0 (custody only) and el2 (storage restriction only) "each cleanly establish only their own element with no bleed into the others"; el1 states only the transfer act, no "contrary to," no mens-rea, no self-serving marker (Apex is a third-party processor). The restriction-leak whack-a-mole is resolved: nothing leaked into el0 or el1.

**But a DEEPER structural tension surfaced, and it is why deny is hard to thicken:** el1's required element is "DISHONESTLY uses or disposes." Under the el1-conduct-only rule, el1 now states a bare innocuous transfer ("transferred to Apex for processing") — which establishes the disposal ACT but NOT the dishonesty. So element 1 as labeled is never established on its own terms; the "dishonestly" is shown nowhere except being simultaneously negated by the good-faith denial. For a mens-rea-bearing element under a good-faith (mens-rea-negating) denial, el1 CANNOT both establish the element's dishonesty AND be consistent with the denial — assert dishonesty and it contradicts the denial (round-9's failure); omit it (my el1 fix) and el1 fails fact_fit (this record). Round-8's 405u_0039 passed only because its el1 carried "disposing CONTRARY TO MANDATE" — the very el1→el2 coupling my amendment-15 fix later banned; that framing established el1 as more-than-innocuous. Remove both the dishonesty and the contrariness and el1 is unestablishable.

**Conclusion:** the good-faith deny shape is inherently tension-bound for offences whose contested element is itself the mens rea (ipc405 use_or_disposal / misappropriation). Further deny sampling on these would not help — it's structural, not sampling luck. The deny shape works cleanly where the defeater is a SEPARATE route (e.g. bns69 "amounts to rape" — a graver-offence defeater that doesn't negate an element's own mens rea). Deny in T stays at 1 legitimate record (405u_0039), validated under its contemporaneous round-8 read; I do NOT re-open that signed, admitted, post-PASS record over the el1 framing amendment-15 introduced later. Deny thread CLOSED. denial_real/coherent/narrative/register all PASSED on 0069 — the denial itself is correct; only the element-establishment fact_fit blocks it.

## P2b/SFT eval-gate prereg @ main `75e335f` — content SIGNED (blob `fc29c0600eed`) — 2026-09-16 09:47 BST

Verified the prereg carries my gate (5ac038b) + both refinements (76f93ad) faithfully: §3 full-IR gold = checker-certified (9bff8f30, fresh per-row calls, fail-closed) ∧ my full content read; near-miss negatives (element-short-by-one / confusable-contract, never nonsense) with my certified held-out labels; §4 conjunction with (1) structural ≥0.98, (2)/(2b) measure-and-set above base, (3) false-prove ≤0.02 HARD, (3b) abstention ≥0.90 HARD, (4) retrieval ≥0.493. The P-eval carve is CLEANER than my proposal — sourced from casehold-TEST (example_ids 47821-53134), disjoint from the casehold-VAL P trained on except the single dup id 47821 (excluded from P-eval); keeps all 5,255 P and all 212 T in training. **Content: SIGNED at blob `fc29c0600eed`.**

**Answers to the two check-me questions:**
1. **Measure-and-set above base for 2/2b/4 is my intent** — deliberately not a fixed floor, so the gate rewards SFT LIFT and isn't gamed by an absolute the base already clears. The safety ceilings (3 ≤0.02, 3b ≥0.90) and structural (1 ≥0.98) STAY FIXED — safety and table-stakes don't scale with the base. **One condition:** "above base" must mean the SFT lift EXCEEDS Track A's preflight/eval NOISE BAND (a real lift beyond the measurement CI), not a trivial +ε that noise alone could produce — tie metrics 2/2b/4 to Track A's noise threshold so "exceeds objectives" is a genuine, not statistical-artifact, improvement.
2. **The ~210-320 sampled → ~140-210 clean gold budget is realistic** for my read cadence in batched cycles (~40/run, ~5-8 reader runs); it IS on the preflight critical path as budgeted — not instant, a few read cycles.

**Carried to the eval-BUILD sign (not this prereg sign):** the val∩test example_id overlap computation (={47821}) committed as leakage evidence (same exclusion-index standard as P2a); R empty-cell (provision×framing gaps) honestly labeled in-registry-only; and my clean-read + positive/negative near-miss certification on the built gold. Freeze on my sign + Track A's shape-sign; then route the eval build. No GPU until frozen + eval built/read-certified/hashed + both bases preflighted.

## P2b prereg re-confirm @ blob `2fdcdd191896` — SIGNED — 2026-09-16 09:49 BST

Diff vs the sha I signed (fc29c0600eed) is EXACTLY my binding condition and nothing else in my content scope: metrics 2/2b/4 now pass only on a statistically-significant SFT-over-base lift (paired 95% CI on Δ excludes zero, §5 protocol, N≥800), a numerically-higher-but-within-noise result is an explicit FAIL; fixed metrics (1 ≥0.98, 3 ≤0.02, 3b ≥0.90) stay absolute; one paired protocol scores both SFT-over-base and base-vs-base. Gold definition, near-miss negatives, and thresholds unchanged. Closes the null-masquerade gap I named. **Re-confirmed: content SIGNED binding blob `2fdcdd191896`.**

**One intersection to reconcile at EVAL-BUILD sizing (not a prereg blocker — flagging for the build):** §5 sets N≥800 "across families" for paired significance, but metric 2/2b is the FULL-IR family specifically, whose gold I budgeted at ~140-210. At N≈200, 2·SE_diff ≈ 0.07, so a full-IR Δ under ~7 points is within noise — metric 2b could be underpowered on its own family. Reconcile at build: either (a) scale the full-IR eval toward ~800 gold (a larger build, ~1,200 sampled + my read — bigger critical-path cost), or (b) accept metric 2b at lower power with the wider noise band it implies (Δ must clear ~7pts, a harder but honest bar). Track A owns the power/sizing call; I own the gold-yield it implies. Let's set the full-IR eval N with Track A before the build so the significance test on the headline metric is actually powered.

## P2b prereg FINAL blob `dbc72d3fb658` @ main `54f0c5d` — content-sign RE-BOUND — 2026-09-16 09:50 BST

Diff vs the blob I re-confirmed (2fdcdd191896) is ONLY Track A's §6 candidate-ordering ruling: fixed order → SEED-PINNED per-item SHUFFLE (identical across both bases, gold position ~uniform) + a required positional-bias diagnostic (accuracy vs gold position; correlation ⇒ flagged). Checker-safe — check() scores the selected candidate ID, order-independent; the shuffle touches display only. My content is UNCHANGED (thresholds, full-IR gold = checker ∧ my read, near-miss negatives, the leakage-disjoint carve, the noise-band condition all identical). No content objection — the shuffle is a validity strengthening (a fixed order would reward a learned positional shortcut the R builders' deterministic candidate ordering could induce, measuring memorized position not held-out identification). **Content-sign RE-BOUND to final blob `dbc72d3fb658`** so my sign + Track A's land on the same version. Freeze on both; then the eval build routes to d0 (sample → my clean read → checker-certify → hash → dual-sign the eval set). Full-IR-N-vs-power intersection (record 90e94ab) still to reconcile with Track A at build sizing. No GPU until frozen + eval built/read-certified/hashed + both bases preflighted.

## P2b prereg AMENDED blob `3b223133a5f8` @ main `ab056f5` — content-sign RE-BOUND + N≈400 feasibility CONFIRMED — 2026-09-16 09:56 BST

Lead retracted the premature freeze (Track A shape-sign e29666a had crossed Track A's own "hold" ruling a31b914); both reviewers re-sign the amended blob. I verified the blob myself: `git rev-parse ab056f5:research/prereg/prereg-P2b-sft-eval-gate.md` = `3b223133a5f816c6e177b50d2bc4f4d2e5005456`, and diffed it against my last-signed blob `dbc72d3fb658` (@ 0d21c3b) — not on the lead's word.

**Diff = exactly three things, all content-safe or content-strengthening:**
1. **CI-bound rule (Track A a31b914):** metric-4's absolute ≥0.493 is cleared by the **Wilson 95% CI lower bound**, not the point estimate; likewise the fixed safety (3 ≤0.02, 3b ≥0.90) and structural (1 ≥0.98) thresholds are met only when their CI bound is on the safe side. This **tightens** a safety-critical gate — no objection, I welcome it.
2. **Aggregate N≥800 → PER-FAMILY N/MDE (Track A a31b914):** removes the "N≥800 across families" text (which P+R bulk would trivially satisfy while the headline **2b full-IR lift lives in the full-IR family ALONE**) and sets **full-IR N≈400 default** (paired McNemar MDE ~3.9–5.5pt ≈ the ~5pt meaningful-lift threshold at ~half the 800 read cost), with a **§5 fallback to N≈200** gated on TWO honest conditions: (i) label the MDE ("a null rules out only >~7pt paired lifts; a smaller true lift is NOT excluded and is NOT evidence T=212 can't teach full-IR"); (ii) a within-noise 2b null **triggers a full-IR N expansion** before any "212 doesn't teach full-IR" conclusion or scale-up kill. Plus: **report the ACHIEVED paired MDE post-hoc** (McNemar b+c discordants) — SFT-over-same-base is correlated, achieved band likely tighter; significance = achieved paired CI, a-priori N only bounds false-negative risk. **This is the resolution of my own record-90e94ab power flag** — power correctly placed where the headline metric lives.
3. Signature block re-opened for re-sign.

**My content byte-unchanged:** gold = checker ∧ my full read, near-miss negatives, leakage-disjoint carve, noise-band condition all identical (verified by the diff touching none of §3/§4-metrics beyond the two Track A rulings above). **No content objection. Content-sign RE-BOUND to amended blob `3b223133a5f8`.**

**Full-IR N≈400 gold-yield: FEASIBLE on my read critical path — no fallback invoked.** In P2a I fully read 5,255 P + 1,909 R + 212 T records via the seat-2 batch reader; ~600 sampled full-IR scenarios netting ~400 clean gold (~66%) is a fraction of that demonstrated throughput (~10–15 read cycles). Honesty caveat, stated not hidden: full-IR scenarios are heavier per-item (full proof trees vs single elements), so I hold the §5 fallback in reserve ONLY as the escape if actual yield falls far below ~66% (sampled ballooning past ~700–800 to net 400) — it is not the plan; N≈400 is.

Freeze on my re-bind (`3b223133a5f8`) + Track A's re-sign. Then route the full-IR eval build to d0 (P+R builds already running per lead): sample → my full clean content read → checker-certify → hash → dual-sign the eval set; at build time I also certify val∩test overlap (={47821}), R empty-cell honest in-registry labeling, and the near-miss positive/negative split. No `create-pod` until frozen + eval built/read-certified/hashed/dual-signed + both preflights signed.

## R-eval framing-a BLOCK + framing-c-only (N=73) — content CONCURRENCE (Track A owns validity) — 2026-09-16 10:12 BST

d0's R-eval build (assistant/trackA/p2b-eval-p-r-build @ e4dbc0a) built P-eval (5,245 = 5,314 val − dup 47821) + R-eval framing-c (73/73), and BLOCKED framing-a as structurally unbuildable. I verified BOTH the block and the framing-c leak-cleanliness independently against the corpus (pravrudhi research/nyaya/corpus/{ipc,bns}.json) + the R-training provenance — not on d0's or the lead's word.

**Structural block on framing-a — INDEPENDENTLY CONFIRMED, exact + bidirectional.** Provision universe = 915 (ipc+bns, 915 distinct ids). Ran the illustration regex (`\bIllustrations?\b`) myself over all 915: exactly 153 bear an illustration segment, 762 do not. framing-a was trained on exactly 153 distinct provisions; untrained-a = 915−153 = 762. My check: untrained-a WITH an illustration = **0**; trained-a WITHOUT an illustration = **0**; total-with-illustration = 153/915. So framing-a is *definitionally coextensive* with illustration-bearing provisions — framing-a's fact_text IS the illustration segment, so the 762 untrained-a cells have literally zero source material. This is not an eligibility-threshold artifact or a build shortcut; it is unbuildable by the framing's own definition. **CONCUR: drop framing-a from the R-eval** (redefining it = scope creep, agreed). The honest future path IF framing-a generalization is ever wanted: hold out illustration-bearing provisions from framing-a TRAINING (a split within the 153), not the 762 — a build-design change, explicitly out of scope now.

**framing-c 73 cells — INDEPENDENTLY CONFIRMED held-out + leakage-disjoint.** My check on the eval provenance vs R-training provenance: 73 records / 73 distinct provisions; provisions also-trained-on-framing-c = **0**; eval fact_text (operative segment) exact-overlapping ANY training framing's fact_text = **0**. Distractors are the below-tau confusable neighbors (provenance carries below_tau/tau/neighbor_similarities) — the *hard* discrimination case, exactly framing-c's design. leak_check_evidence.json + eval_build_report.json (leak_conflicts []) corroborate.

**Content view (the lead's two questions):**
1. **Does framing-c-only R-eval still meaningfully test retrieval generalization for my content standard? YES, with the honest scope already binding.** It tests *task-framing generalization*: the model saw these 73 provisions' text via another framing (b/a) in training and must now identify the correct provision from below-tau confusable neighbors using the operative segment under a framing (c) never applied to them. That is a genuine — and the *strongest*-distractor — generalization test. It is NOT provision-generalization beyond the 915, and now also NOT framing-a generalization.
2. **Is "directional at N=73" honest labeling acceptable on metric 4? YES.** Metric 4 is NOT in the primary gate (1∧2∧2b∧3∧3b) — it is a directional retrieval tier signal. At N=73 the Wilson 95% CI half-width near p=0.5 is ~±0.11, too wide to clear the ≥0.493 bound crisply — which is *exactly why* directional-not-gating is the honest read, and it is consistent with the CI-bound rule I just signed (a bar the CI can't resolve is not a gate).

**CONTENT CONDITION on the §3/§4 amendment (my concurrence is conditioned on this label being factually complete):** the frozen spec must state, explicitly, (a) R-eval = framing-c task-framing generalization, N=73, **directional-only, not gating**; (b) framing-a generalization is **structurally unbuildable on the in-registry 915-provision universe (762 cells have no source material) and is NOT claimed or measured** — not merely "thin"; (c) framing-b remains SKIPPED (914/915 trained, deterministic, nothing to generalize); (d) retain the existing honest label "task-framing generalization on the in-registry universe; NOT provision-generalization beyond the 915." With (a)-(d) in the label, I **CONCUR** with the amendment. The full-IR N≈400 headline build with d0 is unaffected.

## R-eval: framing-c empty-cell DISCARDED (Track A) → provision-disjoint SPLIT — content view (pending sizing) — 2026-09-16 10:24 BST

Track A retracted the whole empty-cell R-eval. **The catch is correct and I own the under-flag:** I verified framing-c's leakage-cleanliness (0 fact_text overlap) but did NOT flag the distractor-difficulty asymmetry. I re-checked the eval provenance: all 292 distractors across the 73 records have TF-IDF similarity <0.5 (max 0.4947), every record below_tau=True. The 73 "held-out" provisions were exactly those with NO tau≥0.5 neighbor, so their distractors are strictly EASIER than training's tau≥0.5 distractors — an easier-than-training eval inflates the metric and is invalid as a frozen held-out spec. Leakage-clean but difficulty-biased; my earlier concurrence saw below_tau and did not raise it. Discarding is right.

**Replacement (Track A): provision-disjoint train/eval split of the existing 1909 R records** — hold out a provision subset entirely from R training, train on rest-of-R + all T + all P, eval on held-out provisions' records with frozen tau=0.5 REAL confusable distractors (no relaxation). Doesn't reopen P2a. Track A sizing the held-out fraction.

**Content view (favorable in principle; meets my standard IF two conditions hold — full verification when routed):**
- This is strictly stronger: genuine provision-generalization (unseen provisions, not just unseen framings) with real-spec tau=0.5 distractors — the exact fix for the killed framing-c difficulty bias.
- **CONDITION 1 (the one that matters most) — cluster-disjoint holdout, not random provisions.** If gold is systematically the held-out (unseen) provision while its distractors are trained (seen) provisions, a model can exploit a FAMILIARITY shortcut ("pick the text I've seen least") and score high without discriminating — a spurious inflation in the opposite direction from framing-c's. Fix: hold out provisions in whole tau=0.5 confusable NEIGHBORHOODS so each eval record's gold AND distractors are all unseen. Track A's "hold out a provision subset" must be cluster-aware. I will verify per-eval-record that gold and distractors are on the same side of the split.
- **CONDITION 2 — candidate-level + T/P leakage scope.** Verify held-out provisions never appear AS a candidate (gold or distractor) in any TRAINED R record. And T/P exposure: T (teacher full-IR) covers Indian provisions, so a held-out R provision may have been taught via T/P as a non-retrieval task — either hold the eval provisions out of T/P coverage too, or scope the label honestly to "held out from all retrieval-framing training" acknowledging T/P may have shown the provision text in a non-retrieval task. I'll check held-out ∩ (T provisions ∪ P provisions) when routed.
- Metric 4 stays directional/non-gating under the Wilson-lower-bound rule — fine; thin held-out N is acceptable for a non-gating tier signal.
- **Label (Track A's proposed shift, acceptable with Condition-2 scoping):** "provision-generalization within trained framings b/c; framing-a fact-snippet-id NOT separately evaluable (all 153 illustrated provisions needed for any framing-a record); framing-b/c real-spec distractors, tau=0.5" — plus the honest T/P-exposure scope from Condition 2.

Full-IR N≈400 headline build with d0 is GO/running, unaffected. I re-sign the amended §3/§4 when the lead routes it WITH the sizing + the actual split, after I verify Conditions 1-2 against the built split.

## R-eval Conditions 1-2 resolved (lead) + negatives N≈250 composition ownership — 2026-09-16 10:36 BST

Lead adopted Condition 1 (CLUSTER-DISJOINT wins over Track A's target-only disjoint, which had exactly the familiarity confound I named — honest thinner N beats confounded larger N for a non-gating metric). framing-b likely drops (914/915 trained → no unseen cross-act distractors). Condition 2 resolved to the honest-label option: eval provisions NOT held out of the thin 212 T; label scoped "held out from all retrieval-framing training; T/P may have shown the text non-retrievally." Both match my asks. R-eval → framing-c cluster-disjoint, small N, directional.

**New work item I own: near-miss NEGATIVES composition, N≈250** (for the metric-3 ≤0.02 false-prove ceiling and metric-3b ≥0.90 abstention ceiling — Track A's Wilson math needs N≥200 to certify). My content standard for the composition, on record before I build/certify it:
- Two near-miss classes, both present and roughly balanced (neither dominates): (i) **element-short-by-one** — a proof missing exactly one required element (tests fabricating the missing element → must abstain/not-prove); (ii) **confusable-contract** — right structure, wrong contract_id (tests mis-certifying under the wrong contract → must not certify). These exercise the two distinct false-prove/abstention failure modes.
- Every negative is a GENUINE near-miss: checker-verified NOT-provable AND my full read confirms it's plausible-but-wrong (not a trivial/obvious reject) — the addendum discipline (76f93ad): trivial negatives don't test the ceiling.
- Negatives leakage-disjoint on the same carve as the gold.
- N≈250 sizing check: at N=250 with 0-4 false-proves the Wilson 95% upper bound stays <0.02 (certifies the ceiling); ≥5 fails — so the ceiling is real, not decorative.

**Pending verifications when the lead routes the amended §3/§4 + the ACTUAL built artifacts:**
1. Full-IR N≈400 gold (d0 running): my full clean content read (checker ∧ my read) + near-miss +/- split certification + val∩test overlap (={47821}).
2. R-eval cluster-disjoint split: Condition 1 per-record (gold + all distractors same-side/all-unseen); Condition 2 (held-out ∩ (T∪P) computed, label factually complete).
3. Negatives N≈250: composition across the two classes per my standard above.
I re-sign only after these verify against the built artifacts. No new objections.

## P2b prereg AMENDED SPEC blob `34802e477ced` @ main `ab444bc` — content RE-SIGN (spec-level; artifact verifications still pending) — 2026-09-16 10:52 BST

The R-eval cluster-disjoint + negatives amendment. I verified the blob myself (`rev-parse ab444bc:...prereg` = `34802e477ced59e7ebf165ccd22ae76af4db5fd1`) and diffed vs my last-signed spec blob `3b223133a5f8` — not on the lead's word.

**The diff faithfully captures every one of my conditions:**
- **§3 R-eval — cluster-disjoint component holdout:** "hold out whole [tau=0.5] components so each held-out gold's tau≥0.5 neighbors — hence its top-4 framing-c distractors — are all in-component and all held-out (Condition 1: gold AND all distractors same-side/unseen, closing the familiarity shortcut; Condition 2a: no trained record uses a held-out provision as a candidate — both satisfied by construction)." ✓ Exactly my Condition 1 + 2a.
- **§3 honest label — Condition 2b:** "gold + all distractors unseen AS RETRIEVAL TASKS; T/P may have shown a held-out provision's text non-retrievally — valid as *unseen-as-retrieval*, not *unseen-entirely*. Framing-a … NOT separately evaluable …; framing-b dropped. Metric 4 directional/non-primary-gate." ✓ Exactly my Condition 2b T/P scope.
- **§3 negatives N≈250 — my composition standard:** balanced element-short-by-one + plausible-but-wrong confusable-contract, every one a genuine near-miss (checker not-provable ∧ my read plausible-but-wrong, never gibberish), leakage-disjoint on the gold's carve, Track B certifies each label. ✓ Matches 85048dd.
- **§4 metric-4 directional/non-primary-gate** (counts only toward "exceeds", thin N acceptable because non-gating). ✓
- **Metrics/ceilings/gold BYTE-UNCHANGED** (verified in the signed blob): 1 ≥0.98, 3 ≤0.02 HARD, 3b ≥0.90 HARD, 4 ≥0.493 Wilson-lower-bound, noise-band excludes-zero, full-IR gold = checker ∧ full read. §1/§2/§5–§8 untouched; the only §4 change is the additive metric-4 directional clarification I concurred with.

**CONTENT RE-SIGN of the amended SPEC blob `34802e477ced`.** This attests the spec faithfully captures my conditions and freezes the spec for d0 to build against.

**EXPLICIT SCOPE — this spec sign is NOT the artifact verification.** Per the lead's framing and §3's "dual-signed as its own step before use," my THREE verifications remain PENDING at the separate **eval-SET dual-sign** against d0's ACTUAL BUILT artifacts (§9's Track-B re-sign line bundles them; I separate them here so no one reads this spec sign as artifact-verified):
1. Full-IR ~400 gold: my full clean content read (checker ∧ my read) + near-miss ± split certification + val∩test overlap ={47821}.
2. R cluster-disjoint split: Condition 1 per-record (gold + all distractors same-component/all-unseen) + Condition 2 (held-out ∩ (T∪P) computed, label factually complete) — I re-run Track A's component math against the built records.
3. Negatives N≈250: composition across the two near-miss classes per my standard.
The sizing numbers (164 provisions / 20 small ≥5-member components / giant 361-comp excluded / framing-c N≈101, 53 IPC + 48 BNS / R-train ~1552) are Track A's component computation over the tau=0.5 graph — I verify these against the built split at the eval-set sign, not now.

**One flag (not blocking my content sign):** Track A's shape-sign commit `3838e59` is NOT resolvable in this repo (unknown object, absent from `--all`) — the amendment commit ab444bc IS present, but TA's separate sign commit isn't fetched here. I sign content independently of TA's shape sign; the lead must confirm Track A's sign binds the SAME blob `34802e477ced` before freezing. No `create-pod` until frozen + eval-SET built/read-certified/hashed/dual-signed + both preflights signed.

## SPEC FROZEN @ main 238b341 (pin 34802e477ced) + R-split N selection rule (164 vs 101) — 2026-09-16 11:04 BST

Lead froze the spec; flag resolved — TA's 3838e59 cites blob 34802e477ced, my 5cc22e6 binds the same, main HEAD:prereg = 34802e477ced59e7...; sign-commits on separate review branches (not cross-fetched) is expected, the blob pin is the binding. My three artifact verifications stay OPEN at the eval-SET dual-sign.

**Content principle for the 164-vs-101 R-split reconciliation (d0 ↔ Track A), stated before d0 finalizes so the selection rule is set right:** N is whatever SURVIVES Condition 1 per-record — NOT a target to hit. A framing-c eval gold is admissible ONLY if ALL of its tau≥0.5 distractors are held-out/in-component (gold + every distractor unseen). If d0's 164 golds include any whose distractor set reaches into the TRAINED provisions (neighbors spanning the split boundary), those records VIOLATE Condition 1 and must be DROPPED, not kept to reach a higher N. The honest N (101, 164, or whatever survives) is exactly the count of golds with a fully-held-out distractor set. Same principle that killed target-only-disjoint and below-tau framing-c: an honest thinner N beats a larger N that reintroduces the familiarity shortcut. Metric 4 is directional/non-gating, so N is free to fall where honesty puts it. I verify this per-record against the built artifact — I do not accept a selection rule that admits a gold with any seen distractor.

## P2b prereg AMENDED SPEC blob `0e36e18a522e` @ main `5411174` — content RE-SIGN (164-survivor reconciliation; spec-level) — 2026-09-16 11:18 BST

R-split reconciliation: applying my governing principle (6035790 — N = honest survivor count) yields **164, not 101**. I verified the blob (`rev-parse 5411174:...prereg` = `0e36e18a522ed4ffb54bf91fc8515f1910bad9bd`) and diffed vs my last-signed `34802e477ced`.

**The reconciliation is correct and BETTER than my earlier drop-to-101 instinct.** Track A's 101 was an over-strict ≥4-distractor subset; it was NOT Condition 1. All 164 held-out-component provisions pass Condition 1 — d0's union-find (edge = source→its actual distractor_set) makes every held-out provision's real distractors same-component/held-out by construction. The 63-gap are low-degree in-component nodes with 1-3 held-out distractors (candidate counts {5-cand:101, 4:33, 3:21, 2:9} = 164 ✓; ≤3-candidate = 30 ✓) — genuinely fewer tau≥0.5 neighbors, NOT below-tau padding. Retaining all 164 keeps every honest signal; **candidate-count-STRATIFIED recall (2/3/4/5-way) + the 5-way stratum N=101 as the hardest-uniform cut** prevents easy 2/3-way items (2-way ≈ 50% baseline) inflating the aggregate. Honest handling — stronger than dropping signal.

**Widened R-train removal scope (Condition 2a) CLOSES a real leakage path I welcome:** remove (i) all a/b/c records SOURCED from any of the 164, PLUS (ii) all R records of ANY framing whose candidate_section_ids include any of the 164 AS A DISTRACTOR. framing-a (±2 doc-order) and framing-b (cross-act) distractors are NOT tau-based, so a held-out provision could appear as an a/b distractor of a non-held-out gold — the model would then have seen its text. Removing those records (NOT patching distractors, which would deviate from the frozen spec) is exactly right. R-train = 1909 − (cross-framing scan), recomputed post-scan (< ~1552).

**Metrics/ceilings/gold BYTE-UNCHANGED** — diff touches only §3 R-eval, the §4 metric-4 note (updated to "all 164, stratified, 5-way N=101 hardest cut"), and §9 sigs. Metric table (1≥0.98, 3≤0.02, 3b≥0.90, 4≥0.493 Wilson-lb), noise-band, full-IR gold=checker∧read, §5/§6/§7/§8 untouched.

**CONTENT RE-SIGN of amended SPEC blob `0e36e18a522e`.** Faithful-capture only. My THREE artifact verifications stay OPEN at the separate eval-SET dual-sign (§9 now makes this an explicit separate gate) — for R I confirm per-record that EVERY admitted gold's distractors are ALL held-out AND sim≥0.5 (survivor count = 164 minus any below-tau defect, expected 0; a below-tau distractor is the exact defect that killed the empty-cell framing-c, so this is the crux check), plus the cross-framing removal scan actually applied to R-train, plus held-out∩(T∪P).

**One content-interpretation note for the eval-set sign (non-blocking, metric 4 is non-gating):** the "≥0.493 beat-1.13B-prior" bar is apples-to-apples only against the **comparable candidate structure** — the prior was measured at a fixed candidate count, so the 5-way N=101 stratum is the comparable number; the mixed-difficulty aggregate-164 is a directional signal, not directly comparable to 0.493. Read the bar against the 5-way stratum at the eval-set sign. No `create-pod` until the eval-SET sign lands + both preflights signed.

## Negatives class-(ii) confusable-contract RULING (I own composition, 85048dd) — 2026-09-16 11:42 BST

trackB (authoring) holds class-(ii) generation for my ruling. Verified against the registry (lean/Score.lean: 14 contracts + element #guards) and the wire schema (src/nyaya_ir/validators.py) — not from memory. The genuine-near-miss bar = share a core element, differ on ONE discriminating element, so a re-tag X→Y is plausible-but-wrong (checker fails on the discriminator), NOT a multi-element trivial reject.

**(a) Confusable-pair groups:**
- **CONFIRM** — all share a core, differ on one discriminator:
  - ipc405_misappropriation ↔ use_or_disposal ↔ wilfully_suffers (shared: entrustment + dishonesty; discriminator: conduct limb A/B/C).
  - ipc415_property ↔ ipc415_damaging_act (shared: deception/inducement; discriminator: consequence — property vs damage).
  - ipc182_misdirected_act ↔ ipc182_abuse_of_power (shared: false info to public servant; discriminator: intended-consequence limb; both omit = MissingIntendedConsequence).
  - bns46_instigation ↔ conspiracy ↔ intentional_aid (shared: abetment; discriminator: route conduct).
  - bns47 ↔ bns46_* (shared: abetment predicate; discriminator: bns47's extraterritorial/location element). CONDITION: trackB's read must confirm each re-tag fails specifically on the abroad/location fact, NOT on abetment (which is shared) — else it's not testing the discriminator.
- **OPTIONAL enrichment:** ipc416 (cheating by personation) shares the deception core with ipc415_* — add it to the IPC-cheating cluster if more IPC cheating negatives are wanted (not required).
- **bns69 ↔ bns85: DO NOT BLESS — not confusable. trackB's instinct is correct.** bns69 core = deception/inducement (allStatedDeceitfulMeans / missingInducement); bns85 core = relationship + cruelty/danger (bothStatedDangerLimb / missingRelationship). NO shared discriminating element; a re-tag fails on relationship AND danger-limb (multiple, entirely-absent elements) = trivial reject, not a near-miss.
  - **bns85:** has NO genuine confusable registry sibling → gets NO class-(ii) negative. Cover it in class (i) (element-short-by-one via missingRelationship / danger-limb omit). Honest and sufficient.
  - **bns69:** its structural sibling is **ipc415_property** — BOTH carry the `missingInducement` predicate (shared inducement-by-deception core; discriminator = consequence: sexual intercourse vs delivery of property). Admit bns69 ↔ ipc415_property ONLY if trackB's read confirms each re-tag is genuinely plausible-but-wrong at the element level and not a subject-matter giveaway (sexual vs property topic could make a model trivially reject); if it doesn't survive the read, DROP it and cover bns69 in class (i).
  - **Standard (binding):** uniform per-contract coverage in class (ii) is NOT the bar — genuine near-miss is. A contract without a genuinely-confusable sibling is represented in class (i), never forced into a fake class-(ii) pair to fill a quota.

**(b) abstain_reason — REJECT "wrong_contract" (OFF-SCHEMA, would fail wire validation).** validators.py:146 requires, for kind=law_abstain, `abstain_reason ∈ {missing_fact, missing_source, conflicting_authority, unsupported_rule}` — "wrong_contract" is not in the enum and the record would fail validate_record (independent of the Lean checker). Use **`missing_fact`** as the default: the confusable sibling Y's discriminating element is not established by the facts, which maps exactly to the checker's element-fact-fit failure. Where the read shows Y's whole actus reus is inapposite (not a single missing fact), **`unsupported_rule`** is the in-schema alternative. Record the "confusable_contract" construction nuance in a PROVENANCE field (e.g. negative_class), NOT in abstain_reason.

Class (i) plan (D-tier 310 checker-verified omit slices, REG-verified, single-element-per-fact-guarded, curated ~9/id, ≤1 omit/source) already meets my standard — no change. trackB's lake-build + sha==9bff8f30 confirm before trusting any REG verdict is correct discipline.

## Eval-set NEGATIVES read-certify — 225 CERTIFIED, ACCEPT at 225 (no forced push to 250) — 2026-09-16 12:14 BST

trackB's built negatives (branch trackB/p2b-near-miss-negatives @ 03ac265, records sha 2d7a25d2…). I verified the BUILT artifact independently (parsed every record + provenance), not trackB's report. This is verification #3 of my three eval-set checks (negatives composition); full-IR gold + R cluster-disjoint verifications still pending their builds.

**Class-(ii) confusable-contract — 100/100 GENUINE near-miss (my core standard).** Parsed each record's REG raw_stdout (Score.lean:407 format: id\tverdict\tdenied\tungrounded\tomitted). Per-record confirmed: verdict=flagged; the SHARED element is SATISFIED under target Y (absent from both `omitted` and `ungrounded`) in 100/100; the DISCRIMINATING element is in `omitted` in 100/100; genuine_partial_miss_confirmed=true. NOT a trivial full-mismatch on any record — the shared element grounds, only the discriminator misses. Groups match my ruling: ipc405 30 / ipc415 20 / ipc182 20 / bns46 30, balanced 10/source-contract. abstain_reason=missing_fact throughout (my validators.py:146 ruling honored; confusable nuance in provenance negative_class, not abstain_reason).

**Class-(i) element-short-by-one — 125, curated correctly.** 14 registry ids covered (9× for 13 ids + 8 ipc416); ≤1 omit slice/scenario (0 scenarios with >1 — no near-dup negatives); checker_expected=checker_actual=flagged, checker_pass=true; sourced from P2a D-tier batch-03 (NOT in SFT T/P/R = leak-disjoint by construction). **bns69 (9) and bns85 (9) ARE represented here** — exactly my ruling (contracts with no confusable sibling covered in class (i), not forced into a fake class-(ii) pair).

**Full 225 record-level:** all kind=law_abstain, all abstain=True, all abstain_reason=missing_fact (in-enum), 0 wire-schema failures, 225 unique record_ids (0 dups), records sha matches manifest, leak-check clean (id collisions vs T/P/R = [], vs class (i) = [], max narrative-Jaccard 0.0938 < flag 0.30). Composition balanced 125 class-i / 100 class-ii (neither dominates).

**Dropped pairs verified consistent with MY OWN rule.** bns47↔bns46_* and bns69↔ipc415_property were dropped because live-check found ZERO shared exact element strings → a re-tag flags ALL target elements (trivial full-mismatch), failing the shared-element condition I placed on each. That is exactly the condition-failure I specified; dropping (not forcing) is correct. The admitted 100 all pass the same live check by construction, so the drops discarded only non-near-misses.

**RULING: (a) ACCEPT the negatives set at N=225. Do NOT push to 250.** 225 clears Track A's Wilson N≥200 floor for both safety ceilings (false-prove ≤0.02: ub≈0.017 at N=225/0 events; abstention ≥0.90: lb≈0.921 at 0.95/225) — statistically sufficient. Every one of the 225 is a genuine, checker-verified near-miss. Closing the 25-gap needs fresh-authored scenarios engineered to share an element string across the bns47/bns69 families — which risks manufacturing artificial near-misses purely to hit a round number. That is precisely the "no forced fake pairs to fill a quota" rule I own: 225 genuine > 250 with 25 engineered. The 25-record shortfall is documented honestly in the manifest shortfall_note. **Negatives portion CERTIFIED.** Final eval-set dual-sign lands when full-IR gold + R cluster-disjoint are also built and certified.

## Eval-set cert #1 — full-IR gold (405) READ — CONDITIONAL, NOT clean-certified as-is — 2026-09-16 11:16 BST

Branch assistant/trackA/p2b-eval-p-r-build @ 80873d2, gold.jsonl 405. I ran the mechanical layer myself + a full semantic read (9 parallel readers, 45/batch, sibling-leak + element-sufficiency + internal-consistency + legal-coherence rubric with a per-record sibling-discriminator map) + adjudicated EVERY flag myself. Full adjudication: docs/reviews/spikes/trackb-p2a-tread/cert1-fullir-defects.md.

**Mechanical layer — PASS.** 405 unique law_apply; all accepted=True, score sha 9bff8f30, nigamana_status=proved, unmapped_established_element_ids=0, shape/target-verdict-consistency all True; 14 contracts (18 bns69 … 37 ipc415_damaging); gold ∩ T-training by id = 0; zero casehold/47821 references; 6 cross-batch template dups excluded (jaccard 0.44–0.58). All 405 teacher_provenance_class=alias_unpinned (expected per the permanent alias switch; validity gated by checker + my read regardless).

**Semantic read — a real defect population the checker cannot catch (~3–4%).** My adjudication:
- **11 CONFIRMED genuine defects → drop/fix:** name-drift 0000 (Mehta/Mehra) + 0045 (Harrow/Halloway) [fixable, my own scan caught 0000 independently]; ipc405 use-vs-misappropriation SIBLING-LEAKS 0011 + 0013 (facts describe dishonest USE = the use_or_disposal sibling, not conversion — a model could ground the wrong sibling, the exact bad-test cert #1 exists to catch); wilfully_suffers 0032 (entrustment in Tamsin but suffering by Jorin — incoherent actor); ipc416 0017 + ipc415 0012 (property-flow reversed); ipc182_misdirected 0039 (unit vacant yet "eviction of the tenants"); ipc416 0038 (false kinship ≠ personation, leaks 415); bns47 0043 (no named underlying offence) + bns85 0037 (perpetrator's relative-of-husband status unestablished).
- **NEEDS Track A contract-semantics ruling (systematic, not unilaterally dropped):** ipc415_property DELIVERY cluster 0006/0009/0033/0043/0044 (5) — each F_el2 shows the deceived buyer RECEIVING/acquiring ownership, no victim parting-with-property; checker passed all, but does the "delivery of property" element accept receipt or require the victim to deliver? bns46_conspiracy object-offence labels 0010/0013/0018 (loose object-offence characterization; conspiracy structure holds — minor).
- **CLEARED as reader over-flag — Track B rules CLEAN:** ipc182_abuse_of_power 0015 + 0032 — the false report about vacant 4B is the deliberate PRETEXT; F_el1/F_el2 establish the harass-4A purpose. Coherent abuse-of-power, keep.

**Leak metric gap (flag, not a hard fail):** t_bns46_conspiracy_4aed7df7_0006 — my independent entity-masked masked-shingle-jaccard(n=3) = 0.308 vs a T-training bns46_conspiracy record, OVER the 0.30 template-dup threshold the WITHIN-batch guard uses, but the cross-batch T-eval leak-check applied only the 0.40 content-word + character-signature conjunction, never the 0.30 masked-shingle metric cross-batch. Recommend d0 re-run the cross-batch check with the masked-shingle metric; drop 0006 if confirmed.

**CERT #1 RULING: NOT clean-certified at 405.** 11 confirmed defects drop/fix + up to 8 pending (5 ipc415 + 3 bns46) on Track A's semantics ruling + possibly 1 leak (0006). Worst case 405 − 20 ≈ 385 clean; best case ≈ 394 — either way ABOVE the N≈400 fallback floor (200) and near target, so the gate stays viable. I re-certify the corrected gold once trackA/d0 drop/fix the confirmed set, rule the two adjudication clusters, and re-run the leak metric. This is 1 of my 3 eval-set certs; negatives certified (ce70b9c), R cluster-disjoint (c220d6a) pending my cert #2. No create-pod.

## Eval-set cert #2 — R cluster-disjoint split (164) — CONDITIONAL PASS + 24% unanswerable-by-text finding — 2026-09-16 11:34 BST

Branch assistant/trackA/p2b-eval-p-r-build @ c220d6a, retrieval_only_eval/batch-02 (164 gold, R-train 1909→1156). I verified every condition per-record against the built artifact (provenance + P2a similarity_graph + r_train_kept), not the build's defects[]=[] report.

**Cluster-disjoint construction — VERIFIED SOUND (Conditions 1 & 2 all clean):**
- Cond 1a — every gold's distractor_set ⊆ the 164 held-out provisions: **0 failures**.
- Cond 1b — every distractor sim ≥ 0.5 (the no-below-tau check; below-tau distractors were the exact defect that killed the empty-cell framing-c): **0 failures, min sim 0.5003 / max 1.0** across all 554 admitted distractors.
- Cond 2a — no kept-train R record uses a held-out provision as a candidate (source or distractor), verified by unwrapping {record,wire} and scanning the IR + passages: **0 touches** across all 1,156 kept-train records.
- Cond 2b — held-out ∩ (T ∪ P): **exactly 1** (BNS/Section 47, cited in T-training's bns47 full-IR task); P (US CaseHOLD) cites no Indian provisions → 0. This is precisely the case the frozen honest label ("unseen-as-retrieval, not unseen-entirely; T/P may have shown the text non-retrievally") was written to cover — label is factually complete.
- candidate_count == len(distractors)+1 for all 164; strata {5:101,4:33,3:21,2:9}=164 match the frozen spec.

**MATERIAL VALIDITY FINDING (not in the build's defects[]): 40/164 (24%) items are UNANSWERABLE-BY-TEXT.** 40 records have a distractor with TF-IDF operative-segment similarity ≥ 0.999 — textually (near-)identical to the gold. These are BNS↔IPC **re-enactment twins** (BNS carried IPC text over verbatim: e.g. BNS/100 ≡ IPC/299 culpable homicide; 20 are pure BNS-gold/IPC-identical-twin, none are 2-way: candidate_count 5:26/4:10/3:4). For a framing-c "which provision is this operative text drawn from" item on a HELD-OUT (unseen) provision, an identical-text distractor makes the gold and that distractor interchangeable — the model cannot distinguish them on text, and being held-out it cannot know the dataset label convention. These 40 measure luck, not retrieval-generalization; a raw 164-recall is depressed/corrupted by a 24% unanswerable subset. (The 110 distractors in the 0.90–0.999 band are fine — very hard but genuinely discriminable, the intended difficulty.)

**CERT #2 RULING: CONDITIONAL PASS.** The cluster-disjoint construction is sound and I certify Conditions 1/2. But metric 4's number is only interpretable on the **124 textually-discriminable items** — exclude the 40 sim≥0.999-twin items (or remove/replace the identical distractor) and report the 40 separately; do NOT report raw-164 recall as "retrieval-generalization." Metric 4 is directional/non-primary-gate so this does not block the gate, but the honest number must exclude the unanswerable subset. Track A owns the metric-validity call; I flag the content/validity finding. Cert #2 of 3 done (negatives ce70b9c; cert #1 conditional ecbf7f4). No create-pod.

## Eval-set RE-CERT #1 — corrected full-IR gold (377) — CERTIFIED (clean) — 2026-09-16 11:48 BST

Branch assistant/trackA/p2b-eval-p-r-build @ 510e892, gold.jsonl 377 (405→377, −28). Verified the corrected artifact independently.

**All my findings + Track A's rulings applied correctly:**
- 377 unique law_apply; **0 records not in the original 405** → the 377 are a strict SUBSET of the cert-#1 read-covered set, so the full semantic read already certifies them (no re-read needed; no top-up/new unread records).
- All 16 semantic drops absent (11 confirmed incl. name-drifts 0000/0045 + 5 ipc415-delivery cluster); 0006 leak dropped; all 12 masked-shingle leak drops removed. 28 = 11 + 5 + 12. ✓
- 3 bns46-conspiracy (b) keeps present (0010/0013/0018 — Track A: object-offence name is not a graded element). ✓
- My 2 cleared over-flags (ipc182_abuse 0015/0032) correctly KEPT. ✓
- Track A rulings converge with d0's contract reads (Track A 4d6441b): (a) ipc415 Limb-A requires the deceived party to part with property — 5 dropped, forward-only finding in known_defects.md, IPC415.lean NOT edited (pin 9bff8f30 intact — correct, no checker change); (c) 415-not-416 → 0038 dropped.

**Mechanical layer — PASS on 377:** all 377 accepted=True, nigamana_status=proved, score sha 9bff8f30, unmapped_established_element_ids=0; 14 contracts (min 17 bns69 / max 36).

**Cross-batch T-train leak — RESOLVED.** My independent masked-shingle(n=3) sweep of the 377 vs T-training: **0 records ≥ 0.30** (max 0.285). My original 0006 flag + d0's 12-record extension fully cleared the train→eval template-leak direction.

**One LOW-PRIORITY flag (non-blocking): 13 within-gold pairs at my approximate masked-shingle ≥0.30** (e.g. ipc405_use_or_disposal 0014↔0036 = 0.375, bns47 0003↔0034 = 0.344) — template redundancy BETWEEN two gold records, NOT train/eval leakage (both are gold, no train overlap; doesn't corrupt the held-out property). My entity-masking approximates the guard's exact `_masked_text`, so these may sit below threshold under exact masking. Recommend d0 confirm with the exact guard masking and dedup one-of-each-pair if real (~≤13 records → ~364); does NOT block re-cert — no leakage, no bad-test, each is individually read-clean and checker-proved.

**RE-CERT #1 RULING: CERTIFIED (clean).** The corrected 377 full-IR gold is content-certified: read-covered, checker-proved, T-train-leak-free. Only open item is the optional within-gold dedup (d0's call). **Count: 377 is content-sufficient — NO top-up needed** (above the Wilson floor 200, 94% of the ≈400 target, every record read-clean + proved + leak-free). If the lead/Track A want the count nearer 400 for power margin, I support it ONLY if the fresh records get the SAME full read + cert cycle (no shortcut); from a content standpoint it isn't required.

**R twin finding (cert #2): report-only route (Track A ruling A) is exactly my recommendation** — metric-4 recount on the 124 discriminable items, R set unchanged/certified. I will spot-confirm the 40 twin-ids when studio commits the recount. Cert status: negatives certified (ce70b9c), re-cert #1 CERTIFIED (this), cert #2 certified-with-report-only-metric-handling (9e92e31). No create-pod until eval-set assembled/hashed/dual-signed + preflights.

## Eval-SET ASSEMBLY — CONTENT-SIGN — 2026-09-16 12:14 BST

Branch p2b-eval-set-assembly @ 4ba7bef, manifest research/gates/P2b/eval_set/eval_set_manifest.json, TOP_HASH `3b6313085f5bfd33f384f7e324c68ceefcdb10d5b6ee1f5eb7e6936452e7325f`. Ran all four (a)-(d) checks myself against the assembled branch, not the lead's report.

**(a) Component shas == certified shas — PASS.** Re-hashed all 5 artifacts as committed on 4ba7bef:
- full_ir_gold (377): `2c8c3dd1...5650879` == my re-cert#1 f6481d0 sha.
- negatives (225): `2d7a25d2...196f003` == my ce70b9c sha.
- r_eval (164): `3916fbc4...28606e5` == my cert#2 9e92e31 sha.
- r_train (1156): `d250910b...5197e4ce` == my cert#2 9e92e31 sha.
- twin_exclusion (40): `b4f98e91...df479d` == my dual-confirmed sha.
All five byte-identical to what I certified — no drift.

**(b) Re-hash + id-set diff — PASS, no record added/removed/edited.** Compared id-sets between the assembled artifacts and my own certified in-memory sets: full_ir_gold 377/377 exact match; negatives 225/225 exact match; r_train 1156/1156 exact match. r_eval: the manifest's `eval_target.jsonl` keys records by top-level `id` while my cert#2 verification used `provenance.jsonl`'s `record_id` — different files, same 164 records; cross-checked the actual id strings (not just counts) and all 164 match exactly, confirming this was a file/key-naming difference in my diff tooling, not a content discrepancy.

**(c) Metric-4 exclusion wires from the pinned twin list — PASS.** Landed `twin_unanswerable_ids.json` on 4ba7bef diffed against my own independently-generated 40-record list (docs/reviews/spikes/trackb-p2a-tread/twin-unanswerable-ids-trackb.json): **record_id sets are IDENTICAL, 0 diff either direction.** Confirms the earlier byte-identical reconciliation (Track A's 40 == mine) survived the merge into the assembly unchanged.

**(d) top_hash reproduces — PASS.** Recomputed sha256 over sorted `"name\tsha256\tcount"` lines for the 5 components independently: `3b6313085f5bfd33f384f7e324c68ceefcdb10d5b6ee1f5eb7e6936452e7325f` — exact match to the manifest's pinned top_hash.

**CONTENT-SIGN: the P2b eval-SET (gold 377 + negatives 225 + R 164/1156 + twin-exclusion 40, TOP_HASH 3b631308…) is CONTENT-SIGNED.** Every component is byte-identical to what I content-certified across cert #1 (re-cert f6481d0), cert #2 (9e92e31), and negatives (ce70b9c); no record added, removed, or edited during assembly; the metric-4 twin exclusion is correctly wired from the dual-confirmed 40-id pinned list; the manifest's integrity hash is independently reproducible. Freeze on my sign + Track A's shape-sign. No `create-pod` until dual-sign lands + both P0 preflights are signed.

## P2b eval-SET FROZEN + merged to main — VERIFIED — 2026-09-16 12:31 BST

Relay confirmed working after the earlier delivery-failure notice. Verified the freeze myself against git, not the lead's report: main @ `06e808e` ("Merge p2b-eval-set-assembly: DUAL-SIGNED P2b eval-SET (top_hash 3b631308)"), preceded by `c8e9c54` ("P2b eval-SET FROZEN: dual-signed (TA shape 02ae005 + TB content d838d97), top_hash algo documented"). Re-read the merged manifest from main: top_hash `3b6313085f5bfd33f384f7e324c68ceefcdb10d5b6ee1f5eb7e6936452e7325f` and counts {full_ir_gold:377, negatives:225, r_eval:164, r_train:1156, twin_exclusion:40} — EXACT match to what I independently verified and content-signed. Track A's sign commit `02ae005` isn't resolvable in this local checkout (same as the earlier `3838e59` pattern — sign commits live on review branches, not cross-fetched); the binding is the top_hash match, confirmed.

**P2b eval-SET milestone CLOSED.** Next/last pre-pod gate is the two P0 preflight files (1.13B + 3-4B base) — job spec + ShaEnforcingChecker wiring + ≤30-min measurement plan, Track A verifies. Not my gating axis unless a preflight touches eval-set content; nothing queued on me right now. Still no `create-pod` until both preflights signed.

## 3-4B base family FINAL: Qwen2.5-3B-Instruct — content concur f31d7c97 — 2026-09-16 12:41 BST

Operator delegated the 3-4B family pick to lead+reviewers (decide-together, prereg §5 "family fixed at preflight"). My content-capability read: no a priori schema-malformation showstopper on Qwen2.5-3B-Instruct / Phi-3.5-mini / Llama-3.2-3B (no hands-on eval data for any against nyaya_ir); leaned toward Qwen2.5-3B (Apache-2.0, clean for the public-HF-repo checkpoint house rule; Qwen2.5 family instruction-tuned toward structured output — soft positive prior, teacher-lineage argument is a tiebreaker not load-bearing) over Llama-3.2-3B (license complicates public-repo publishing) and Phi-3.5-mini (known off-distribution brittleness risk). CONDITIONED my concur on the P1 "IR emission parse rate (zero-shot + 5-shot-from-goldens)" measure actually running and being reviewed at preflight, not assumed.

**Condition adopted, not just noted:** lead added `ir_emission_parse_rate_gate` to BOTH P0 preflight files (1.13B + Qwen2.5-3B) — parse rate zero-shot AND 5-shot, on each base's un-SFT'd model, reported in the P0 file, reviewed at preflight; a badly-degenerate result is explicit written grounds to reconsider regardless of this concurrence. studio's preflight driver computes it (not a prose promise). Measured on BOTH bases (not assumed via teacher-lineage), which is exactly the honesty check I asked for.

**Family FINAL: Qwen2.5-3B-Instruct** (my f31d7c97 + Track A 3137ada + lead, operator-delegated). I review the parse-rate numbers at preflight before the base is locked for the full run — this is a real open item on me (not closed by this concurrence). Nothing else queued now.

## Few-shot contamination guard — CONTENT-SIGN disjointness — 2026-09-16 12:53 BST

Branch assistant/studio/p2b-cli @ 6ee876b, `select_few_shot_examples` in p2b_qwen_entrypoint.py. Verified the actual code, not the report.

**Structural enforcement — CONFIRMED, matches my guard exactly.** `eval_ids = frozenset(row["record"]["id"] for row in eval_set.full_ir_gold)`; spares = T-tier rows sorted by id (deterministic, never dict/set order) with `row["id"] not in eval_ids`; `if len(spares) < n: raise FewShotSelectionFailed(...)` — NEVER pads, NEVER silently reuses. `run_p2b_preflight` requires `few_shot_examples` as a parameter (not an optional/defaultable seam), so every caller including the CLI gets the guard. This is exactly my condition #2 from the harness-build flag: fail-loud, disjoint, no eval-SET row ever enters the few-shot prefix.

**Is `full_ir_gold` the right/complete exclusion set (vs also negatives/R)? YES, and it's structurally the ONLY relevant one.** Two independent confirmations:
1. Traced `few_shot_examples`' only use site: `run_p2b_preflight`'s docstring states plainly — "the 5 records used for the un-SFT'd base_predictor's IR-emission-parse-rate capability floor check (5-shot-from-goldens, `render_prompt_5shot`)." It is never injected before negatives or R-eval scoring — those are scored zero-shot through the checker, no in-context prefix at all. So there is no code path where a T-tier few-shot example could contaminate a negatives or R-eval answer.
2. Empirically confirmed anyway (defense-in-depth, since I don't rely on the docstring alone): re-verified from my own certified artifacts — T-tier ids (210) ∩ negatives ids (225) = **0**; T-tier ids ∩ R-eval ids (164) = **0**. Even if a future change widened where few_shot_examples gets used, there is currently no id-level overlap to exploit.

**CONTENT-SIGN: `select_few_shot_examples`'s disjointness guard is CERTIFIED** — enforces exactly my meta-overfitting condition (fail-loud, deterministic, `full_ir_gold`-disjoint by construction and by explicit runtime check), and `full_ir_gold` is the complete correct exclusion set given the guard's actual (and only) use site. No widening to negatives/R needed — not a real risk, would be dead code. Track A verifies the non-swallow (leak-check-before-scoring, sha-enforcement) in parallel. This closes my last flagged item before the pod.

## GPU portability + checkpoint/resume (assistant/studio/p2b-gpu-portability @ 745d99a) — adversarial content-sign — 2026-09-16 (see commit timestamp)

Off main b772691, 6 files +944/-14. Verified the actual diff + tests myself, not the report.

**(1) No silent resume failure — PASS.** `test_resume_continues_from_the_saved_step_not_from_zero`: genuine crash (STOP-file injected mid-run) then resume reaches `global_step==6` (target) from a `crashed_step>0` (real partial progress, not a no-op). `test_resumed_run_adapter_is_close_to_an_uninterrupted_run`: uninterrupted-reference vs crash+resume run, same seed/config/data, `torch.allclose(atol=1e-4)` on every LoRA param — genuine numeric assertion, not a docstring claim. Optimizer+RNG restored (`weights_only=False`, own trusted checkpoint files only); resume resumes at saved `pair_index` (the NEXT unprocessed pair, no off-by-one verified by trace).

**(2) Fail-closed hashing — PASS.** `compute_config_hash` covers ALL 7 `LoraSftConfig` fields (r/alpha/dropout/target_modules/learning_rate/num_epochs/seed — checked against the dataclass, no gap). `find_resumable_checkpoint` returns a state only when BOTH config-hash and dataset-hash match; any mismatch raises `CheckpointRefused` fail-closed — no silent-accept path. `pytest.raises` assertions confirm both mismatch directions.

**(3) Gate/parse-rate untouched — PASS.** `p2b_eval.py`/`p2b_preflight.py`: zero diff. Entrypoint diff only wires `checkpoint_config` + a pre-training `configure_for_device` call; `select_few_shot_examples`, eval-set loading, `ShaEnforcingChecker` path unchanged.

**(4) Durability — FLAG, non-blocking.** `rsync_checkpoint` calls `subprocess.run(["rsync",...])` with default `check=False`, never inspects the return code; the caller in `train_lora_sft` has no try/except around it. A genuine rsync failure (network/permission/bad dest) is COMPLETELY SILENT — no exception, no log. Best-effort (not blocking a step on network egress) is the right design choice, but silent best-effort means the house rule's "off-RunPod copy before delete" could go unmet for a whole run with zero visibility in pod logs. Recommend (cheap, non-blocking): log a warning with returncode+stderr on rsync failure.

**RULING: SIGN on dimensions 1-3 (clean); FLAG dimension 4 (durability visibility gap, recommend cheap logging fix, does not block dual-sign or the pod's current run).** Reported to lead per instruction, not Track A.

## Dim-4 fix (rsync-failure visibility) — scoped-delta confirm — MERGE OK — (timestamp above)

Verified 745d99a..13a90e5 myself (2 files, +81/-3 total). p2b_sft.py delta confined exactly to `rsync_checkpoint` + new `_default_rsync_runner`: OSError from the runner -> `logger.warning` + return; non-zero returncode -> `logger.warning` (returncode+stderr) + return; both paths `return` normally, never raise -- never blocks training, no longer silent. Grepped: no other function touched (save_checkpoint/load_checkpoint/find_resumable_checkpoint/train_lora_sft/hash functions/device-fit all absent from this diff) -- confirms the signed logic (dims 1-3) is untouched. tests/test_p2b_sft.py: 3 new tests, all real caplog-based assertions (nonzero-returncode warns with the right returncode+stderr text; runner-raising warns; success path asserts `not caplog.records` -- no false-positive warnings). Exactly closes my dim-4 flag.

**Scope confirmed. MERGE OK.** My dims-1-3 sign (46ef544) carries; this delta is additive-only on the flagged path.

## Launch+smoke-test procedure — adversarial content review (design, no code yet) — 2026-09-16 (see timestamp above)

Operator-routed review of the studio smoke-test flags + launch checklist for the relaxed 80GB/multi-GPU run (P2b preflight OOM'd on 24GB L4 + 48GB L40S; fp32 full-vocab logits for the ~33k-token tail ~20GB fwd + ~20GB bwd). No branch/commit exists yet for this design; reasoning from domain engineering + already-verified code (configure_for_device, checkpoint/rsync).

**Five findings, reported to lead only (per instruction):**
1. Longest-3-pairs is NOT a safe peak-memory upper bound: (a) confirm optimizer.step() actually runs in smoke (Adam state materializes lazily on first step, fwd/bwd-only undermeasures); (b) allocator fragmentation over many varying-length steps is structurally untestable by a 3-step smoke; (c) eval-time KV-cache generation peak (max_new_tokens=256) is a separate memory axis from train fwd/bwd — design only sorts TRAIN pairs longest-first, not eval items. Recommend --eval-longest-first too.
2. Multi-GPU mis-shard risk real: `configure_for_device` (already-merged code) calls `torch.cuda.get_device_properties(device_index=0)` — blind to GPUs beyond 0. Fine for one big card, blind to NCCL/sharding failure modes for true multi-GPU, which a 1-GPU smoke can't rehearse. Ask whether "multi-GPU OK" means one bigger card (low risk) or real N-GPU sharding (needs its own smoke path); recommend the single-bigger-card path.
3. PARTIAL-smoke-P0 contamination risk from filename alone: needs a structurally distinct path (separate dir or filename suffix), not just an internal JSON marker; smoke's n=4 eval numbers should be labeled memory-check-only, not gate-preview (statistical noise at n=4).
4. Auto-terminate false-positive risk: single SSH-unreachable check conflates network blip with process-dead; recommend multiple consecutive checks + a heartbeat FILE on the network volume (written by the training process) over SSH-reachability polling; ask whether this reuses the existing seat-independent watchdog/STOP-file convention or is a new ad hoc monitor; either way it should check for a recent successful rsync (now logged per the dim-4 fix) before deleting the pod.
5. $25/day enforcement needs to be a LIVE re-check against RunPod's actual billing state, not a one-time pre-flight estimate (this session's own ETA already slipped once); also confirm the cumulative caps ($100/$180/$200) are checked, not just today's $25.

No sign/block posture here (design review, pre-code) — findings are recommendations for studio's implementation to address before the full relaxed run launches.

## Launch+smoke findings — dispositions confirmed — 2026-09-16 (see timestamp above)

All 5 findings from the design review actioned:
1+2 (memory-bound/multi-GPU) -> STRATEGY: single-card path chosen (no multi-GPU build), efficiency-first (chunked/fused CE primary to fit a moderate card, escalate only if it can't).
1 detail (optimizer.step + eval-longest-first) -> folded into studio's PR: --eval-longest-first, optimizer.step() + save_every_steps=1 in smoke.
3 (PARTIAL-P0 naming) -> folded into studio's PR: structurally-distinct output path + report_kind field, n=4 labeled memory-check-only.
4 (monitor false-positive) -> lead-owned build: multiple consecutive checks before terminate, process-written heartbeat file, check recent successful rsync-to-5090 BEFORE deleting any pod.
5 (billing) -> lead-owned build: LIVE get-billing recheck for $25/day + cumulative $100/$180/$200.

PENDING when studio's code lands: shape-verify the critical invariant -- chunked-CE == current CE within tolerance (gate math must not move). No action now.

## Chunked-CE memory fix (assistant/studio/p2b-gc-multigpu @ 5ec3061) — dual-sign content-verify — (timestamp above)

Off main 13a90e5. THE fix for the OOM: `chunked_cross_entropy_backward` replaces `peft_model(...).loss.backward()` by default. Full numerical verification (not smoke-pass), per the lead's ask.

**(1) Loss/grad equivalence — VERIFIED CORRECT, careful check:**
- **Reference is right.** `peft_model(input_ids=..., labels=labels).loss` is exactly what the pre-fix `train_lora_sft` called (`outputs.loss.backward()`), and HF's causal-LM `.loss` property is a library-wide convention (shared `loss_function` across `XForCausalLM` classes) — the test validates the GENERIC HF convention on a cheap GPT2 fixture rather than the live 3B model, which is standard and sufficient given the convention is architecture-independent, not Qwen-specific code.
- **Traced the math myself, not just read the docstring.** Chunk loss = `F.cross_entropy(..., ignore_index=-100, reduction="sum")` per chunk, scaled by `1/n_valid` where `n_valid` is computed ONCE globally (before the loop) — so `total = Σ_chunks(chunk_valid_sum) / n_valid_global = total_valid_sum / n_valid_global`, exactly the reference's mean-over-valid-tokens. This is NOT a per-chunk-mean bias (the classic wrong way to chunk a mean loss) — confirmed correct.
- **Shift/masking verified, no off-by-one.** `shift_hidden = hidden[:, :-1, :]`, `shift_labels = labels[:, 1:]` matches the reference's `logits[..., :-1, :]`/`labels[..., 1:]` shift exactly; applying `lm_head` AFTER the position-slice is mathematically identical to slicing AFTER `lm_head` (a linear per-position map commutes with position-indexing) — traced this myself, no discrepancy.
- **Backward reconstruction is exact, not approximate.** Verified the two-stage backward (per-chunk local backward into a detached leaf accumulating a full-size gradient buffer, then ONE `hidden_states.backward(gradient=full_hidden_grad)` through the backbone) is mathematically exact given cross-entropy is additively separable across sequence positions — no cross-chunk interaction exists in the loss itself, only in the (unchunked) backbone forward, which IS handled by the single final backward. `lm_head` is the SAME live module (not a copy), so weight-tied embedding gradients accumulate correctly across all chunks.
- **LoRA A-init test-bug fix VERIFIED genuinely fixed, not vacuous.** Read the actual test code: `base_peft_model = self._wrap_peft(model)` wrapped ONCE, then `ref_model = copy.deepcopy(base_peft_model)` and `chunked_model = copy.deepcopy(base_peft_model)` — both start from the IDENTICAL LoRA A/B init. The equivalence test genuinely isolates only the compute-path difference; not vacuous.
- **Tolerance: loss `< 1e-5` (tighter), gradients `torch.allclose(atol=1e-4)`.** Consistent with the SAME atol=1e-4 bar I already reviewed and signed for the checkpoint-resume equivalence test earlier in this session — not a new/weaker standard. Per-step gradient tolerance affects the training TRAJECTORY (compounds like any GPU floating-point non-associativity does, not unique to this fix), not eval scoring directly (p2b_eval.py is untouched — the equivalence claim is properly scoped to "chunked-CE training produces the same model within tolerance," not "chunked-CE IS the eval-scoring metric").
- Tests exercise short/single-chunk, >10-chunk long (chunk_size=7 over 100 tokens), chunk-size-larger-than-sequence, and all-prompt-no-continuation (returns 0.0, no raise) — good coverage of chunk-boundary/degenerate cases.

**(2) Smoke cannot contaminate the gate — VERIFIED in code.** `smoke_output_path()` rewrites to a structurally distinct directory (`<parent>/smoke/<stem>.smoke.json`), and traced the CLI: `out_path = smoke_output_path(args.out) if smoke else args.out`, `write_p0_report(run.p0_report, out_path)` called exactly once — no bypass exists, a smoke run cannot land at the canonical P0 path regardless of `--out`. `report_kind` is `"smoke"`/`"full"`; `primary_pass`/`exceeds` explicitly nulled with a `smoke_note` when smoke — exactly closes my earlier finding-3.

**(3) Gate/eval-set/parse-rate untouched — CONFIRMED.** `p2b_eval.py` and `p2b_preflight.py`: zero diff lines (confirmed via `git diff --stat`, not present in the changed-files list at all).

**SIGN on all three dimensions.** Reported to lead per instruction, not Track A.

## CRITICAL finding: registry=None toy default on real-data scoring paths — gate-integrity ruling — (timestamp above)

5090 smoke surfaced a pre-existing bug (masked until chunked-CE let real-data scoring runs advance): P2b scoring defaults to `registry=None`, a 2-rule TOY unit-test fixture (`nyaya_ir.validators.Registry` default `{R416_NEG, R416_POS}`). Verified myself, not just the report: confirmed `Registry`'s default rules ARE that 2-rule frozenset (validators.py); confirmed `registry=None` is the default on 4 real-data-facing functions in p2b_eval.py (`structural_validity`, `structural_validity_rate`, the checker `__init__`, `default_full_ir_correctness`); confirmed `build_registry()` (scripts/p2a_deterministic_records.py) mechanically derives `{CID}_POS` per contract + `{CID}_NEG` where `denial` is set — from the SAME `CONTRACTS` table underlying the deny/el0-el2 structural work and the confusable-contract negatives already certified, not an independently-invented alternate registry. Lead's independent verification: build_registry reproduces the stored wire byte-for-byte on 377/377 gold + 225 negatives, default toy registry fails validation on 377/377, self-compare (gold_wire vs gold_wire) reads False with registry=None / True with build_registry.

**No prior gate result tainted** (this scoring path never ran on real data before — leak-check crashed first) but ANY future run under today's default would silently read checker_pass_rate/correctness ~0 for every real item — a plausible-looking but entirely bogus gate result, precisely the failure class this session's adversarial review exists to catch.

**Ruling on the three integrity questions (design decision, no code yet):**
1. **build_registry IS canonical; threading it RESTORES correct semantics, does not change the gold's meaning.** Mechanically derived from the single-source-of-truth CONTRACTS table; the byte-for-byte reproduction on all 602 real records is only possible if this is the registry the wire was originally encoded against (a wrong registry cannot deterministically reproduce every stored wire byte-for-byte). Did not personally re-run all 602 comparisons (pre-code design ask); recommend the byte-for-byte claim be a real, checked CI test before merge, not just a narrated result.
2. **YES, pin the rule-set hash, fail-closed, exactly like EVAL_SET_TOP_HASH / PINNED_SCORE_BINARY_SHA256.** Same silent-drift class this session has already fixed twice. Mechanism: sha256 over the sorted rule-set (same pattern as compute_config_hash/compute_dataset_hash), pinned in the P0, checked fail-closed at preflight -- refuse, don't just log, on any CONTRACTS-table edit that moves the derived rule-set.
3. **YES, essential -- and recommend REMOVING the `registry=None` default entirely** from the real-data-facing signatures (structural_validity, structural_validity_rate, checker __init__, default_full_ir_correctness) rather than adding a runtime check alone: a missing-required-argument error is a stronger guard than a skippable runtime assertion. Keep registry=None only where it stays genuinely toy/unit-test-only. Mirrors assert_score_binary_ready's existing fail-loud pattern in this codebase.

Good catch by studio+lead -- exactly the class of silent-wrong-default this whole review discipline is built to catch before it corrupts a real gate result. Reported to lead, design ruling given; will content-verify the actual implementation (registry threading + pin + fail-loud guard) when studio's code lands, same rigor as the chunked-CE verify.

## registry=None finding — abstain-sentinel exposure subtlety + SIGN (a)+(b) — (timestamp above)

New fact from studio's call-site inventory: the toy-registry bug is masked ONLY for ABSTAINING items (the UDAHARANA UNSUPPORTED sentinel skips the registry.rules lookup) — non-abstaining near-miss/negatives items that assert a real rule_id WOULD be silently mis-scored under registry=None. Verified the mechanism myself: `src/nyaya_ir/validators.py:123-127` confirms `tr.udaharana.rule_id == UNSUPPORTED_RULE` requires `t.abstain and t.hetvabhasa.verdict is None` -- structurally corroborates the bypass is abstain-only, not universal.

**This makes my Q3 ruling MORE load-bearing, not less**: a partial/silent exposure (only SOME items affected, depending on assert-vs-abstain, with no enumeration yet of how many of the 164 r_eval / 225 negatives assert) is exactly the failure shape a runtime-only check can miss in practice if a future caller happens to construct a mostly-abstaining test batch and never notices the asserting subset is wrong. Reinforces: remove the `registry=None` default entirely (Q3) rather than relying on per-call-site runtime discipline -- the signature itself must force every real caller through `build_registry()`.

**SIGN (a) + (b), per lead's ask (no re-verify-from-scratch needed, the 377/377 + 225/225 byte-exact reproduction is now confirmed on BOTH families -- gold AND negatives):**
- (a) build_registry canonical + threading it is RESTORATIVE (reproduces the frozen wire on both full_ir_gold and negatives), not a semantics change. SIGNED.
- (b) PIN the registry rule-set hash in the P0, fail-closed, same pattern as the score-bin/eval-set pins already established. SIGNED.
Fix scope must explicitly cover the asserting subset of r_eval/negatives (not just abstaining items) -- the fail-loud default-removal (Q3) is what guarantees this without needing the assert/abstain split enumerated first.

## registry threading FIX (assistant/studio/p2b-smoke-eval-fix @ 7b5f578) — full implementation dual-sign — (timestamp above)

Off 5ec3061, 13 files. Verified every claim against the actual diff/tests/independent computation, not the report.

**(1) Byte-for-byte reproduction IS a real CI test.** `tests/test_p2b_registry.py::TestBuildRegistryReproducesTheFrozenWire`: `assert len(rows) == 377` / `== 225`, genuine per-record `from_dict` -> `serialize(record, registry=build_registry())` -> compared against stored `row["wire"]`, `assert mismatches == []`. Not narrated — this is a real checked assertion, re-run every CI pass.

**(2) Pin covers the SORTED rule-set, fail-closed, checked in the P0 path.** Independently computed the pinned hash myself from the CONTRACTS table shown in the diff (17 rules: 14 `_POS` + 3 `_NEG` for the denial-bearing ids) — my own `sha256("\n".join(sorted(rules)))` == `81ca9eff360688003397ece7d04753988a7e3a7a7ca2e25698046b5cc419fa90`, the exact pinned constant. `verify_registry` raises `RuleSetMismatch` (not log) on mismatch; `run_p2b_preflight` calls `verify_registry(build_p2b_scoring_registry())` at line ~149 — wired into the real scoring path, not just available. Test suite includes a genuine tamper test (`dataclasses.replace(..., rules=frozenset({"NOT_A_REAL_RULE"}))` -> `pytest.raises(RuleSetMismatch)`) and an order-independence test.

**(3) Asserting subset genuinely exercised under the real registry.** `TestAssertingSubsetCoverage`: negatives — every one of 225 rows classified, `(asserting, abstaining) == (225, 0)`, and EVERY asserting record's `rule_id` asserted `in registry.rules` PLUS `serialize(record, registry=registry)` actually called (would fail/mismatch if wrong) — this is exactly the path that was silently broken before the fix, now under real test coverage. r_eval — `(asserting, abstaining) == (0, 164)`, predicate checked for all 164. Matches the lead's reported split, independently confirmed via the test's own logic, not just narration.

**(4) `applies_s416` origin CONFIRMED independently on main (pre-fix state).** `scripts/p2a_partial_ir_build.py:119` and `scripts/p2a_retrieval_only_build.py:131` both hardcode `predicate="applies_s416"` — a genuine pre-existing convention (both scripts predate this fix), not invented to patch around the bug. Adding it to `build_p2b_scoring_registry`'s `predicates` set is restorative (the predicate r_eval records were always authored with), not a semantics change; `build_registry()` itself is left untouched (verified: separate function, `base.rules` reused unmodified) so the T-tier/negatives registry is provably unaffected by the r_eval predicate addition.

**(5) Restorative, not a semantics change; eval-set/gate data untouched.** `git diff --stat 5ec3061 7b5f578 -- 'research/**'` is EMPTY — zero eval-SET data files touched, only scoring code. Confirmed `registry=None` REMOVED (not just supplemented) from all four real-data-facing signatures in `p2b_eval.py` (`structural_validity`, `structural_validity_rate`, the checker `__init__`, `default_full_ir_correctness`) — now `registry: Registry` with NO default, a missing-argument error rather than a skippable runtime check, exactly what I asked for in Q3.

**BONUS beyond my ask:** `TestNoRegistryDefaultRegressionInTheP2bScoringPath` — an AST-based static-analysis test scanning `p2b_preflight.py`/`p2b_eval.py` source for ANY direct `nyaya_ir` scoring call (`serialize`/`parse`/`validate_record`/etc.) missing an explicit `registry=` kwarg, failing CI on any future regression. Stronger than what I asked for — catches a future mistake at test-collection time, not just at the specific 4 signatures fixed today.

**SIGN on all five dimensions, no flags.** Reported to lead per instruction, not Track A. This was the critical gate-integrity fix of the whole P2b effort; verified rigorously.

## max_new_tokens truncation finding — gate-validity ruling — (timestamp above)

5090 full run surfaced: --max-new-tokens 128 (CLI default 256) truncates generation well before the gold wires' ANSWER line (median ~294 tokens deep, wire median ~483/max ~613 tokens) -- so metrics 1/2/2b/3 would read a plausible-looking structural 0 on every real item, indistinguishable from "base can't emit IR." Same failure class as the registry bug (silently wrong, not crashing).

Verified independently before ruling: (1) sampled one real gold wire (7b5f578, gold.jsonl row 1) -- ANSWER at line 21/24, 1808 chars total; ANSWER genuinely sits near the tail, and 1808 chars is roughly consistent (char/token ~4-4.5) with the claimed ~483-token median -- a light plausibility check, not a full independent recompute of all 377 (no tokenizer access here). (2) grepped the prereg for max_new_tokens/max-new-tokens: ZERO occurrences -- genuinely unpinned, confirmed.

**Ruling:**
(a) CONFIRMED -- 128/256 invalidate metrics 1/2/2b/3: a truncated wire is incomplete (missing NIGAMANA/ANSWER/AUDIT), so structural validity fails and checker_pass/correctness can't reach a verdict (no ANSWER to score) -- a budget artifact masquerading as a capability measurement.
(b) LOGIC SOUND: budget=1024 (>613 max) + fail-loud guard (refuse unless budget >= 1.1x REAL computed max_target_tokens) makes grounded=False a genuine non-emission signal, not truncation -- conditioned on the standard assumption that an SFT-trained model's generation length tracks its gold's length; a genuinely degenerate/looping generation could still hit the cap, but that IS a legitimate model failure, not a false negative, so it doesn't undermine the fix.
(c) RECORD BOTH VALUES (budget + computed max_target_tokens) in the P0 for reproducibility/auditability -- analogous to N/achieved-MDE already recorded as derived measurements. Recommend pinning the ADEQUACY RELATIONSHIP (budget >= 1.1x max_target_tokens, recomputed + fail-closed-checked against the real frozen eval-set every run), NOT a hardcoded "1024" constant -- a fixed integer pin (like the score-binary sha) is for bit-identical artifact content; a token budget should stay a checked, derived, re-verified relationship so it doesn't go stale if the eval-set or base model's tokenizer ever changes.

Reported to lead per instruction, not Track A.

## Generation-budget fix (assistant/studio/p2b-codec-stopiteration-fix @ 0c87b5e) — implementation dual-sign — (timestamp above)

Off 7b5f578, 6 files. Verified every claim against the actual diff + my own independent computation, not the report.

**(1) Relationship, not literal -- CONFIRMED, no hardcoded integer anywhere.** `compute_max_target_tokens(eval_set, tokenizer)` tokenizes every real row's OWN target text (`full_ir_gold`/`negatives`' frozen `wire`, `r_eval`'s frozen `target`) with the model's own tokenizer, fresh every call -- no cached/pinned number. `resolve_max_new_tokens`: `requested is None` DERIVES `ceil(margin * max_target_tokens)`; an explicit `requested` is validated via `assert_generation_budget_adequate`, which raises `GenerationBudgetTooSmall` fail-closed on inadequacy. CLI defaults `max_new_tokens` to `None` -> derive mode. Grepped for `1024`/hardcoded literals in the diff: none found tied to the budget logic.

**(3) Float-edge -- INDEPENDENTLY VERIFIED myself, not just read.** Ran `_required_max_new_tokens`'s exact formula in Python: `100*1.1` raw IEEE-754 = `110.00000000000001`, naive `ceil()` = 111 (wrong, over-strict by 1), `ceil(round(100*1.1, 9))` = 110 (correct) -- reproduces the exact bug-and-fix claimed. Confirmed NO under-counting risk: `round(x, 9)` only strips floating-point representation noise at the ~1e-16 scale; token counts are integers and the margin (1.1) is a fixed, exactly-representable-enough decimal, so 9-decimal rounding cannot mask any genuine fractional requirement (there are none -- tokens are discrete). Spot-checked several realistic max_target_tokens values (294/483/613/999999): all resolve identically between naive and fixed at those specific values (no boundary hit), confirming the fix changes nothing except the exact float-noise edge case it targets.

**(4) Restorative, additive-only -- CONFIRMED.** Traced the negatives-loop diff: `abstained_flag = _model_abstained(sft_answer, record.prompt, registry=registry)` -- the exact same call, same arguments, as before this fix -- and its consumption (`if abstained_flag: abstained += 1; continue`) is UNCHANGED. Truncation tracking (`_track_truncation`, `neg_non_abstained_total`/`_truncated` counters) runs ALONGSIDE as separate accumulators, never altering what feeds `abstained_flag` or the metric itself. `max_new_tokens` defaults to `None` in `run_p2b_preflight`'s signature -- every pre-existing test/caller unaffected unless it opts in.

**(2) RECORD BOTH -- CONFIRMED, and more.** P0's `generation_budget` dict: `margin`, `max_target_tokens_observed`, `max_new_tokens_requested`, `max_new_tokens_effective`, PLUS `truncation_rate_overall` and `truncation_rate_negatives_non_abstained` -- both values I required, plus derived diagnostics I didn't ask for but are directly useful.

**BONUS beyond my ruling, and it matters: `_generation_hit_budget_ceiling` + `truncation_rate_negatives_non_abstained`.** This directly answers the residual risk I flagged in my own (b) ruling on the prior finding ("a genuinely degenerate/looping generation could still hit the cap") -- Track A traced it further and found the SPECIFIC harm: a truncated negatives-family generation that never reaches ABSTAIN reads as "failed to abstain" rather than "ran out of budget," which could corrupt `abstention_recall` (metric 3b, a HARD ≥0.90 safety ceiling) specifically. The fix tracks this as a SEPARATE, non-corrupting diagnostic (post-hoc, per-item, proxy-based via re-tokenizing the answer and checking near-ceiling length + absent ANSWER line) rather than silently baking it into the metric -- a nonzero `truncation_rate_negatives_non_abstained` is a red flag to investigate, not a silent metric distortion. Exactly the right shape of fix for a safety-critical metric.

**SIGN on all four dimensions, no flags.** Reported to lead per instruction, not Track A.

## Full Qwen-3B P0 — decoding degeneration finding — gate-validity ruling — (timestamp above)

P0 came back all-0.0/primary_pass=false. Lead's harness-sound checks (structural_validity(gold)=3/3, correctness(gold,gold)=3/3, SFT target=target-only) rule out a parsing/scoring bug; raw dumps show Qwen-3B knows the wire grammar but naive greedy/no-repetition-penalty/no-stop rambles into repetition loops past a near-valid answer (23.6% overall truncation, 28.9% on non-abstained negatives), and the SFT adapter separately collapsed to an always-abstain shortcut. Lead is NOT launching 1.13B on this result — correct, not a clean gate measurement.

**Ruling on the three questions:**

**(1) CONFIRMED invalid as a gate result — but for TWO DIFFERENT reasons on different arms, worth keeping separate, not lumped as one "decoding artifact":**
- **checker_pass/correctness/structural/abstention_recall**: directly corrupted by rambling-truncation. These require reaching a clean final ANSWER/AUDIT close (full-IR) or the ABSTAIN line (negatives) — truncation at the budget ceiling reads as a structural failure indistinguishable from incapability, same class as the earlier registry/budget findings. Not trustworthy as-is.
- **SFT-arm collapse-to-always-abstain**: this is a SEPARATE, GENUINELY REAL finding, not a decoding artifact — a 1-epoch/r8 SFT config mode-collapsing to a trivial always-abstain shortcut is a real training-strength problem (would show as artificially LOW false-prove — trivially safe by never asserting — and near-zero correctness). Flagging this distinction because the decoding fix alone will NOT fix the SFT collapse; that needs its own remediation (more epochs/rank/etc.), tracked separately from the decoding re-measure.
- **retrieval_recall 0.267 CI[0.180,0.376]**: checked `_parse_identify_ids` myself — it scans EVERY wire line for the FIRST `"IDENTIFY "` line, no requirement to reach the wire's final close, so it IS structurally more robust to TRAILING repetition-loop rambling than the full-IR/checker path (a model that states its identification correctly and only then loops would still score correctly here). But NOT fully clean: if the loop starts BEFORE the model reaches its own IDENTIFY line, the parser falls through to `answer_wire.split()` (the whole raw text as token-soup) — garbage, not a real measurement, for the same underlying decoding-degeneration reason. Recommend a targeted diagnostic (same idea as `truncation_rate_negatives_non_abstained`): fraction of retrieval generations that hit the IDENTIFY-line fallback vs. found a real IDENTIFY line, to quantify how much of the wide CI is decoding-contaminated vs. genuine. Partially-clean, not certified clean.

**(2) Harness-config fix, NOT a new prereg amendment/sha — WITH ONE CONTENT CONDITION.** The prereg pins ONLY "greedy (temp 0)" (line 146: "our SFT model on our GPU is deterministic at temp 0, decoding variance ≈ 0") — it says NOTHING about repetition penalty, no-repeat-ngram, or stop sequences. Adding those does not contradict "greedy temp-0": greedy argmax token selection is still fully deterministic with a repetition penalty or stop condition layered on top — these are ADDITIONAL constraints on the same deterministic search, not a different decode mode. This is filling a genuine gap the prereg never addressed, not overriding a frozen decision — same shape as the max_new_tokens fix I already ruled on (measurement-config, restores validity, doesn't touch gate math).

**CONTENT CONDITION (my stake, not a process question):** unlike max_new_tokens (an objectively-derived scalar with a clean adequacy criterion from the eval-set itself), repetition_penalty/no_repeat_ngram_size are free hyperparameters with actual tunable VALUES. The nyaya-law-v1 wire format LEGITIMATELY contains repeated structural tokens (multiple NODE/EDGE lines, repeated `G_SATISFIES(E(...,AC),E(...,EL))` patterns are normal, correct wire content, not degenerate rambling). An overly aggressive repetition penalty or no-repeat-ngram window COULD suppress genuinely-required repeated tokens in a well-formed wire — a NEW failure mode, not a fix. **Require a sanity check before trusting numbers under the new config**: verify the chosen decoding params do NOT corrupt legitimate gold-shaped generation (same "restores vs. changes meaning" check I required for the registry and budget fixes) — e.g. force-decode or score the gold wire text itself under the new repetition-penalty/no-repeat-ngram settings and confirm it still parses/validates cleanly, analogous to `structural_validity(gold)=3/3` already run for the harness-soundness check.

**(3) YES, record/pin the decoding config in the P0, same discipline as the budget fix.** Recommend recording the EXACT values (repetition_penalty, no_repeat_ngram_size, stop-sequence definition) in a `decoding_config` block in `measurements`, analogous to `generation_budget`. Given repetition_penalty/no_repeat_ngram are free-tuned values (not objectively derived like max_new_tokens), I'd go further than "record": recommend the CHOSEN values be justified in the P0 (e.g. "smallest repetition_penalty that eliminates looping on a held-out sample without corrupting gold-shaped generation") so a future re-run doesn't silently drift to different, unexplained values.

This is a re-measure with fixed decoding + separately-addressed SFT collapse, not a base-lock — agreed with lead's framing. Reported to lead per instruction, not Track A.

## Root-cause confirmation: chat-template gap + structurally-enforced SFT imbalance — (timestamp above)

Verified both of the lead's root-cause diagnoses myself before agreeing, not on trust.

**Chat-template gap CONFIRMED in code.** Read `HfPredictor.generate` (src/prabhasa_nyaya/p2b_sft.py): `inputs = self.tokenizer(prompt_text, return_tensors="pt")` -- raw prompt text straight to the tokenizer, no `apply_chat_template()` call anywhere in the class, no `<|im_start|>`/`<|im_end|>` role-wrapping, no explicit `eos_token_id` override beyond the tokenizer's bare default. For an Instruct-tuned model (Qwen2.5-3B-Instruct) that was trained exclusively in chat-template format, this is a genuine, significant harness defect -- explains BOTH degraded grammar adherence (the model has never seen raw-completion-style prompts) AND the lack of a natural stop point (the model was never trained to emit `<|im_end|>` outside a chat-formatted context it doesn't recognize it's in). Bigger than a decoding-parameter tweak, exactly as claimed.

**SFT imbalance root cause is STRONGER than "empirically confirmed" -- it's STRUCTURALLY ENFORCED, independently verified from my own earlier work this session.** When I checked the `abstain_reason` schema for the confusable-contract negatives ruling, I already read `src/nyaya_ir/validators.py:154`: `require(t.abstain and t.abstain_reason=='dataset_label_only', 'holding_selection/statute_identification must abstain with dataset_label_only')`. This means P (`holding_selection`, ~5245) and R_train (`statute_identification`, 1156 post cluster-disjoint split) are not merely empirically 100% abstain -- the wire VALIDATOR ITSELF forbids them from ever asserting. **This sharpens the fix's scope precisely: any rebalancing/upweighting can only act within T (~210 rows, the only tier where "assert" is even a schema-valid target) -- P and R_train cannot structurally contribute assert examples no matter how weighted.** Root cause confirmed and scoped more tightly than the lead's framing alone implied.

**Responses to the lead's three points:**
- Gold-generation sanity check under the FULL new config (chat-template + stop + repetition params): AGREE, this is the natural strengthening of my own condition (2) from the prior finding -- no new content concern, confirmed necessary given chat-template is the bigger change.
- Retrieval-recall IDENTIFY-fallback-rate diagnostic routed to studio: agreed, nothing to add.
- Chat-template = "config fix not gate-math amendment, even more clearly than repetition params": AGREE. Same prereg gap-filling logic as before (prereg pins only "greedy temp-0", is silent on prompt formatting) -- fixing this restores correct use of the instruct model rather than changing what's measured.

**ONE NEW implementation-verify flag for when the fix lands (not resolved now, holding for the code-verify step):** confirm `apply_chat_template()` (or equivalent) is applied CONSISTENTLY to BOTH the zero-shot path (`render_prompt`) AND the 5-shot-from-goldens path (`render_prompt_5shot`, used for the IR-emission-parse-rate check I already dual-signed) -- if chat-templating lands on one path but not the other, the zero-shot-vs-5-shot parse-rate comparison becomes apples-to-oranges, a new, different measurement-validity problem. Will verify this explicitly against the actual diff.

Net: re-measure = chat-template + stop + repetition params (decoding, one content condition: verify against gold-shaped generation under the FULL config) + rebalanced T-tier SFT data (separate, now precisely scoped to T-only). Not a base-lock, agreed. Reported to lead per instruction, not Track A.

## Chat-template + decoding fix (assistant/studio/p2b-chat-template-decoding-fix @ fae7305) — dual-sign, 4 clean + 1 flag — (timestamp above)

Off 0c87b5e, 5 files. Verified every claim against the real diff + tests, not the report.

**(1) Both-paths consistency — CONFIRMED, no apples-to-oranges risk.** `_render_chat_prompt` is called at exactly ONE shared point, inside `HfPredictor.generate()` -- both `render_prompt` (zero-shot) and `render_prompt_5shot` only ever produce a plain `prompt_text` string handed to `generate()`, which then chat-templates it identically regardless of which condition called it. Confirmed `render_prompt_5shot` builds each of its 5 example blocks via `render_prompt(example_prompt)` (the SAME function, so `WIRE_FORMAT_INSTRUCTION` -- item 4 -- is also included identically in both conditions, not just the chat template). Training mirrors via `_render_chat_full` -- same user turn, target as assistant turn. No drift possible between conditions.

**(2) Gold-sanity framing -- CONFIRMED ACCURATE, but doesn't close my actual concern (see (3)).** `structural_validity(gold)=3/3` parses the STATIC STORED wire text, which by construction is never touched by a generation-time decoding parameter -- the framing "a static-gold parse-path regression check, unaffected by decoding by construction" is honest and correct. It proves the parse PATH itself still works post-fix. It does NOT prove `repetition_penalty=1.15` leaves a FRESHLY-GENERATED wire structurally uncorrupted -- that requires generating (not parsing pre-existing text) under the shipped config, which is a different check (see (3)).

**(3) Decoding values -- justification for `no_repeat_ngram_size=0` is sound and independently sensible; `repetition_penalty=1.15`'s SAFETY claim has a real, non-blocking gap.**
- `no_repeat_ngram_size=0` (HF's own no-op default, i.e. NO new hard constraint added at all): the shipped code confirms this -- the safest possible resolution, consistent with "verified unsafe, kept off" rather than "added then mitigated." No concern.
- `repetition_penalty=1.15`: the docstring's own empirical claim is "regenerating the 3 items that previously hit the ceiling... none hit it anymore even at 1.0... generation still STOPS CLEANLY at every value 1.0-1.3." **"Stops cleanly" (reaches the end-of-turn token) is necessary but NOT sufficient for "content not corrupted."** A soft repetition penalty could still bias a generation AWAY from a legitimately-required repeated n-gram mid-sequence (e.g. a second `G_SATISFIES(...)` needed for a second element) while the generation still terminates cleanly at `<|im_end|>` -- producing a malformed/incomplete wire that nonetheless "stopped cleanly." I checked the test diff specifically for this: `TestChatTemplateSft`'s three tests (`test_prompt_ids_is_a_true_prefix_of_full_ids`, `test_masked_region_ends_exactly_at_the_assistant_turn_start`, `test_generate_stops_on_the_real_end_of_turn_token_not_eos_token_id`) cover chat-template/masking correctness and stop-token correctness -- NONE of them generate under `repetition_penalty=1.15` and check `structural_validity`/checker on the FRESH output. The "3 items regenerated, none hit ceiling" claim is a real investigation result (I have no reason to doubt it happened) but is NOT captured as a committed, re-run-every-time CI test, and does not itself check post-generation structural validity -- same gap-class as the registry byte-for-byte claim before it was turned into a real test.
  **Non-blocking recommendation, condition on trusting RE-MEASURE numbers (not on merging this code):** before the gated re-launch, run `structural_validity`/checker on a small held-out sample's FRESH generations under the shipped `repetition_penalty=1.15` (not the static gold) and confirm no content corruption -- ideally as a committed test, at minimum as a recorded pre-launch check. The guard code itself (no_repeat_ngram=0, chat-template, stop-criteria) is correct and doesn't need to change; this is about validating the ONE remaining tuned parameter empirically before trusting numbers under it.

**(4) WIRE_FORMAT_INSTRUCTION fairness -- CONFIRMED FAIR, not coaching.** Read the literal text: "Respond with ONLY the nyaya_ir wire grammar below (NODE/ROLE/.../ANSWER lines, in that structural order) -- no prose, no markdown, no explanation outside the wire lines." Pure FORMAT instruction -- what grammar/lines to use, explicitly NO content/verdict/contract hint. Applied via the single fixed `render_prompt` (prereg S6: "identical for both bases and eval") -- can't differentially favor one arm. Legitimate and standard (equivalent to "respond in JSON only" for a chat model that would otherwise default to explanatory prose) -- addresses a FORMAT-parseability problem, not a content/correctness one. Not gaming.

**(5) identify_fallback_rate -- CONFIRMED wired exactly as requested.** Tracked per-item across BOTH base and sft generations in the r_eval loop (`identify_fallback_hits += int(not _has_real_identify_line(...))` for each), recorded in `PreflightResult.identify_fallback_rate`. Closes the diagnostic gap I flagged on the prior retrieval-recall ruling.

**RULING: SIGN 1/2/4/5 clean. FLAG 3 (repetition_penalty content-corruption check) as a specific, non-blocking, well-scoped follow-up -- condition on trusting the RE-MEASURE's numbers under this config, not on merging this fix.** Reported to lead per instruction, not Track A.

## Flag-3 closure: fresh-generation decoding check (assistant/studio/p2b-freshgen-validation @ 5369440) — dual-sign

Off `fae7305`, 2 files (+411/-0, no binaries), fetched from `github-axismeru:AxisMeru/prabhasa-nyaya` and read directly — confirmed diff shape matches the report exactly.

**(1) Genuinely generates + scores through the real sha-enforced checker, not gold text — CONFIRMED.** `run_check` loads the real eval-SET (`load_and_verify_eval_set`), real SFT dataset for contamination-free few-shot spares, real fail-closed registry (`verify_registry(build_p2b_scoring_registry())`), asserts the real pinned score binary is ready, loads the real cached Qwen2.5-3B-Instruct base model (no adapter — correctly scoped: this is a decoding question, independent of SFT), and for each penalty calls `predictor.generate(prompt_text)` fresh per item, then scores through `structural_validity(...)` and `_sha_enforced_checker(...)` — the SAME real checker path production preflight uses, not a mock or a static gold comparison. `resolve_max_new_tokens` is reused (the 2026-09-16 budget-adequacy fix), correctly guarding against the exact "truncated before ANSWER" artifact class being independently distinguished from a decoding-corruption finding.

**(2) structural-prefix-match as the sufficiency test — CONFIRMED the right test, and better-designed than I expected.** `wire_structural_prefix` splits at the first `\nANSWER ` marker; the corruption concern (a soft penalty suppressing a legitimately-required repeated structural n-gram) can only show up in the NODE/ELEMENT/CLAIM/.../ABSTAIN prefix, never in the free-text ANSWER sentence, which is expected paraphrase variance by design — verified this split logic directly (read the function, plus its dedicated tests: exact-prefix-strip, no-answer-line passthrough, same-prefix-different-answer-text, and a differing-prefix negative control that confirms the check isn't vacuously always-true). Genuinely stronger than a bare pass-rate comparison: `run_check`'s `raw_rates_vacuous`/`all_prefixes_match`/`inconclusive` branching correctly refuses to report a false "safe" verdict when the baseline itself is 0/0 (no legitimate near-valid output existed for a penalty to have corrupted) UNLESS every candidate reproduced the baseline's structural prefix byte-for-byte on every item, which is direct, conclusive evidence rather than an inferred one. This is exactly the discipline this flag asked for.

**(3) Real committed test, not narrated result — CONFIRMED.** Fetched the branch, ran `tests/test_p2b_freshgen_decoding_check.py` myself (PYTHONPATH=src, real project venv, off-tmpfs worktree per the ENOSPC lesson) — 11/11 pass, covering `choose_repetition_penalty`'s full decision surface (matching/beating/corrupting-vs-baseline, tolerance, multi-candidate largest-wins, missing-baseline KeyError) and `wire_structural_prefix`'s exact split semantics including the specific real 2026-09-17 finding pattern (differing ANSWER paraphrase, identical prefix). The docstring is honest about what's mocked vs real: `choose_repetition_penalty`/`wire_structural_prefix` are pure logic tested against synthetic dicts (legitimate — no measurement claimed here); the actual rate/prefix numbers only ever come from a real GPU run against real cached weights, never fabricated or reported from this test file.

**Secondary finding independently verified:** the base model's `structural_validity` failures trace to `src/nyaya_ir/validators.py:89`'s exact span-bounds check (`0 <= s.start <= s.end <= len(sources[s.source_id])`) — confirmed this line exists as described, real out-of-bounds-span rejection, not an exact-text-match artifact. Consistent with a learnable convention gap (dose-response / corpus-enrichment territory) rather than a decoding-corruption question — orthogonal to flag-3, correctly kept separate in the report.

**RULING: flag-3 CLOSED. SIGN all 3 dimensions.** The `inconclusive`-vs-`all_prefixes_match` branch design is exactly the right way to handle a possible 0/0 baseline without either hiding it or blocking on it — no further condition on the re-measure numbers from this angle. Reported to lead per instruction, not Track A.

## Batched generation (assistant/studio/p2b-freshgen-validation @ b431d25) — structural-purity proof for false_prove/abstention_recall

Off merged `5369440`, FF-clean. Confirmed source-scope matches lead's claim: `git diff 5369440 b431d25 --stat` touches only `p2b_preflight.py`, `p2b_qwen_cli.py`, `p2b_qwen_entrypoint.py`, `p2b_sft.py` + 2 test files — no eval-set/gate/registry/validators/codec files.

**Crux ask: does any scored field read the ANSWER free-text as input?** Read every metric function in `p2b_eval.py` directly, tracing each one's actual inputs:
- `RegistryChecker.__call__` (feeds `grounded`, hence `checker_pass_rate` AND `false_prove_and_abstention`'s false-prove side): reads `target.trace.pratijna.subject` (event_id) and `target.graph.claims` (satisfies-claims) to build the REG wire sent to the Lean binary. Never touches `target.answer`.
- `default_full_ir_correctness` (feeds `correctness_rate`/`correctness_lift`): compares `frozenset(s.passage_id for s in pred.cited_sections)` and `pred.abstain == gold.abstain` only. Never touches `.answer`.
- `false_prove_and_abstention` itself: takes a pre-computed `model_abstained: bool` per item (never the raw text) plus calls `checker(...)` (proven above). Traced `model_abstained`'s source to `p2b_preflight._model_abstained`, which returns `target.abstain` — the parsed structural ABSTAIN-line boolean, not a text read.
- `structural_validity` (feeds gate metric 1): calls `nyaya_ir.parse`, which validates the whole wire grammatically but the boolean result is parse-success/failure, not an ANSWER-content read.

**VERDICT: structurally proven, not just empirically observed at n=8.** Every scored field lead asked about (`checker_pass`/`grounded`, `false_prove_rate`, `abstention_recall`, `correctness_rate`) is a pure function of `target.graph.claims`, `target.trace.pratijna.subject`, `target.cited_sections`, and `target.abstain` — the parsed structural fields. `target.answer` (the free-text field the ANSWER line populates, confirmed in `nyaya_ir/codec.py`) is never read by any of these four functions' bodies. So batch-induced FP divergence confined to the ANSWER line — as lead's n=8 empirical check found (4/8 exact-text diverged, 8/8 structural_validity+grounded held) — CANNOT perturb any of these numbers, unconditionally, not just in the tested sample.

**Caveat (the one thing NOT proven structurally):** this proof is conditional on divergence staying confined to the ANSWER line. Whether batch-FP rounding ever perturbs an EARLIER (pre-ANSWER) token under some batch composition/size other than the one 8-item sample tested is an empirical claim, not something the code structure guarantees — a different batch size or composition could in principle diverge somewhere in the structural prefix instead.

**On the belt-and-suspenders ask (final certified number re-run at batch_size=1):** given the structural proof above, this is NOT necessary to protect the primary gate numbers from ANSWER-text FP divergence — that class of concern is closed regardless of batch size, unconditionally. Recommend keeping it anyway, but downgrade it from "necessary" to "cheap insurance": its only remaining value is as a guard against the caveat above (untested batch compositions diverging pre-ANSWER), which is a much narrower and lower-probability concern than the one that motivated the ask. Not a blocking condition either way.

**RULING: SIGN.** No structural gap in the scoring-purity claim. Reported to lead per instruction, not Track A.
