# P2a teacher gate — Track B reads and rulings, 2026-09-15 16:42 BST

## 1. Inventory D-count @ `assistant/trackA/p2a-inventory-d-count` `07606c0` (code `0e548f8`, an ancestor): CLEARED
D = 480 with records/provenance hashes; cross-check records == non-skipped provenance rows; 140 control / 30 deny / 310 omit; 40 skips excluded; 10 control scenarios per id; every verdict matched. **Ruling on the floor: 25 % is authoritative** (amendment (d), 2026-09-15 15:38 BST, superseded amendment (c)'s 40 %, which was structurally unreachable with one omission slice per element). 29.2 % passes. The inventory note may cite amendment (d) and drop the discrepancy text at the next regeneration; no other change.

## 2. P2a-manifest.json DRAFT @ `assistant/trackA/p2a-manifest-draft` `6e7533b`: NOT FROZEN; three rulings to fold in, then route back for the freeze signature
(a) **Per-id allocation**: the flat 143/142 split stays as the cap per id, but sampling runs in **rounds of 20 proposals per id (280 per round)**, stopping when T ≥ 200 and every id has A_i ≥ 5, or at the cap, whichever first. Same allocation, less spend; the round index goes into each attempt's provenance.
(b) **Teacher** (methodology mine; spend the lead's): the box has one configured API provider, DashScope international (chat.env, model qwen-plus), and the routing configs name `qwen3.8-max`. Proposal: **primary teacher `qwen3.8-max` via DashScope international**, snapshot id taken from the endpoint's model listing at freeze time and recorded; temperature 0.7, top_p 0.9, max_tokens 2048; seed per attempt = first 8 hex of sha256(attempt_id); system prompt: "emit only the nyaya-law-v1 wire form for the given prompt object; no prose"; user prompt = the record's `prompt` object rendered exactly as the D-tier records render it plus the grammar section of `docs/typed-ir/nyaya-ir-schema.md`; few-shot = 3 D-tier batch-03 records (one control, one omission, one denial) drawn from contract_ids other than the target id, fixed per id and listed in the manifest. Fallback teacher, only if the primary's parse rate < 0.5 after round 1: `gpt-6-astra` through the Codex CLI with the same prompt, recorded as a manifest amendment. Lead confirms DashScope credit availability and cost before freeze. Nothing samples until I sign the complete manifest.
(c) **`checker_adapter.verdict_field_mapping` (ruled here, same rule the D-tier generator uses):** a proposal asserts element *k* of `contract_id` iff `target.elements[k].status == "established"` AND it carries ≥ 1 evidence span into the prompt's facts; each such element becomes one `G_SATISFIES(E(the conduct in the facts,AC),E(<element string from --describe-contract>,EL))` claim; an element entry whose id is the Contract's denial entity with status established becomes the denial claim; anything else (unknown, missing, no evidence) is not sent and surfaces as `omitted`. Graph nodes and the teacher's AUDIT/answer text are never a source of claims. The expected verdict for acceptance is `grounded` for a control-shaped proposal, `flagged` naming exactly the elements the proposal itself marks not-established for an omission-shaped one, and denied for a denial-shaped one; a proposal whose self-declared shape and the checker's verdict disagree is rejected (`checker_disagrees_with_self_report`).

## 3. replay/decide DESIGN @ `assistant/trackA/p2a-replay-decide-design` `ab2c066`: CLEARED for implementation
Five-stage replay (round-trip, structural validation, independent span/hash/event/rule checks, REG certification, fail-closed), exact + narrative-only Jaccard dedup, lineage/template-family exclusion closing exclude's deferred item, decide as literal conjunction with invalidation before arithmetic: all as the prereg requires. **Ruling on the ambiguity: fresh checker call.** `replay` re-runs the compiled binary for every record; a teacher-time REG report is kept in `proposals.jsonl` only as a cross-check, and any disagreement between the stored report and the fresh call rejects the record (`replay_disagrees_with_sampling_time`). The binary sha256 in the manifest must equal the sha256 replay records for every call.

Addendum (via lead): DashScope international serves `qwen3.8-max` on the configured key (HTTP 200 ping by the lead); API spend signed off by the lead, bounded by rounds-of-20 and the T ≥ 200 / A_i ≥ 5 stop. Manifest additions: parse the `content` field only; `reasoning_content` stored verbatim, never parsed; `max_tokens` applies to content; reasoning budget/disable recorded if the API exposes it, else "not controllable"; truncated or empty content = malformed attempt in the denominator, retries are new attempts with their own `attempt_id`.

## Manifest @ `a3e7e26`: NOT FROZEN, one structural fix; replay/decide @ `2f2d719`: two rulings — 2026-09-15 16:59 BST

**Manifest defect:** `teacher.user_prompt_template` binds "the target record's own prompt object" from batch-03. The teacher would then emit IR for the D-tier's own 140 scenarios; by amendment (c) lineage (scenario_id) those are the same records, T would be a re-labelling of D, and the yield gate's T ≥ 200 would measure nothing about the teacher's ability to produce new supervision. Ruling: **the teacher batch samples new scenarios, two stages per attempt, both recorded under one `attempt_id`:**
- Stage 1 (scenario): given `contract_id`, its element list from `--describe-contract`, the limb read from the Contract's module doc, the chained-element and dependency rules, and a target shape (`control` | `omit:<k>` | `deny`) drawn round-robin, the teacher authors a prompt object (neutral narrative + one self-contained fact per element it intends to establish). Guards before stage 2: narrative-only Jaccard ≥ 0.40 against every D-tier scenario of the same id and every earlier accepted proposal of the same id → rejected as rephrasing (logged, counts in the denominator); the omission/dependency rules apply (an `omit:<k>` shape may not target a declared dependency; the dropped element's fact must be absent).
- Stage 2 (IR): the stage-1 prompt object rendered as the D-tier renders prompts, plus the grammar section, plus the fixed cross-id few-shot; output parsed from `content` only.
- Acceptance = stage-2 record passes replay (structure, spans into the stage-1 facts, fresh REG verdict agreeing with the self-declared shape). Provenance per attempt: `attempt_id`, `contract_id`, `shape`, `round`, `seed`, `stage1_prompt_sha256`, `jaccard_max_overlap` (+ against), stage-2 wire, REG raw, binary sha256.
Few-shot policy (fixed cross-id batch-03 examples) stands. The two deferred live values (`model_snapshot_id`, `reasoning_budget_control`) are fetched by the lead now and inserted; I sign the freeze when the two-stage prompt source and the live values are in.

**replay/decide rulings:** (1) canonical `target.elements` shape is the positional list in `--describe-contract` order, as the real records have it; `_normalize_elements` accepting a dict-by-name too is acceptable provided a dict with a name not in the element list is rejected, not dropped. (2) Near-duplicate dedup is required, not optional: reuse the D-tier script's constant (narrative-only Jaccard ≥ 0.40 reject, 0.30–0.39 flagged in `checks.jsonl`), computed on the stage-1 narrative against all D-tier narratives of the same id and all earlier accepted proposals of the same id; exact-hash alone does not meet the prereg's "rephrasings do not multiply" rule. Otherwise the implementation follows the cleared design.

Rulings (via lead, 17:02 BST): live values accepted (`qwen3.8-max-0902`; `enable_thinking:false` recorded as `reasoning_budget_control`; enabling thinking later is a manifest amendment with a new sha). Elements shape: one canonical positional form, dict-by-name rejected as `non_canonical_elements` (reconciled with Track A). P/R scope: `P_min`/`R_min` remain conjuncts; with no P/R data, `decide` emits **PARTIAL**, never PASS, citing the prereg lines; D and T reported in full; the teacher batch runs on the frozen manifest; P and R inventories are the next work item; P2b opens only on a six-conjunct PASS.

## Two-stage branch @ `ce9968c` — manifest content READ OK; replay HELD; freeze deferred — 2026-09-15 17:13 BST

Verified by me from a throwaway worktree at `ce9968c`: build 137/0; binary sha256 `9bff8f30…` equals the manifest's pin; `--describe-source bns69` returns the s.69 text; `decide` fails closed when `checks.jsonl`/`accepted.jsonl` are missing (correct). Manifest content: the two-stage teacher (stage-1 authoring with the ≥ 0.40 Jaccard guard before stage 2; stage-2 rendering with cross-id few-shot; provenance incl. `stage1_prompt_sha256`), live values (`qwen3.8-max-0902`, `enable_thinking:false` with the amendment-path fallback), rounds of 20, verdict mapping and self-report agreement, fallback teacher not pre-authorised: all as ruled. Nothing in the manifest content changes; the binary pin re-points to the post-`#guard` build per the lead, and the freeze names that SHA.

**replay defect (HELD):** stage 1 `_round_trip_ok` accepts a `wire` only if it starts with a REG/COMP/A3 tag. The records' `wire` field (D-tier batch-03 and, by the manifest, every teacher proposal) is the nyaya-law-v1 line grammar (`RECORD … NODE … CLAIM …`), which is what the teacher emits and what the schema round-trips. Reproduced: an untouched batch-03 control record submitted as a proposal is rejected at `round_trip`; so are all three test proposals; `contract_id` never gets derived. As written, replay would reject 100 % of valid proposals, and the "literal D-tier copy caught at Jaccard 1.0" test cannot have been run against the real `wire`. Required fix: stage 1 = a real round-trip, `nyaya_ir.codec.parse(wire, record.prompt, registry=<contract's rule registry>)` succeeding and re-encoding to a target equal to `record.target` (or, if the codec has no encoder, structural equality on elements/claims/trace); the REG line is built at stage 4 from the canonical elements, as the design already says. Re-test with the three cases (D-tier copy → `near_duplicate_rephrasing` at 1.0; dict elements → `non_canonical_elements`; new narrative → accepted) and commit the test proposals and `checks.jsonl` as evidence.

Run order accepted: reachability regenerated against the pinned binary before replay; decide's "binary changed since reachability" check enforces the pin.

## MANIFEST FREEZE SIGNED — 2026-09-15 17:33 BST, `assistant/trackA/p2a-two-stage-teacher` @ `79dc50b`

Verified by me from a throwaway worktree at `79dc50b`: `lake build` 137/0; binary sha256 `9bff8f30…` equals the manifest pin and `reachability.json`'s recorded binary (reachability PASS, regenerated against the pinned binary; run order honoured). Manifest content unchanged from the `ce9968c` read (two-stage teacher, stage-1 Jaccard guard, `qwen3.8-max-0902`, `enable_thinking:false`, rounds of 20, verdict mapping, self-report agreement, fallback not pre-authorised). replay stage 1 is now a real `nyaya_ir.codec` round-trip with structural equality against `record.target`; REG built at stage 4 from canonical elements.

Replay re-tested by me on my own three proposals: D-tier copy → `near_duplicate_rephrasing`, Jaccard 1.0, `contract_id` derived; dict elements → `non_canonical_elements`; new narrative with a stale wire → `round_trip` rejection (correct: the wire's embedded prompt hash no longer matched the edited prompt, i.e. tamper protection), and the same record with its wire re-serialised by the generator → accepted, Jaccard 0.03, self-report agrees. The committed evidence artifact (`research/gates/P2a/evidence/track-b-round-trip-retest/`) shows the same three outcomes from replay's own output.

**Freeze: SIGNED at `79dc50b`.** The frozen manifest is `research/prereg/P2a-manifest.json` at that SHA (status text to be flipped to FROZEN with this signature cited in the merge commit, no other edit). Lead merges the range onto main on Track A's confirmation at the same SHA; the teacher batch then samples by API in rounds of 20 per id under the signed spend, writing `proposals.jsonl` immutably; `replay` then `decide` (expected PARTIAL until P/R exist). Any change to the manifest after this point is an amendment with a new sha and a new signature.

## Round-1 T read (partial, batch paused at the DashScope quota wall) — 2026-09-15 18:27 BST, `assistant/trackA/p2a-teacher-sampling` @ `19da65e`

Read against the pushed artifacts (sha256 `proposals.jsonl` `cdec04a0…`, `sampling_log.jsonl` `70745c61…`, `checks.jsonl` `bfe30772…`, `accepted.jsonl` `5004c246…`, `yield.json` `53d22d11…`, `gate.json` `85075958…`); byte-identical to the working-tree snapshot I took at 18:21. Every `checks.jsonl` row carries the pinned binary `9bff8f30`. Recount by `jq` on the raw files, not by the gate script.

**Counts (corrected).** 239 attempts, 12 of 14 ids reached (bns46_conspiracy, bns46_intentional_aid never sampled; bns85 and bns46_instigation 0 accepted, every attempt an API error). 118 sampler-accepted, 66 replay-accepted, decide FAIL (T 66 < 200; A_i floor unmet). API errors: 41 stage-1 + 1 stage-2 quota 403s, plus 3 content-moderation 400s (all bns69 omit:0, a recurring denominator cost for that id). ipc405_misappropriation is 10/20 sampler-accepted (8 after replay), ipc405_use_or_disposal 9/20 (5); the "20/20" figures were attempts, not acceptances. Token use so far 928k prompt / 80k completion.

**The 66/118 gap is systematic, not teacher noise.** The 66 replay-accepted records are all `control`. Every one of the 48 omission-shaped proposals was rejected `not_grounded`; the other 4 rejections are controls where stage 1 authored 1–2 facts for a 3-element contract and stage 2 still asserted `_POS` — correct refutations, 4/70 = 6 % teacher noise. The omission slice yields 0 by construction, on three defects that stack:

1. **replay contradicts the frozen manifest for non-control shapes.** Manifest `stage_2_ir_rendering.acceptance`: "a fresh REG verdict that agrees with the proposal's own self-declared shape". replay computes `self_report_agrees` (true on all 52) and then unconditionally requires `actual_verdict == "grounded"` (`p2a_yield_gate.py` ~L1100), so an omission whose checker verdict is the correctly diagnosed `flagged` can never be accepted; the same holds for denial shapes. Conformance fix, no amendment: verdict must be shape-conditional (control → grounded; omit:k → flagged with exactly element k in the omitted bucket; deny → denied).
2. **harness envelope hard-codes `kind="law_apply"`** (`p2a_sample_teacher_batch.py` ~L343) while `validators.py:143` requires kind ↔ `target.abstain`. The 16 `stage2_envelope_serialize_failed: kind/abstain mismatch` attempts (all omission shapes: bns69 7, ipc416 4, ipc405m 3, ipc405u 2) are the cases where the teacher DID abstain, i.e. imitated the D-tier omission exemplar (`law_abstain`, `ABSTAIN true missing_fact`, `NIGAMANA not_proved`) and was killed by the harness. Fix: kind derived from `target.abstain`. Raw stage-2 text is not logged on this failure path (only on parse failures), so their content cannot be audited; log it.
3. **the 48 that passed the envelope are wrong records**: element k absent from the prompt AND from the record (no `ELEMENT elk e unknown`), yet `CLAIM c_applies … BY <ID>_POS`, `NIGAMANA proved`, `ABSTAIN false`. That is the teacher concluding without an element. Nothing in the manifest's acceptance rule checks target/verdict consistency, so under fix (1) alone these would be accepted as T supervision. **Ruling (amendment, my signature): T acceptance adds target–verdict consistency** — grounded ↔ `proved`/`ABSTAIN false`; flagged (omission) ↔ `not_proved`/`ABSTAIN true missing_fact`/omitted element `unknown`/`kind law_abstain`; denied ↔ the D-tier deny convention. With that rule the 48 stay rejected, and correctly so. The stage-2 prompt invites the error: it names only the `_POS` rule and gives no omission instruction. **Amendment (pre-approved wording):** append to the stage-2 prompt: "If the prompt has no fact for an element, mark that element `unknown`, cite no claim for it, emit `NIGAMANA not_proved` and `ABSTAIN true missing_fact`, as in the omission example." New manifest sha + Track A shape sign + my signature; then the batch resumes.

**Sampler shape bug (harness, no amendment).** `shape = shape_cycle[(attempts_by_id[cid] + i) % 4]` while `attempts_by_id[cid]` is also incremented per attempt, so the index advances by 2: shapes alternate control/omit, `deny` is never reached (0 deny attempts in 239), and `k = index % n_required` collapses for even element counts — 2-element ids only ever get `omit:0`, 4-element ids only `omit:2` (observed: omit:0 74, omit:1 15, omit:2 30). For ipc405_wilfully_suffers, whose only omit index is its declared dependency, 10 of 20 attempts were rejected pre-API (`omit_targets_declared_dependency`, 19 across four ids). The manifest (guards_before_stage_2) says such attempts are "rejected at stage 1", so they legitimately sit in N_i for round 1; going forward the sampler must draw only from allowed shapes (index from `attempts_by_id` alone; k over non-dependency indices; deny for denial-bearing ids).

**decide denominator does not match the frozen `denominator_policy`.** `yield.json` N_i sums to 118 (proposals only); the manifest counts every attempt (malformed, refusal, checker error) — that is the 239 rows of `sampling_log.jsonl`. A_i/N_i is therefore inflated (ipc405_wilfully_suffers reads 1.0; it is 6/20). Conformance fix: N_i from `sampling_log.jsonl`, A_i unchanged. 26 `stage1_json_parse_error` rows (all `finish_reason: stop`, 78–196 completion tokens, "Expecting ':' delimiter" at ~600–900 chars) and 14 `KarakaRole` parse failures (`Agent`, `KARTA`, `Kartr`…) are unrecoverable without the raw text; log stage-1 raw content on failure, and the grammar block should enumerate the six role tokens (the schema's `ROLE` line does not). A `response_format: json_object` change would be a sampling-parameter amendment; not requested.

**Standing.** The 66 replay-accepted controls stand as T records (binary pinned, recount agrees, Jaccard max 0.39, 12 flagged 0.30–0.39). FAIL is the honest decide state; nothing signed, the freeze stands. Before any further spend: fixes (1), (2), sampler, denominator, logging; then the amendment (stage-2 omission instruction + target–verdict consistency rule) with a new sha routed to me; replay re-run on the existing 118 (no spend) to show the 48 still reject under the new rule and the 66 still pass; then resume to T ≥ 200 with A_i ≥ 5 on all 14 ids. Denial and omission slices must be present in T (they are 0 today) — I will not read a T tier that is 100 % controls as meeting the manifest's shape cycle.

## Teacher swap notice (operator-directed, via lead) — 2026-09-15 18:33 BST

Lead reports the operator has directed the teacher to change from `qwen3.8-max-0902` (DashScope quota exhausted) to `claude-haiku-4-5-20251001` run headless on seat 2, riding in the same amendment sha as fix 3. No objection in principle. Signature conditions sent to the lead: (1) sandbox as a binding manifest field — all tools disabled, empty cwd outside any checkout, no `--add-dir`, `--output-format json`, argv logged per row (the checker does not catch a teacher that reads the held-out sets); (2) sampling fields stated honestly — no seed/temperature/top_p claims, non-seeded, attempt_id the identity, thinking off, malformed output stays in the denominator; (3) per-record `teacher_model`, the 66 qwen controls stay as qwen records, yield split by teacher; (4) a content read of accepted records for fact–element fit is a non-optional part of the T read, since the checker validates the IR layer only. Flagged, not ruled: seat-2 load versus its pod stand-by role.

## Harness fixes 1/2/4/5 @ `efc65d9` — pre-read, one gap — 2026-09-15 18:38 BST

Scope change noted from the lead: the amendment sha will be fix 3 only; the teacher stays `qwen3.8-max-0902`; the Haiku swap and my four conditions are deferred to a possible follow-on amendment. Fine — my read said qwen was not what broke.

Read of `efc65d9` (code-only, on `assistant/trackA/p2a-teacher-sampling`): fix 2 kind derived from `target.abstain` (L350) — OK. Fix 1 replay verdict is shape-conditional (control → grounded and no denial; omit:k → flagged with the dropped element in the omitted bucket; deny → flagged with a denied bucket; any unmapped claim rejects under every shape) — OK, and the lead reports fix 1 alone passes 90/118, confirming the 24 proved-with-missing-element records need fix 3. Fix 5 N_i from `sampling_log.jsonl` with a fallback when absent — OK. Raw stage-1 content and stage-2 target logged on every failure — OK. **Gap in fix 4:** `allowed_shapes` (L506) lists `omit:k` for every k, so dependency-target omissions (ipc405_wilfully_suffers omit:0, ipc405_use_or_disposal omit:0, ipc182_misdirected_act omit:0, ipc415_damaging_act omit:1) are still drawn and still rejected pre-API; with the shorter cycle they are now 1 in 5 attempts for wilfully_suffers rather than 1 in 2. The list must exclude the dropped index in `OMISSION_DEPENDENCIES[cid]`. The shape index now advances by 1 and `deny` is reachable — OK. Control share per cycle is 2/(n+2) or 2/(n+3): 25 % for bns47 with denial absent, i.e. at the amendment (d) floor; acceptable, to be reported in yield.

## AMENDMENT fix-3-target-verdict-consistency @ `7d7f6cb` — content SIGNED at the git sha; two conditions — 2026-09-15 18:44 BST

Verified from origin (`assistant/trackA/p2a-teacher-sampling` @ `7d7f6cb`): teacher unchanged (`qwen3.8-max-0902`); my omission instruction line verbatim in the stage-2 template; `.acceptance` carries the target–verdict consistency rule (grounded ↔ proved/abstain false; flagged ↔ not_proved/abstain true missing_fact/kind law_abstain; denied ↔ D-tier deny convention); replay enforces `target_verdict_inconsistent` after the fix-1 shape check; fix-4 gap closed (`allowed_shapes` drops the `OMISSION_DEPENDENCIES` index); yield reports control share. No-spend re-run on the same 118, recounted by me: 66 accepted (all controls), 28 `not_grounded`, 24 `target_verdict_inconsistent` (all `omit:2`, the proved-with-missing-element records), binary `9bff8f30` on every row, N_i sums to 239 (fix 5 in effect), decide FAIL. This is the evidence I asked for.

**Signature: the amendment content is SIGNED at git `7d7f6cb`.** Two conditions, neither a content change:

1. **The content sha is not reproducible.** `status` cites `d9e3da0d…` and `amendments[0].prior_sha256` cites `96b3b61c…`; `amendments[0].new_sha256` is null. Neither value reproduces from the committed file by any obvious rule (raw bytes; `jq -S` with/without `status`, `signatures`, `amendments`; `json.dumps(sort_keys)`), and the raw sha of the manifest at `79dc50b`/`3c43fd5`/`efc65d9` is not `96b3b61c` either. The manifest states no rule. A freeze sha nobody can recompute is not a pin. Required: the manifest states the rule (recommended: sha256 of `json.dumps(manifest_without_status_and_signatures, sort_keys=True, ensure_ascii=False)`, computed by `p2a_yield_gate.py` and printed by a `--manifest-sha` flag), `new_sha256` populated, and `prior_sha256` recomputed by the same rule from the manifest at `3c43fd5`. Until then the binding identity of this amendment is the git sha `7d7f6cb` and this signature attaches to that.
2. **Stage-1 fact labelling bug (harness, fix before resume, no manifest change).** `stage1_prompt_obj.facts` labels the teacher's fact list by position (`enumerate(facts)` → `F_el{i}`), and the stage-1 prompt asks for a plain list "or omitted per the shape". For any `omit:k` with k < n−1 the list is one short and every later fact is mislabelled: the 12 `omit:0` records carry `F_el0`/`el0` for what is really element 1 (the D-tier exemplar keeps the true index, `F_el1`). Such records are rejected by the fix-1 shape check (21 of the 28 `not_grounded` are these), so no validity leak, but omission yield for k < n−1 is 0 by construction. Fix: stage 1 returns facts keyed by element index (`{"0": …, "2": …}` or a null at the omitted slot) and the harness labels `F_el<index>`; the stage-2 prompt gains one line: "element ids are positional: el<k> is the k-th required element; a fact `F_el<k>` supports el<k> only". If the lead treats the stage-2 line as manifest content, fold it into the next sha with the teacher decision; I pre-approve that wording.

On (1) resolved and Track A's shape-sign, merge. Resume still waits on the operator's teacher decision and on (2).

## Sha rule reconciled; T coverage shape given to d0 — 2026-09-15 18:48 BST

Track A's shape-sign is in; it independently found condition 1 (status embeds its own hash). Lead reconciled the content-sha rule to mine (exclude `status`, `signatures`, amendment sha fields; sort_keys; ensure_ascii=False; `--manifest-sha` flag); both reviewers recompute the pin on d0's next tip before merge. Resume bucket additions: Track A's deny comparison against `DENIAL_ELEMENT_BY_ID`; my condition 2. Correction sent: the four never-omitted elements are the dependency-dropped side of the frozen definitional dependencies and must stay unsampled by rule; the labelling bug is what zeroes the other indices. Coverage shape given: omission directions = every index except the dependency-dropped one; deny for 405m/405u/bns69 only; control ≥ 25 % per id; yield reports accepted per direction and per deny. Advisory (not a frozen conjunct): ≥ 1 accepted per allowed direction and ≥ 1 deny per denial-bearing id on top of A_i ≥ 5, applied as a signing check on the T read.

## Condition 1 met @ `20c6aa4` — manifest sha reproduced; amendment SIGNED at content sha `391535fa…` — 2026-09-15 18:50 BST

Rule now stated in the manifest (`manifest_sha_rule`: strip top-level `status` and `signatures` and each amendment's two sha fields, then sha256 of `json.dumps(sort_keys=True, ensure_ascii=False)`). Recomputed by me two ways: my own six-line script on the raw file from origin, and `p2a_yield_gate.py manifest-sha` from a throwaway checkout at `20c6aa4`. Both give `391535faee670db6cc074223d33aff9056a8cccd1da44f4f8075f20c52c102a4`; the prior over the `3c43fd5` base gives `7bb639390211f775bcd613b699eca0f5a4fa6662fc0ad418ae32305b6df1debf`; both equal the manifest's fields. Content diff `7d7f6cb`→`20c6aa4` touches only the sha fields, `status`, the rule text and the script. **The fix-3 amendment signature now binds to content sha `391535fa…` (git `20c6aa4`).** Lead merges on Track A's confirm. Teacher decision: operator stays on qwen3.8-max (paid credits); no Haiku amendment; the next sha carries condition 2, Track A's deny comparison and per-direction reporting only.

## Resume-bucket amendment condition-2-positional-fact-labelling @ `32aa071` — content SIGNED at `fd5ec12e…`; merge HELD on evidence — 2026-09-15 18:58 BST

Pin: my script and `manifest-sha` from a throwaway checkout both give `fd5ec12eba7040c8d404d0beff6aabebd23e4480e87391a9b792d70931874dd6`; prior `391535fa…` = merged fix-3 tip. Content diff vs main: the amendment entry plus my positional-element-id line verbatim in the stage-2 template; nothing else. Harness: stage 1 must return exactly n facts (n+1 for deny) with `""` at the omitted slot, enforced before indexing (`stage1_fact_count_mismatch`, raw logged), so `F_el<i>` is now the true index. Replay: deny branches compare the checker's denied bucket to `DENIAL_ELEMENT_BY_ID[cid]`; accepted rows gain `shape` and `teacher_model`; decide gains `accepted_by_direction_i`. Coverage shape is the one I specified. **Content: SIGNED at `fd5ec12e…`.**

**Merge HELD on one thing:** the evidence at `32aa071` was not regenerated — `research/gates/P2a/` is byte-identical to main, `accepted.jsonl` rows carry no `shape`/`teacher_model`, and `yield.json` has no `accepted_by_direction_i`. The reported "still 66/118, all-control per direction" was not produced by this code as committed, so the new reporting path is untested. Required before merge: replay + decide re-run at the tip, committed, with the fields present and 66/28/24 unchanged. Not a content objection.

## Evidence tip `a68aa45` recounted — hold released; merge and resume GO fine from Track B — 2026-09-15 19:01 BST

Manifest byte-identical to the signed `32aa071` (content sha `fd5ec12e…` carries). Evidence regenerated by this code: `accepted.jsonl` 66 rows, all `shape=control`, `teacher_model=qwen3.8-max-0902`, binary `9bff8f30`, the same 66 attempt_ids as main; `checks.jsonl` 66 / 28 `not_grounded` / 24 `target_verdict_inconsistent`, binary pinned on every row; `yield.json` T 66, N 239, `accepted_by_direction_i` present (all-control for round 1, as expected before the cycle fix), `control_share_i` reported; decide FAIL. One harness change after my sign, read and accepted: `stage1_fact_count_mismatch` becomes a fixed reason string with got/expected as their own log fields (Track A's note), no behaviour change.

Resume expectations restated for the T read: rounds of 20 per id across all 14 ids; omission directions per the coverage shape and deny for 405m/405u/bns69 actually drawn; per-direction acceptance reported; advisory ≥ 1 accepted per allowed direction and ≥ 1 deny per denial-bearing id checked at signing; a Sonnet seat-2 content read of accepted records for fact–element fit before I sign T.

## Condition-2 amendment MERGED (`c65f132`); resume snag on the teacher pin — 2026-09-15 19:07 BST

Main `c65f132` carries both amendments; content sha `fd5ec12e…` reproduces on main. Resume GO released, then stopped cleanly by d0: the paid credits do not serve the exact pinned snapshot `qwen3.8-max-0902` (135 attempts, all 403 FreeTierOnly, 0 spend); the alias `qwen3.8-max` and other qwen models succeed on paid. The lead's earlier live check had tested the alias, not the snapshot. Operator being asked: (a) enable paid access for -0902 (preferred, no amendment), or (b) amend the request string to the alias. My conditions for (b), sent to the lead: alias only if every response's returned `model` field is recorded per attempt and equals `qwen3.8-max-0902`, with any other value rejected as `teacher_model_mismatch` in the denominator; if the response reports only the alias, the resolution is unproven and a dated paid snapshot must be pinned instead and treated as a teacher change with the existing per-teacher split reporting; the amendment states request string, required resolved string and the date of the models-list check; the 135 zero-output 403s stay in N_i per the frozen policy but yield reports `infra_error_count_i` separately.

## Teacher-change amendment teacher-model-change-qwen3.7-max @ `642b856` — content SIGNED at `c87bb4d1…` — 2026-09-15 19:17 BST

Operator approved the teacher change and delegated the pick: `qwen3.7-max-2026-06-08`, a paid fixed dated snapshot whose response.model echoes the dated id (the alias failed that test and is out). Pin: my script and `manifest-sha` from a throwaway checkout both give `c87bb4d149827b9ed0ae300492857d1cbeac87b078fcaaf8d7540007d8231b08`; prior `fd5ec12e…` = merged condition-2 tip. Manifest diff vs main: model/model_snapshot_id, a snapshot note, `response_model_guard`, `sandbox` (N/A, API is the only channel), `sampling_params_note` (0.7/0.9/seed kept), the amendment entry, and the note that my content read stays mandatory. Nothing else. Code: harness records `stage1_resolved_model`/`stage2_resolved_model` from response.model and rejects `teacher_model_mismatch` into the denominator; replay reads `teacher_model` from each proposal (fallback label `qwen3.8-max-0902` for round-1 rows); decide adds `T_by_teacher`, `A_i_by_teacher`, `infra_error_count_i`. Evidence regenerated by this code: 66/28/24 unchanged, all 66 labelled `qwen3.8-max-0902`, `T_by_teacher` = {qwen3.8-max-0902: 66}, infra errors bns85 20, bns46_instigation 19, bns69 3, bns47 2. **Content: SIGNED at `c87bb4d1…` (git `642b856`).** Merge on Track A's sign; resume GO on the new snapshot.

Advisory (not blocking): the 3 bns69 rows counted as infra are DashScope content-moderation 400s (`data_inspection_failed`), a teacher-side refusal on the content, not infrastructure; report them under a `content_moderation_count_i` when the next sha happens. The hard-coded fallback teacher label should apply only to rows that predate the field; the harness now always writes it, so fine as long as that stays true.

## Round-3 read @ `da2d129` — content signal POSITIVE, but a structural HOLD (attempt_id collision on resume) — 2026-09-15 19:35 BST

New-teacher (qwen3.7-max-2026-06-08) first output: 26 replay-certified acceptances, 13 control / 4 omit:0 / 9 omit:1 — omission acceptances for the first time, so condition-2's positional fix works; Jaccard max 0.28 across the 26; binary `9bff8f30` on every row; `resolved_model` on every WRITTEN row equals the pin (api-error rows carry none, correct); 3 deny attempts drawn and correctly rejected (teacher authored no denial fact); infra vs moderation split present. That is the positive signal the lead flagged.

**HOLD — attempt_id collides across the resume boundary.** The resume restarted the sampler's counters (round_idx→1, attempts_by_id→0) instead of continuing them, so every new attempt is `t_<cid>_r1_<nnnn>`, colliding with round-1's ids. Confirmed: `sampling_log.jsonl` 283 rows / 239 unique; `proposals.jsonl` 150 / 134 (16 dupes); `accepted.jsonl` 92 / 84 (8 accepted ids each name two different records, different teacher, different content — e.g. `t_ipc405_misappropriation_r1_0000` is Kavita/sealed-parcel under qwen3.8 AND "sold the collection at auction" under qwen3.7). Consequences: (1) `proposals.jsonl` is supposed to be append-only immutable keyed by attempt_id — the key is now ambiguous; (2) `seed = sha256(attempt_id)` reuses the SAME seed (2650936628) for two different prompts, breaking the per-attempt determinism claim; (3) the `round` field is null on all 26 new records, so the manifest's promise ("every attempt carries its own round_index; a round-3 attempt is distinguishable from a round-1 attempt") fails; (4) any downstream consumer keying by attempt_id silently drops the 8 collided records. T=92 counts correctly TODAY only because decide iterates rows rather than keying by id — at 200+ the collisions multiply.

**Required before scaling (harness, no manifest change):** attempt_id must be globally unique across resume boundaries — resume continues the counter, or prefixes a run-id/teacher tag, or a persisted monotonic counter; the `round` field is populated on every record; `proposals.jsonl` append refuses an id that already exists with different content. Re-emit round 3 under unique ids before resuming to 200 (no re-spend needed if the existing 26 are re-keyed deterministically from their own content/teacher, but the seed-collision means a clean re-sample is cleaner). Content read (Sonnet seat-2, spikeF) is running in parallel; verdict to follow.

## Round-3 content read (Sonnet seat-2, spikeF) — NOT clean; narrative non-neutrality + template repeats — 2026-09-15 19:39 BST

Reader output committed at `docs/reviews/spikes/trackb-p2a-tread/content-read-r3.json`. Per-question FAILs over the 26: fact_fit 2, narrative_neutral 22, omission_real 0, denial_real 0 (n/a), coherent 0, english_register 0. The teacher itself authors well — coherent 26/26, plain-English register 26/26, omissions genuine 26/26, fact_fit 24/26 — so this is NOT a qwen3.7-max quality rejection. Two systematic supervision-quality defects, both stage-1-prompt, fold into the re-sample:

1. **F_narrative is not neutral (22/26).** The stage-1 template already asks for "neutral scene-setting, no element content", but the teacher ignores it: the narrative states el0 (the entrustment/dominion relationship) and often el2 (the governing direction) in full, before the element facts restate them. Verified directly: use_or_disposal 0003's narrative names Priya Sharma "appointed as the estate executor… the will explicitly mandated…" — that is el0 and el2 established in the narrative. The checker cannot see this (it validates only that element k's evidence span sits in F_el<k>), so a record whose narrative pre-establishes an element is certified anyway. This round no omit record leaked its OWN omitted element (the 4 omit:0 kept the narrative anonymous — 0000: "a dispute arose… legal counsel reviewed"), so no false omission slipped THIS time; at scale an omit whose narrative leaks element k would be a false omission the checker passes. This is exactly the licensed-but-wrong supervision the content read exists to catch. **Fix: a stage-1 narrative-neutrality guard (reject before stage 2 when F_narrative's content overlaps any F_el<k> beyond a threshold, or independently establishes an element), plus stronger prompt wording. A guard is more reliable than wording for a weaker model.** The 2 fact_fit FAILs are a downstream symptom (element facts redundant because the narrative already stated them).

2. **Template repeats (3 pairs in 26).** Priya-Sharma-estate-executor (0003/0007), informal-village-entrustment (0002/0012), and Elias-Thorne-archival-custodian which repeats the SAME character across two different contracts (misappropriation 0013 / wilfully_suffers 0002). The narrative-only Jaccard missed all three because wording varies while character/template repeats. **Fix: cap identical scenario frames per id (my earlier advisory ≤ 4) and add a character-name/template diversity check across ids, not just narrative-token Jaccard.**

**Verdict: HOLD the round on content too.** Both fixes fold into the re-sample d0 is already doing for the attempt_id collision. Neither changes the teacher pin. Whether the narrative-neutrality guard needs a manifest amendment (the template wording is manifest content) or rides as harness code is the lead's call; I sign either, but no round scales to T≥200 until narrative neutrality is enforced and the template-repeat rate is controlled. The 66 round-1 records stand as the T baseline unchanged.

## Next sha spec: neutrality guard + wording + diversity — pre-approved wording and guard calibration — 2026-09-15 19:42 BST

Lead takes both (guard = hard enforcement of the validity hole; stronger stage-1 wording as a manifest amendment to cut the leak rate so the guard only mops up the residual, prior c87bb4d1). My inputs so d0 builds to a signable target:

**Guard calibration (my sign checks this).** A token-overlap threshold guessed in the abstract will mis-fire. The guard must be calibrated and its calibration committed as evidence: run it over the existing round-3 26 and show it REJECTS the 22 narrative_neutral FAILs and PASSES the 4 clean omit:0 records (0000/0005/0010/0015). Gold-standard definition of a leak, to calibrate against: strip all F_el facts, leave only F_narrative, and if the checker would establish any element from narrative spans alone, the narrative leaks — the guard is a cheap proxy for that and must agree with it on these 26. Commit the guard's per-record output alongside the re-sample.

**Pre-approved stronger stage-1 neutrality wording** (drop-in for the narrative field spec, replacing "neutral scene-setting, no element content"): "A 2-3 sentence factual backdrop naming only the parties and the setting. It MUST NOT state or imply any required element: no entrustment or custodial relationship, no governing direction/contract/duty, no dishonest act, use, disposal, deception, or mental state. Test: a reader seeing ONLY this narrative must not be able to tell whether any element is satisfied. Each element's content appears ONLY in its own F_el<k> fact."

**Diversity check definition.** Cheap hard check: the primary party name in F_narrative must not repeat a prior accepted record's, across ALL ids (this alone catches Elias-Thorne-across-two-contracts and the Priya-Sharma pair). Softer: a scenario-frame signature (relationship type + object class) capped at ≤ 4/id. Narrative-token Jaccard stays but is not the diversity gate — it missed all three repeats.

I sign the sha on: pin reproduces; the wording is verbatim the above (or a variant Track A and I agree); the guard-calibration evidence shows 22 reject / 4 pass on the round-3 26. Then re-sample under guard+reworded prompt, fresh batch to me for the content read.

## Amendment narrative-neutrality-and-diversity-guards @ `42b8a90` — content SIGNED at `bba608d2…`; two architecture rulings — 2026-09-15 19:53 BST

Pin reproduces (my script = `bba608d2821659c61478232dc0eaf56b823b49637b44a27da6ff53942387ea12`; prior `c87bb4d1`). Wording verbatim mine. Diversity guard = primary-party-name hard gate across all ids + frame-signature ≤4/id soft cap, as specified. Calibration committed (`narrative_leak_guard_calibration.json`): 0 false positives on the 4 gold-clean (0000/0005/0010/0015), 9/22 recall on the gold-leaky, gold standard = my content read. Crucially the manifest describes the guard HONESTLY — "a cheap regex proxy, not a full semantic replacement for Track B's content read… reported as measured, not claimed" — and keeps the content read MANDATORY before I sign T. No overstatement, so **content SIGNED at `bba608d2…`.**

**The lead's architecture point is correct and I rule on it.** A bag-of-words guard cannot reach 22/22 because the leaks are semantic paraphrases (use_or_disposal 0003 scored zero overlap); my own gold standard ("strip F_el, does the checker establish an element from the narrative alone") is itself a semantic judgment needing an LLM. A 22/22 regex is an impossible bar; holding the sign on it would block on something that cannot exist.

**Ruling (a) — sign now, recall deferred to a measured re-sample: YES.** The signature covers the verbatim wording and the diversity guard; the 41%-recall/0-FP regex is a cheap first-line pre-filter, not the neutrality gate, and the manifest says exactly that.

**Ruling (b) — the T-validity gate at scale is a FULL content read of every accepted record, not a slice.** With a sub-100%-recall guard, weak-guard + slice-read is the one unsound combination (a leak that passes the guard and misses the slice enters T). The content read is cheap relative to teacher sampling — one Sonnet seat-2 agent read all 26 this round in a single call; a few hundred records is a handful of calls — so a full read is affordable and is the authoritative semantic check. I decline the Haiku-guard-so-the-read-can-stay-a-slice option: it puts a second, weaker model with its OWN uncalibrated recall and its own provenance into the sampling loop, to save a read that is already cheap. Operationally the full read is INCREMENTAL: each round's newly-accepted records get a full content read before they count toward T, and I sign T only when every constituent record has passed. The reworded prompt + regex pre-filter are efficiency (fewer wasted stage-2 spends, fewer leaks reaching the read), not the gate. Escalate to an LLM guard only if a measured round shows the full read is impractically large — and even then it would supplement a read, after its own recall calibration, never replace it.

Next: re-sample a small batch (~20-40) under the reworded prompt + guards + the attempt_id-collision fix; I full-read it and we measure the NEW leak rate (informs efficiency, not whether the read is needed). Merge on Track A's shape-sign.

## Diversity hard-gate flaw (my spec's fault) — reframe ruling + pre-approved wording — 2026-09-15 20:01 BST

d0's measured re-sample (round4-measured branch, 12 attempts, 0 accepted, 0 spend) rejected 9/12 as `primary_party_name_reused` — d0 flagged rather than loosening a signed guard, correct. The flaw is in MY spec, now in the manifest text I signed (line 91: "primary party name (first proper noun in the narrative)"): keying the FIRST capitalized token against ALL-TIME accepted history is a lifetime ban on common names. Worse, the extractor `_primary_party_name` returns the first Title-case token, which is often not a person — round-1's top "names" are Meera 26, Pune 23 (a city), Ravi 17, Tuesday 10, Their 10. So the gate bans cities and days too. My finding was never "no two records share a first name"; it was the SAME DISTINCTIVE CHARACTER in the SAME FRAME (Elias Thorne across two contracts; the Priya Sharma executor pair).

**Consequence to note: neutrality is STILL UNMEASURED.** 0 accepted this round is the diversity gate over-rejecting, not the reworded prompt passing — the whole point of the round (the new leak rate) is not yet known.

**Ruling (1) — scope: the hard cross-id gate fires only when BOTH the primary party's full name AND the frame signature match a prior accepted record.** The frame conjunction is the load-bearing fix: "Ravi in a warehouse entrustment" and "Ravi in a bank fraud" are different frames and must not collide; only "the same named character in the same scenario frame" is the repeat I flagged. Also fix the extractor to target a person name (the first run of one-or-more consecutive capitalized name tokens, excluding place/day/sentence-initial words), not the first capitalized token. Keep the frame-signature ≤4/id soft cap (it does the per-id anti-padding) and narrative-Jaccard (near-dup). I decline the recency-window and cap-K alternatives — both still key on the wrong thing (name alone) and either ban common names within the window or admit exact-frame repeats above K.

**Ruling (2) — manifest amendment, both sign.** The flawed rule is manifest text I signed, so correcting it is a new sha, not a harness-only fix. Pre-approved wording (drop-in for the diversity clause): "the HARD cross-id gate rejects an attempt (rejection_reason character_frame_reused) only when BOTH the primary party's full name — the first run of one or more consecutive capitalized name tokens, excluding place/day/sentence-initial words — matches a prior ACCEPTED record's AND that record's frame signature also matches: the same named character in the same scenario frame (the round-3 repeats: Elias Thorne across two contracts, the Priya Sharma executor pair). A shared common first name alone, or a shared city/day, does NOT trigger it. The frame signature (every capitalized name token in the narrative) is capped at ≤4 identical repeats per id (soft cap, scenario_frame_repeated_too_often). Narrative-token Jaccard stays as the near-duplicate guard, not the diversity gate."

Sign the next sha on: pin reproduces; wording the above or an agreed variant; a re-run of the round4 12 showing the 9 now pass the diversity gate (they are distinct scenarios, only common names collided) — that is the diversity analogue of the 22-reject/4-pass calibration. Then the measured re-sample actually produces acceptances and I do the full content read for the neutrality number.

## Amendment diversity-guard-scope-fix @ `d113977` — content SIGNED at `eaf5c49c…` — 2026-09-15 20:12 BST

Pin reproduces (my script = `eaf5c49c7292d69f5609300f0ad73ea1fa5b95aed770824b9071c79833dbb999`; prior `bba608d2`). Wording verbatim my ruling (hard gate = full-name AND frame-match, `character_frame_reused`; frame ≤4/id soft cap; Jaccard not the gate). Extractor fixed: `_primary_party_name` now skips days/months and place-preceding prepositions and greedily joins a consecutive capitalized run into a multi-word name ("Elias Thorne", "Priya Sharma"), not the bare first token; `_PROPER_NOUN_STOPWORDS` now also excludes he/she/it/they/we/i/you so pronouns no longer leak. The attempt_id-collision fix rides in this branch (ids continue as `t_<cid>_0020`, `round` field on every row, append-refuse check seeded from proposals.jsonl history).

**Equivalent-shape calibration ACCEPTED over spending 9 paid calls** (the round-4 9 rejected narratives were never stored — rejected pre-stage-2 before this commit added unconditional logging — and reproducing them costs ~9 stage-1 calls; d0 correctly did not spend). `diversity_guard_calibration.json` replays both extractors over round-1's real 118 narratives: old (first-token, name-alone-vs-all-history) → 79/118 triggers, reproducing the ~67 % over-rejection failure mode round-4 hit; new (person-name run, name AND exact-frame conjunction) → 3/118. Stronger than the literal 9: 118 real narratives, same failure mode at the same rate, fix reduces to near-zero.

**I independently verified all 3 residual new-gate triggers are genuine identical-cast repeats** (reproduced the extractor+signature against the 118 proposals): (1) ipc415_property_r1_0012 (Ravi/Meera/Jaipur, antique workshop) vs r1_0003 (Ravi/Meera/Jaipur, textile shop); (2) ipc415_damaging_act_r1_0007 (Ravi/Meera/Coimbatore, logistics) vs ipc415_property_r1_0009 (Ravi/Meera/Coimbatore, logistics depot); (3) bns47_r1_0011 vs r1_0001 — Rohan Mehta / Pune / India logistics consultancy, near-VERBATIM opening. All three are the same named characters in the same setting, cosmetically varied — exactly the low-diversity padding the gate should catch and that narrative-Jaccard missed. The hard gate keys on the exact conjunction (full_name, full capitalized-token frozenset), so only an identical cast+setting can trigger it; a shared common first name alone cannot. **Content: SIGNED at `eaf5c49c…`.** Merge on Track A's shape-sign; then the measured re-sample finally accepts records and I do the full content read for the neutrality number.
