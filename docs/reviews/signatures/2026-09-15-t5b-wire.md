# T5b atomic wire — reviewer record (Track B content/integrity gate)

## HELD on one fix — 2026-09-15 12:00 BST
Lean: `prabhasa-nyaya` `assistant/trackA/t5b-atomic-review-rebased` @ `45c4bdf` (on `db5c5cd`). Python: `pravrudhi` `assistant/trackA/t5b-python-registry` @ `467fce4`.

Verified by me (throwaway worktrees; binary built from `45c4bdf`, `lake build` 135 jobs 0 errors; Python tests run from a detached worktree at `467fce4` with the test's binary path pointed at that build):
- **No free-text parsing**: `check_registry` takes `assertions: dict[element_name, bool]`; only `True` entries become `G_SATISFIES(E(conduct,AC),E(element,EL))` claims; nothing reads or parses a model's prose. `scoreREGLine` builds the `Reading` from those claims only. Confirmed.
- **Drift test**: `--list-contracts` prints exactly the 14 ids; equals `KNOWN_CONTRACT_IDS`; `knownContractIds` is `#guard`ed against `contractForId`. Confirmed.
- **Exit criterion, all 14 ids**: for each id, `--describe-contract` names the required elements; all-met → `grounded`; first element dropped → `flagged` with exactly that element in `omitted_claims`. Recorded in `docs/reviews/spikes/trackb-wire/reachability-14-ids-2026-09-15.json` (binary sha256 inside). Confirmed.
- Unknown id refused before subprocess (`UnknownContractError`) and `MALFORMED` at the binary. Confirmed.
- Branch's own tests: 10 passed against my build.
- `abetsCommission` string reworded to cite s.45 routes as extended by s.46: matches my ruling.

**Defect (integrity):** a claim that matches a Contract's `denials` is returned in BOTH `denied_claims` and `unlicensed_claims`. `scoreREGLine` computes `denied` from `c.denials` and `ungrounded` from `c.unlicensed r`, and a denial entity is never in `axioms`, so every refutation is also counted as a hallucination. Reproduced on `bns69` (`amountsToRape`), `ipc405_misappropriation` and `ipc405_use_or_disposal` (good-faith defeater): identical claim string in both buckets. That is one condition reported twice, the same double-encoding rule enforced in the registry, and it contradicts the module's own doc ("the product must not present these three as one finding"): a consumer counting flags gets two for one.

Required fix (either side, d0's choice; Lean preferred so every consumer inherits it): exclude denied claims from the unlicensed bucket, e.g. `ungrounded := (c.unlicensed r).filter (fun cl => !c.denials.contains cl)`, plus a `#guard` on the REG path (bns69 or ipc405 limb A with the defeater asserted → denied has one entry, unlicensed is empty) and a Python test mirroring it. No other change.

Signature: **HELD** pending that fix. Re-check: rerun the recorded 14-id file plus the three denial runs and confirm `denied` = 1, `unlicensed` = 0.
